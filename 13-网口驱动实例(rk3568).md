- [1. 设备树节点](#1-设备树节点)
	- [1.1. compatible 字段](#11-compatible-字段)
- [2. `rk_gmac_dwmac_driver`](#2-rk_gmac_dwmac_driver)
- [3. rk适配层初始化](#3-rk适配层初始化)
	- [3.1. 获取资源](#31-获取资源)
		- [3.1.1. `stmmac_get_platform_resources()`](#311-stmmac_get_platform_resources)
		- [3.1.2. `stmmac_probe_config_dt()`](#312-stmmac_probe_config_dt)
		- [3.1.3. `rk_gmac_setup()`](#313-rk_gmac_setup)
	- [3.2. SoC 定制](#32-soc-定制)
		- [3.2.1. `rk_gmac_powerup()`](#321-rk_gmac_powerup)
- [4. stmmac 通用驱动初始化](#4-stmmac-通用驱动初始化)
	- [4.1. 网络子系统，私有数据初始化](#41-网络子系统私有数据初始化)
	- [4.2. GMAC 硬件探测](#42-gmac-硬件探测)
		- [4.2.1. 绑定硬件操作接口](#421-绑定硬件操作接口)
		- [4.2.2. 读取硬件特性寄存器，修正平台设置](#422-读取硬件特性寄存器修正平台设置)
		- [4.2.3. 配置高层特性](#423-配置高层特性)
	- [4.3. ndev 配置](#43-ndev-配置)
	- [4.4. NAPI 配置](#44-napi-配置)
- [5. PHY 连接](#5-phy-连接)
	- [5.1. MDIO 总线注册](#51-mdio-总线注册)
		- [5.1.1. 老旧平台的兼容](#511-老旧平台的兼容)
		- [5.1.2. 回到正轨](#512-回到正轨)
		- [5.1.3. 另一些兜底](#513-另一些兜底)
	- [5.2. 调试问题](#52-调试问题)
	- [5.3. PHY 设置](#53-phy-设置)
		- [5.3.1. phylink](#531-phylink)
- [6. 网络设备注册](#6-网络设备注册)
- [7. phylink 是什么？](#7-phylink-是什么)
- [8. 关键结构体组织方式](#8-关键结构体组织方式)


以 VDU 板子为例子，使用的是 rk3568 内置的 GMAC1 + 裕泰微YT8011 T1 PHY 芯片。

# 1. 设备树节点

还是从设备树入手，先来看 GMAC1 对应的设备树节点：

```c
    /* rk356x.dtsi */
    gmac1: ethernet@fe010000 {
            compatible = "rockchip,rk3568-gmac", "snps,dwmac-4.20a";
            reg = <0x0 0xfe010000 0x0 0x10000>;
            interrupts = <GIC_SPI 32 IRQ_TYPE_LEVEL_HIGH>,
                    <GIC_SPI 29 IRQ_TYPE_LEVEL_HIGH>;
            interrupt-names = "macirq", "eth_wake_irq";
            clocks = <&cru SCLK_GMAC1>, <&cru SCLK_GMAC1_RX_TX>,
                <&cru SCLK_GMAC1_RX_TX>, <&cru CLK_MAC1_REFOUT>,
                <&cru ACLK_GMAC1>, <&cru PCLK_GMAC1>,
                <&cru SCLK_GMAC1_RX_TX>, <&cru CLK_GMAC1_PTP_REF>,
                <&cru PCLK_XPCS>, <&cru CLK_XPCS_EEE>;
            clock-names = "stmmaceth", "mac_clk_rx",
                    "mac_clk_tx", "clk_mac_refout",
                    "aclk_mac", "pclk_mac",
                    "clk_mac_speed", "ptp_ref",
                    "pclk_xpcs", "clk_xpcs_eee";
            resets = <&cru SRST_A_GMAC1>;
            reset-names = "stmmaceth";
            rockchip,grf = <&grf>;
            snps,axi-config = <&gmac1_stmmac_axi_setup>;
            snps,mixed-burst;
            snps,mtl-rx-config = <&gmac1_mtl_rx_setup>;
            snps,mtl-tx-config = <&gmac1_mtl_tx_setup>;
            snps,tso;
            status = "disabled";
    };
```

```c
    /* rk3568-ok3568c.dts */
    &gmac1 {
            phy-mode = "rgmii";
            clock_in_out = "output";

            snps,reset-gpio = <&gpio3 RK_PB5 GPIO_ACTIVE_LOW>;
        snps,reset-active-low;
        /* Reset time is 20ms, 100ms for rtl8211f */
        snps,reset-delays-us = <0 20000 100000>;

            assigned-clocks = <&cru SCLK_GMAC1_RX_TX>, <&cru SCLK_GMAC1>;
            assigned-clock-parents = <&cru SCLK_GMAC1_RGMII_SPEED>, <&cru CLK_MAC1_2TOP>;
            assigned-clock-rates = <0>, <125000000>;

            pinctrl-names = "default";
            pinctrl-0 = <&gmac1m1_miim
                        &gmac1m1_tx_bus2
                        &gmac1m1_rx_bus2
                        &gmac1m1_rgmii_clk
                        &gmac1m1_rgmii_bus>;

            tx_delay = <0x35>;
            rx_delay = <0x00>;

            phy-handle = <&rgmii_phy1>;
            status = "okay";

    };
```

## 1.1. compatible 字段

利用 `compatible` 字段找到实际使用的 GMAC 驱动。不过特别注意到这里字段中有两个字符串，在实际的搜索中我们也会找到两个文件：

* "rockchip,rk3568-gmac" 对应 `drivers/net/ethernet/stmicro/stmmac/dwmac-rk.c`
* "snps,dwmac-4.20a" 对应 `drivers/net/ethernet/stmicro/stmmac/stmmac_platform.c`

这是 GMAC 驱动的一些特殊性导致的。GMAC 芯片有一个厂商 Synopsys，他们设计了一个被广泛使用的 IP核 DesignWare，简称 “dw”。然后 ST 觉得它很好用所以基于它搞了自己的 “stmmac”，这两个算是合作关系。后来大量的 SoC 厂商都采用他们的方案。

为了做兼容，他们就搞出了一套标准的接口，目前是 `drivers/net/ethernet/stmicro/stmmac/stmmac_platform.c`。他就是 Synopsys DesignWare GMAC 驱动的通用“平台总线驱动”。每个 SoC 厂商会在这个通用驱动的基础上搞自己的胶水驱动，胶水驱动会利用通用驱动来填空。

所以我们要看驱动源码，还是可以从 rockchip 厂商的胶水驱动 `dwmac-rk.c` 入手，一步一步了解 GMAC 芯片的初始化逻辑和操作。

---

# 2. `rk_gmac_dwmac_driver`

拉到 `dwmac-rk.c` 底部，可以看到 GMAC 这个平台设备的驱动结构体：

```c
static struct platform_driver rk_gmac_dwmac_driver = {
	.probe  = rk_gmac_probe,
	.remove = rk_gmac_remove,
	.driver = {
		.name           = "rk_gmac-dwmac",
		.pm		= &rk_gmac_pm_ops,
		.of_match_table = rk_gmac_dwmac_match,
	},
};
```

查看用于匹配的结构体 `of_match_table = rk_gmac_dwmac_match` 可以发现，每一个 `compatible` 字段都对应了一个 `data`，而我们的 rk3568 自然也是有一个自己的 `rk3568_ops`：

```c
static const struct rk_gmac_ops rk3568_ops = {
	.set_to_rgmii = rk3568_set_to_rgmii,
	.set_to_rmii = rk3568_set_to_rmii,
	.set_to_sgmii = rk3568_set_to_sgmii,
	.set_to_qsgmii = rk3568_set_to_qsgmii,
	.set_rgmii_speed = rk3568_set_gmac_speed,
	.set_rmii_speed = rk3568_set_gmac_speed,
	.regs_valid = true,
	.regs = {
		0xfe2a0000, /* gmac0 */
		0xfe010000, /* gmac1 */
		0x0, /* sentinel */
	},
};
```

稍微看一下大概就是一些配置 GMAC 数据总线速率的操作函数和 GMAC 片上地址。

---

# 3. rk适配层初始化

现在来看一看胶水驱动的 `probe` 函数，看看 GMAC 控制器的初始化逻辑。

`rk_gmac_probe()` 主要工作有：

1. 获取资源：从系统和设备树中解析硬件信息和配置，并保存到对应结构体中。
2. SoC 定制：通过配置 GRF 和时钟，让 MAC 适配 RK3568 芯片。
3. 连接 PHY：建立与外部 PHY 芯片的通信。
4. 移交控制：将配置好的硬件交给通用驱动来启动和管理

值得注意的是，`dwmac-rk.c` 作为 rockchip 芯片为 stmmac 驱动定制的适配层，本质上就是有一些自己的结构体数据作为 `data`，配置好硬件后将控制权移交给通用的 stmmac 驱动。所以 3 和 4 步作为通用步骤，其实都被放在了一个接口函数中： `stmmac_dvr_probe()`。该函数的描述也是：

```c
/**
 * stmmac_dvr_probe
 * @device: device pointer
 * @plat_dat: platform data pointer
 * @res: stmmac resource pointer
 * Description: this is the main probe function used to
 * call the alloc_etherdev, allocate the priv structure.
 * Return:
 * returns 0 on success, otherwise errno.
 */
int stmmac_dvr_probe(struct device *device,
		     struct plat_stmmacenet_data *plat_dat,
		     struct stmmac_resources *res);
```

## 3.1. 获取资源


其中定义了三个结构体(指针)：

```c
	struct plat_stmmacenet_data *plat_dat;
	struct stmmac_resources stmmac_res;
	const struct rk_gmac_ops *data;
```

`plat_stmmacenet_data` 应该是一个和平台设备生命周期绑定的一个 GMAC 私有数据结构，其中包含有各种基本信息。`stmmac_resources` 则是包含 GMAC 各个中断号、MAC 地址以及 IO 映射地址的资源结构体。最后一个就是刚刚说过的对应芯片的操作函数组。

然后就是为这几个结构体初始化：

```c
data = of_device_get_match_data(&pdev->dev);
```

函数内部就是再次通过 `compatible` 字段来找到那个对应的 `data`。

### 3.1.1. `stmmac_get_platform_resources()`

接下来的初始化就复杂多了。我们进入到 `stmmac_get_platform_resources()` 中，看函数名我们大概也能够猜到它是在初始化 `stmmac_resources` 的。

```c
int stmmac_get_platform_resources(struct platform_device *pdev,
				  struct stmmac_resources *stmmac_res)
{
    memset(stmmac_res, 0, sizeof(*stmmac_res));
    stmmac_res->irq = platform_get_irq_byname(pdev, "macirq");
    stmmac_res->wol_irq =
		platform_get_irq_byname_optional(pdev, "eth_wake_irq");
    stmmac_res->lpi_irq =
		platform_get_irq_byname_optional(pdev, "eth_lpi");
    stmmac_res->addr = devm_platform_ioremap_resource(pdev, 0);
    return PTR_ERR_OR_ZERO(stmmac_res->addr);
}
```

果不其然其中的主要操作就是从设备树属性中获取中断号，然后就是进行 GMAC 寄存器的 IO 映射。内部的具体细节先忽略。不过可以注意到结构体中明显给还有个 MAC 地址没有获取，别着急，就在下面。

### 3.1.2. `stmmac_probe_config_dt()`

在 `stmmac_probe_config_dt()` 中进行了更为细致的初始化，该函数的注释写道：“解析设备树驱动参数”。

首先就是利用 `devm_kzalloc()` 来为 `plat_stmmacenet_data *plat` 分配内核内存并将其与 `device` 结构体的生命周期绑定。这一步应该比较熟悉了。然后就是 `of_get_mac_address()`，顾名思义，就是获取 MAC 地址了。我们到内部看了发现首先就是通过查找 `device_node` 的属性看看有没有 MAC 地址，都没有找到就调用 `of_get_mac_addr_nvmem()`。

这里的 `nvmem` 指的是 `non-volatile mem` 非易失性存储器。这个函数的意思大概是从片上的非易失性存储器中获取 GMAC 芯片的硬件地址(注意区分，不是设备树节点的那个地址)。 简单来说就是芯片会将 MAC 地址固定烧录到一个存储中，然后配置设备树节点，有一个 `nvmem-cells`的属性会指向该存储。通过这个 handle 来获取 MAC 地址。不过很可惜咱们这里没有走条路，因此在代码层面上看会发现 `of_get_mac_address()` 会返回一个错误码，不过只要不是 `-EPROBE_DEFER`，就会执行 `eth_zero_addr()`，暂时将 MAC 地址置 `0`。

我们继续往后看吧，接下来是 `device_get_phy_mode()`。这里唯一需要注意的是它使用的结构体是 `device -> fwnode_handle`。这个 `fwnode_handle` 是一个固件抽象层。因为 Linux 内核中，外设的组织方式不只有设备树一种，还有一种 ACPI (x86下比较常见)。他们的设备节点数据结构有差异，但是为了统一访问接口，所以内核中设计了这么一个 `fwnode_handle` 的固件抽象层，我们想要获取一些统一的属性，可以调用 `fwnode` 的相关接口来避免设备组织方式的不同。这里大概一个体现。

同时我们会注意到在 `device` 这个内核设备模型中也包含有 `fwnode_handle`，它与 `device->of_node` ，或者说 `device_node` 设备节点中的 ` fwnode_handle` 其实是一个。

总之， `device_get_phy_mode()` 通过 `fwnode_handle` 获取到设备树节点中的 `phy_mode` 属性并传递给 `plat->phy_interface`。这里还有一个 `mac_mode`，不过咱们的设备树节点没有这个属性，所以最终会设置为与 `phy_mode` 一致。他们之间应该是有一些区别的。

接下来是一系列从设备树节点中读取属性并存放到 `plat` 结构体中：

```c
    plat->phy_node = of_parse_phandle(np, "phy-handle", 0);
    plat->phylink_node = np;
    of_property_read_u32(np, "max-speed", &plat->max_speed);
    plat->bus_id = of_alias_get_id(np, "ethernet");
    ...
```

这里通过 `phy-handle` 句柄将 PHY 芯片的设备节点解析到，并存放到 `plat->phy_node` 中。`phylink` 应该也是一个很重要的概念。

随后通过 `of_device_is_compatible()` 进行了第二轮的字段匹配，这个大概就是我们 GMAC 芯片的 `compatible` 字段有第二个字符串的用处？这里应该是匹配到了 "snps,dwmac-4.20a"，然后配置了：

```c
    plat->has_gmac4 = 1;
    plat->has_gmac = 0;
    plat->pmt = 1;
    plat->tso_en = of_property_read_bool(np, "snps,tso");
```

> TSO：TCP Segmentation Offload。这是一种利用网卡硬件来分担 CPU 负载的技术。开启该功能后内核的 TCP/IP 协议栈可以向网卡发送远大于网络 MTU(最大传输单元)的数据块。网卡硬件会自动将大块数据分割成多个符合 MTU 要求的小数据包，并负责添加 IP 头、复制 TCP 头、计算校验和等。能够显著降低 CPU 负载，不过该功能仅在较新的 GMAC4 及以上版本的硬件中支持。

接下来就是一大段的读取设备树节点配置了，中间有很多 “snps,*” 的属性，这些都是 Synopsys DW GMAC 的一些通用配置属性，具体的描述可以在 `Documentation/devicetree/bindings/net/snps,dwmac.yaml` 中找到。(有些多我暂时搁置了)

配置好了以后函数会将这个 `plat` 结构体指针返回到外面，我们又回到了 `rk_gmac_probe()`。 Synopsys 的 GMAC 有不同版本，主要就是 gmac 和 gmac4。这里是在 第二次 `compatible` 的时候配置的，上面的代码可以发现。

### 3.1.3. `rk_gmac_setup()`

接下来就是将一些 rk 定制的一些函数指针传递给 `plat`。然后初始化了一个 rk GMAC 的私有数据结构 `bsp_priv`，它本来是一个 `void*`，这里给他赋值为 `rk_priv_data`。

```c
plat_dat->bsp_priv = rk_gmac_setup(pdev, plat_dat, data);
```

注意到这个私有数据结构也与 `struct device` 的生命周期进行了绑定，所以要与 `plat` 进行区分。 `plat` 是 DW GMAC 通用驱动的数据结构，虽然也是我们当前驱动私有的数据结构，但是内容是通用驱动的。而 `bsp_priv` 则是 rk 正经的定制私有数据结构。为其分配内核空间后，解析设备树属性来填充 `bsp_priv` 结构体。

---

## 3.2. SoC 定制

完成 `rk_gmac_setup()` 后就是一系列的调用：

```c
/* common system unit */
rk_gmac_csu_init(plat_dat);

ret = rk_gmac_clk_init(plat_dat);

rk_gmac_powerup(plat_dat->bsp_priv);

stmmac_dvr_probe(&pdev->dev, plat_dat, &stmmac_res);

dwmac_rk_create_loopback_sysfs(&pdev->dev);
```

在 `rk_gmac_csu_init()` 中，通过 rockchip 定义的接口获取了 `aclk` 和 `pclk` 这两个关键的总线时钟。

> `aclk` 是 AHB/AXI 总线时钟。这是高速总线时钟，服务于 DMA 和高带宽数据搬运。
> `pclk` 是 APB 总线时钟。它是低速外设总线时钟，服务于**寄存器配置接口**。CPU 通过这条总线来读写 MAC 内部的寄存器。

不过似乎在这个驱动中并没有使用它，csu 模块在当前并没有使用。

`rk_gmac_clk_init()` 中就是在对 GMAC 要使用的时钟进行初始化。看起来就是通过时钟 ID 将对应的时钟绑定到 `bsp_priv` 中。

### 3.2.1. `rk_gmac_powerup()`

在函数中检查了对应模式的操作函数是否存在，并使能时钟：

```c
ret = rk_gmac_check_ops(bsp_priv);
ret = gmac_clk_enable(bsp_priv, true);
```

不过似乎使能时钟可能失败？所以需要去配置 GMAC/PHY 模式的对应寄存器，这里就会调用操作函数，比如 RGMII 就是 `rk3568_set_to_rgmii()`。而怎么去找到要配置的寄存器的呢？是一个叫做 grf 的关键结构。

grf 全称为 General Register File，它在设备树中也会有一个节点：

```c
grf: syscon@fdc60000 {
    compatible = "rockchip,rk3568-grf", "syscon", "simple-mfd";
    reg = <0x0 0xfdc60000 0x0 0x10000>;
    ...
}
```

可以把它理解成 Rockchip SoC 的“系统控制中心”或“万能配置寄存器组”。它是一个由众多寄存器组成的集合，软件通过配置这些寄存器，来控制芯片的许多基础且重要的功能。它的主要职责包括：

* IOMUX 控制（引脚功能复用）：决定一个物理引脚是作为 GPIO、UART、I2C 还是其它功能接口使用。

* GPIO 控制：配置 GPIO 引脚的上拉/下拉电阻、驱动能力等电气特性。

* 系统控制：处理一些通用的系统级设置。

* 系统状态记录：存放一些表示 SoC 当前状态的信息。

简单来说，许多外设（如你之前关注的 GMAC）要想正常工作，通常都需要先去 GRF 里进行一番“配置”。

> 在这里会自然而然想到一个问题，grf 看起来也是在配置寄存器，而在最开始我们也是将 GMAC 芯片的寄存器地址通过 IO 映射到了内存区域中。这两个都是在操作 GMAC，有什么区别吗？
> 查阅资料后大致分清楚了两者的关系。 GMAC的寄存器 IO 映射后，操作它是配置 GMAC 芯片内部的工作逻辑，比如数据包收发开关，CRC校验等。这些操作完全是 GMAC 内部控制，与 GMAC 是怎么与 SoC 引脚是没有关系的
> 而 grf 则是 GMAC 芯片如何与外部世界(PHY 芯片以及引脚)连接的。具体包括 SoC 的引脚复用控制、接口的时序调整以及 PHY 的模式配置。
> 两者要协同配置，才可以是使得 GMAC 芯片正常工作。

其他外设的设备树节点可以通过 phandle 来引用 grf。 然后可以通过特定的接口函数来获取 grf 的句柄。比如这里我们在 `rk_gmac_setup()` 中使用了 `syscon_regmap_lookup_by_phandle()` 来获取：

```c
bsp_priv->grf = syscon_regmap_lookup_by_phandle(dev->of_node,
							"rockchip,grf");
```

后续我们在配置 GMAC 寄存器时就会使用到这个句柄。比如这里我们的 `rk3568_set_to_rgmii()` 中就会使用 `regmap_write()` 来配置特定的外设寄存器。

完成配置后会上电 GMAC 并再次使能时钟。然后还有个 `pm_runtime_get_sync()`，看起来和电源管理技术啥的有关，我暂时没管。

接下来就是调用通用 stmmac 驱动的 `probe` 函数了。这个应该是 GMAC 驱动的核心。

---

# 4. stmmac 通用驱动初始化

在完成了 GMAC 芯片的定制化配置，私有结构体的初始化并上电后，适配层就需要进行 stmmac 通用驱动的 `probe` 了。

stmmac 通用驱动的核心源码就在 `drivers/net/ethernet/stmicro/stmmac/stmmac_main.c` 和 `drivers/net/ethernet/stmicro/stmmac/stmmac_platform.c`

而通用驱动的入口就是 `dwmac-rk.c` -> `rk_gmac_probe()` 中调用的 `stmmac_dvr_probe()`。这个 `probe` 比较复杂，完全可以单拎出来分析。这里我们可以大致理一下它的关键流程。

就像适配层驱动有自己的私有数据 `plat_stmmacenet_data`，`rk_priv_data`， `stmmac_resources` 一样，通用驱动也会有自己的自由结构，这个结构体会将设备-驱动相关的结构体以及私有数据组织在一起，通过指针保证相互能够找到。

在适配层中有着这样的关系：

```c
plat_stmmacenet_data -> bsp_priv = rk_priv_data
  stmmac 平台数据                   rk 平台数据

stmmac_resources
  stmmac 平台资源
```

而在 stmmac 通用驱动中则有一个驱动自己的私有数据结构 `stmmac_priv` 其中有：

```c
priv->plat = plat_dat;
priv->* = res -> *;
```

简单来看就是 平台私有数据下有一个 `plat` 指针，会指向 `plat_stmmacenet_data` 将其关联，而对 `stmmac_resources` 则是有很多相同的成员，通过赋值获取这些资源。

这是私有数据的一个组织结构。现在我们来看看通用驱动到底干了些什么：

1. 网络子系统，私有数据初始化：`devm_alloc_etherdev_mqs()`。
2. 硬件探测与特性声明：`stmmac_hw_init()`。
3. PHY 连接：`stmmac_mdio_register()` + `stmmac_phy_setup()`。
4. NAPI 准备：`stmmac_napi_add()`。
5. 设备注册：`register_netdev()`。

咱们一步一步来看。

---

## 4.1. 网络子系统，私有数据初始化

这一步可以看作是数据准备阶段。类似于网络设备在内核中有自己的子系统来单独管理：`net_device`。同时还有专用的接口函数。

之前我可能搞混了一件事情，字符设备的 `cdev` 其实是一种设备类型，不能算作是子系统，而之前接触道德 hwmon 和这里的网络子系统，才是正经的子系统。 `cdev` 只能算作是一个接口标准。而 hwmon 以及网络子系统则是功能上的完整结构。

这是我看了相关源码后的一个发现，hwmon 和 网络子系统的注册流程非常相似，倒是 `cdev` 是一个比较单独的流程。

D老师给了一个非常形象化的比喻，如果将驱动程序看作是“电器”，那么 `cdev` 和 `sysfs` 则是一种“插座”标准，只要电器支持就可以使用这个插座。而子系统则像是具体的电器品类，这一个品类有自己的独特特性和机制，但是只要有合适的“插头”，就可以使用对应的“插座”。

```c
ndev = devm_alloc_etherdev_mqs(device, sizeof(struct stmmac_priv),
				       MTL_MAX_TX_QUEUES, MTL_MAX_RX_QUEUES);
```

```c
/* net/devres.c */
struct net_device *devm_alloc_etherdev_mqs(struct device *dev, int sizeof_priv,
					   unsigned int txqs, unsigned int rxqs)
{
	struct net_device_devres *dr;

	dr = devres_alloc(devm_free_netdev, sizeof(*dr), GFP_KERNEL);
	if (!dr)
		return NULL;

	dr->ndev = alloc_etherdev_mqs(sizeof_priv, txqs, rxqs);
	if (!dr->ndev) {
		devres_free(dr);
		return NULL;
	}

	devres_add(dev, dr);

	return dr->ndev;
}
```

这里面干的事情和之前的 hwmon 子系统有一点类似，它创建了一个 `devres`，这个结构下有子系统的数据，然后通过 `devres_alloc()` 和 `devres_add` 将其与设备节点 `device` 关联，将其挂在 `device->devres_head` 下。这个可能是子系统的一个标准做法。

```c
SET_NETDEV_DEV(ndev, device); /* ndev->dev.parent = device */
```

将 `ndev` 与 `device` 关联，这样就实现了一个双向的查找(从 `device` 到 `ndev` 已经实现了)。

接下来就是私有数据的初始化配置：

```c
    priv = netdev_priv(ndev);  
	priv->device = device;
	priv->dev = ndev;

	stmmac_set_ethtool_ops(ndev);
	priv->pause = pause;
	priv->plat = plat_dat;
	priv->ioaddr = res->addr;
	priv->dev->base_addr = (unsigned long)res->addr;
	priv->plat->dma_cfg->multi_msi_en = priv->plat->multi_msi_en;

	priv->dev->irq = res->irq;
	priv->wol_irq = res->wol_irq;
	priv->lpi_irq = res->lpi_irq;
	priv->sfty_ce_irq = res->sfty_ce_irq;
	priv->sfty_ue_irq = res->sfty_ue_irq;
    ...
```

这里有一个值得注意的点：`priv = netdev_priv(ndev);`，而这个函数内部为：

```c
/* include/linux/netdevice.h */
static inline void *netdev_priv(const struct net_device *dev)
{
	return (char *)dev + ALIGN(sizeof(struct net_device), NETDEV_ALIGN);
}
```

长得是不是蛮奇怪的？他并不是一个赋值，而是一个指针位移。那么为啥这么做呢？其实和前面的 `devm_alloc_etherdev_mqs()` 有关系。因为这个接口函数不仅声明了 `ndev` 还在其后申请了大小为 `sizeof(struct stmmac_priv)` 的连续内存空间！这不就是我们的 `priv` 吗。所以这个私有数据在内存的位置是紧跟在 `ndev` 后的，并且是按照 `NETDEV_ALIGN` 对齐的。

为什么这样设计？D老师告诉我这是内核经典的“**容器模式**”：
1. **分离“通用”与“专用”**：内核的网络子系统只关心 `net_device`，私有结构体 `stmmac_priv` 内核不关注，因此这里是两个结构体。
2. **一次分配连续内存**：`alloc_etherdev` 一次性分配连续内存，避免了两次独立分配带来的内存碎片和额外开销，同时也方便内存管理。
3. **高效转换**：通过这里的 `netdev_priv` 宏，在驱动中可以通过指针唯一快速地由 `net_device` 获取到驱动的私有数据。

在配置私有数据的时候这里还将设备结构 `device` 与 `ndev` 给关联了起来(其实之前不已经关联了吗？)：

```c
dev_set_drvdata(device, priv->dev);
```

虽然已经见怪不怪了，这里非常多的结构体指针总是会在成员中相互指来指去的，这里又多了一个 `device->drvdata = ndev`。总之就是顶层的 GMAC 设备结构可以更快速地找到 网络子系统结构体和私有结构了。


此外还初始化了工作队列和内核进程(maybe)，还有 reset control 相关的。这一部分先 mark 一下。

## 4.2. GMAC 硬件探测

核心函数就是 `stmmac_hw_init()`。我们直接进入函数内部进行查看。

```c
/**
 *  stmmac_hw_init - Init the MAC device
 *  @priv: driver private structure
 *  Description: this function is to configure the MAC device according to
 *  some platform parameters or the HW capability register. It prepares the
 *  driver to use either ring or chain modes and to setup either enhanced or
 *  normal descriptors.
 */
static int stmmac_hw_init(struct stmmac_priv *priv);
```

这个函数最主要的作用就是“**探明硬件信息，确定工作模式，绑定底层回调**”：

### 4.2.1. 绑定硬件操作接口

其中比较核心的是 `stmmac_hwif_init()`。它会去读取 Synopsys ID，根据 ID 从预定义好的表中匹配对应的回调函数集。

这里有一个核心结构体： `mac_device_info`。它就是“**MAC 硬件操作手册**”：

```c
struct mac_device_info {
	const struct stmmac_ops *mac;
	const struct stmmac_desc_ops *desc;
	const struct stmmac_dma_ops *dma;
	const struct stmmac_mode_ops *mode;
	const struct stmmac_hwtimestamp *ptp;
	const struct stmmac_tc_ops *tc;
	const struct stmmac_mmc_ops *mmc;
    ...
};
```

该结构体中包含有一系列函数指针，而我们 `stmmac_hw_init()` 的核心任务之一就是根据探测到的硬件版本号，绑定对应的操作函数。

### 4.2.2. 读取硬件特性寄存器，修正平台设置

现代的 GMAC 核都有 HW Feature 寄存器（如 HW_FEATURE），通过读它，驱动能精确知道这个芯片到底支持什么。

```c
priv->hw_cap_support = stmmac_get_hw_features(priv);
```

如果硬件支持特性寄存器，则 `priv->hw_cap_support == 1`，然后进入分支：

```c
if (priv->hw_cap_support) {
    dev_info(priv->device, "DMA HW capability register supported\n");

    /* We can override some gmac/dma configuration fields: e.g.
		 * enh_desc, tx_coe (e.g. that are passed through the
		 * platform) with the values from the HW capability
		 * register (if supported).
		 */
    ...
}
```

在分支中会根据特性寄存器中的值来重写 `priv->plat` 中的一些特性值。确保所有的功能都是硬件支持的，避免出现设备树配置说支持，而实际硬件不支持的情况。

### 4.2.3. 配置高层特性

在 `stmmac_hw_init()` 的最后是一些高层功能的配置。我没有太看懂这一个部分。就先不纠结这里。

## 4.3. ndev 配置

在完成硬件探测后，还需要把这些硬件特性同步给我们的网络子系统，所以就有这里的一大段配置：

```c
    ...
    stmmac_check_ether_addr(priv); /* 检查 MAC 地址，如果无效就分配随机 MAC 地址 */

    ndev->netdev_ops = &stmmac_netdev_ops; /* 绑定网卡操作函数 */

    ndev->hw_features = NETIF_F_SG | NETIF_F_IP_CSUM | NETIF_F_IPV6_CSUM |
                    NETIF_F_RXCSUM;
    ret = stmmac_tc_init(priv, priv);
        if (!ret) {
            ndev->hw_features |= NETIF_F_HW_TC;
        }

    	if ((priv->plat->tso_en) && (priv->dma_cap.tsoen)) {
		ndev->hw_features |= NETIF_F_TSO | NETIF_F_TSO6;
		if (priv->plat->has_gmac4)
			ndev->hw_features |= NETIF_F_GSO_UDP_L4;
		priv->tso = true;
		dev_info(priv->device, "TSO feature enabled\n");
	}

	if (priv->dma_cap.sphen && !priv->plat->sph_disable) {
		ndev->hw_features |= NETIF_F_GRO;
		priv->sph_cap = true;
		priv->sph = priv->sph_cap;
		dev_info(priv->device, "SPH feature enabled\n");
	}
```

这一段就是根据硬件特性(`stmmac_hw_init()`读取的)，声明网卡支持的功能，比如这里的 TSO，SPH等。

剩下还进行了很多配置，比如设置 DMA 掩码、初始化 RSS (接收端缩放)、设置 MTU 范围、初始化锁和消息级别等等。

这一部分就是在完成硬件探测后，对 `net_device` 结构体进行配置填充。


## 4.4. NAPI 配置

NAPI (New API) 是 Linux 内核为了解决“**接收中断风暴**”而引入的一种**混合中断+轮询**的数据包接收机制。在 NAPI 之前，网卡每受到一个数据包就会触发一次 CPU 中断。当网络流量极大时，CPU 就会被中断淹没。

而 NAPI 有特殊的机制，一次中断来临后，通过轮询+批量取包来避免大量 CPU 中断，在这个过程中关闭了网卡中断。

通过 `stmmac_napi_add()` 将轮询操作函数注册到 `net_device` 上。

# 5. PHY 连接

这里比较有意思，MDIO 总线注册的代码是这样的：

```c
	if (priv->hw->pcs != STMMAC_PCS_TBI &&
	    priv->hw->pcs != STMMAC_PCS_RTBI) {
		/* MDIO bus Registration */
		ret = stmmac_mdio_register(ndev);
		if (ret < 0) {
			dev_err_probe(priv->device, ret,
				      "%s: MDIO bus (id: %d) registration failed\n",
				      __func__, priv->plat->bus_id);
			goto error_mdio_register;
		}
	}
```

这里首先判断了 `priv->hw->pcs` 不是 `TBI` 和 `RTBI` 才决定进行 MDIO 总线注册的。那么这个 pcs 是什么？ TBI 又是什么？

PCS (Physical Coding Sublayer)指物理编码子层，是 OSI 七层模型中物理层的一个子层。它位于 MAC 与 PMA 之间。这里 `priv->hw->pcs` 记录了当前 MAC 硬件配置的 PCS 接口类型，我们常见的就是 `RGMII` 和 `SGMII`。

而 TBI 是一种特殊的模式(Ten-Bit Interface，十位并行接口)，它常见于 MAC 直连 MAC 或者连接 SFP 光模块的场景。大概就是无需外部 PHY 管理或者 PCS 内部有自管理。 简单来说就是不需要 MDIO 总线。

我们还是直接进入 `stmmac_mdio_register()` 内部看一看吧。

## 5.1. MDIO 总线注册

关键结构体是 `struct mii_bus *new_bus`，这个结构体应该就是对应的 MDIO 总线。而 `struct device_node *mdio_node` 则是 MDIO 总线这个设备树节点对应的内核结构。

> 这里我总觉的 MII 和 MDIO 的概念并没有做区分呢。这里需要注意，其实是因为早期是没有 MDIO 的概念的，IEEE 802.3 标准中定义的是 MII 总线。而 MDIO 是它的子集。所以内核中的 `mii_bus` 其实就是 MDIO 总线的结构体，**只是命名没有更新**。
> 并且要注意的是内核只抽象了 MDIO 总线，对那个数据总线比如 RGMII，是没有办法抽象的，他们是纯硬件电路连接。


`of_mdiobus_register()` **就是总线注册的核心**。而实际的接口是 `__of_mdiobus_register()`：

```c
int __of_mdiobus_register(struct mii_bus *mdio, struct device_node *np,
			  struct module *owner);
```

在里面对 `mii_bus` 进行了一些基础配置(比如解析设备树节点属性)。很重要的一步肯定是要扫描 MDIO 总线上的 PHY 设备了，这一步是通过 `__mdiobus_register()` 来完成的。

```c
/**
 * __mdiobus_register - bring up all the PHYs on a given bus and attach them to bus
 * @bus: target mii_bus
 * @owner: module containing bus accessor functions
 *
 * Description: Called by a bus driver to bring up all the PHYs
 *   on a given bus, and attach them to the bus. Drivers should use
 *   mdiobus_register() rather than __mdiobus_register() unless they
 *   need to pass a specific owner module. MDIO devices which are not
 *   PHYs will not be brought up by this function. They are expected
 *   to be explicitly listed in DT and instantiated by of_mdiobus_register().
 *
 * Returns 0 on success or < 0 on error.
 */
int __mdiobus_register(struct mii_bus *bus, struct module *owner);
```

完善 `mii_bus` 配置后，调用 `device_register()` 完成对 MDIO 总线设备的注册。

初始化总线的互斥锁：

```c
	mutex_init(&bus->mdio_lock);
	mutex_init(&bus->shared_lock);
```

### 5.1.1. 老旧平台的兼容

中间还有一部分是获取 总线的复位 GPIO 口(没有理解说错的话)。然后就是扫描总线上的 PHY 了。

```c
	for (i = 0; i < PHY_MAX_ADDR; i++) {
		if ((bus->phy_mask & BIT(i)) == 0) {
			struct phy_device *phydev;

			phydev = mdiobus_scan(bus, i);
			if (IS_ERR(phydev) && (PTR_ERR(phydev) != -ENODEV)) {
				err = PTR_ERR(phydev);
				goto error;
			}
		}
	}
```

不过有一些诡异，这个循环其实是不会进入 `if` 分支的，因为在 `__mdiobus_register()` 外，也就是 `__of_mdiobus_register()` 中用：

```c
	/* Mask out all PHYs from auto probing.  Instead the PHYs listed in
	 * the device tree are populated after the bus has been registered */
	mdio->phy_mask = ~0;
```

将 `phy_mask` 置为全 `1` 了。注释告诉我们在总线注册的过程中不会再去扫描总线来找 PHY 了！而是再总线注册完成后 PHY 设备才会连接。这应该是为设备树做的优化，因为设备树明确了 PHY 地址，不再需要扫描总线来浪费时间了。

最后好像调用了一个 `mdiobus_create_device` 来创建了一个 `mdio_device` 设备，这个设备下包含前面的 `mii_bus` 设备。这套来套去的好复杂。

不对，我是默认了 MDIO 总线上只会有 PHY，所以应该都是 `phy_device` 作为 MDIO 总线上的设备结构，但是不是这样的，就像 I2C 总线上的设备有一个统一的结构体 `i2c_client`，MDIO 总线上也有一个设备抽象就是 `mdio_device`。因为 MDIO 总线上除了有 PHY，还会有以太网交换机等其他设备，需要一个更通用的设备结构体。

```c
mdiobus_setup_mdiodev_from_board_info(bus, mdiobus_create_device);
```

这个和前面的扫描 PHY 芯片一样，同样是用来解决未使用设备树的老旧平台上怎么去注册设备，这个 `mdiobus_create_device` 就是用于注册设备的。

所以最后这两个部分和现代 Linux 使用设备树是没关系的。有了设备树后，所有的设备信息都是在设备树文件中**静态声明**的。不需要单独在 C 代码中通过类似 `mdiobus_register_board_info()` 进行预先注册。

### 5.1.2. 回到正轨

在 `__mdiobus_register()` 内部进行了老旧平台的兼容工作(注册 MDIO 总线设备)。而回到 `__of_mdiobus_register()` 中，在完成了 MDIO 总线设备(mii_bus)的注册后，对 MDIO 设备树节点进行子节点变量并注册其中的 PHY 设备：

```c
/* Loop over the child nodes and register a phy_device for each phy */
	for_each_available_child_of_node(np, child) {
		addr = of_mdio_parse_addr(&mdio->dev, child);
		if (addr < 0) {
			scanphys = true;
			continue;
		}

		if (of_mdiobus_child_is_phy(child))
			rc = of_mdiobus_register_phy(mdio, child, addr);
		else
			rc = of_mdiobus_register_device(mdio, child, addr);

		if (rc == -ENODEV)
			dev_err(&mdio->dev,
				"MDIO device at address %d is missing.\n",
				addr);
		else if (rc)
			goto unregister;
	}
```

这里 `of_mdio_parse_addr()` 是在查看子节点的 `reg` 属性，一个 MDIO 总线上可以挂 32 个 PHY，因此地址从 `0~31` 都是合法的。比如我们的 PHY 节点就是：

```c
&mdio1 {
	rgmii_phy1: phy@1 {
		compatible = "ethernet-phy-ieee802.3-c22";
		reg = <0x1>;
		clocks = <&phy_xtal>;
		//interrupt-parent = <&gpio3>;
		//interrupts = <RK_PB6 IRQ_TYPE_LEVEL_LOW>;
		//reset-gpios = <&gpio3 RK_PB5 GPIO_ACTIVE_LOW>;
	};
};
```

这里 `reg` 就是 1。然后判断该子节点是否是 PHY 设备：`of_mdiobus_child_is_phy()`：

```c
/* drivers/net/mdio/of_mdio.c */
bool of_mdiobus_child_is_phy(struct device_node *child)
{
	u32 phy_id;

	if (of_get_phy_id(child, &phy_id) != -EINVAL)
		return true;

	if (of_device_is_compatible(child, "ethernet-phy-ieee802.3-c45"))
		return true;

	if (of_device_is_compatible(child, "ethernet-phy-ieee802.3-c22"))
		return true;

	if (of_match_node(whitelist_phys, child)) {
		pr_warn(FW_WARN
			"%pOF: Whitelisted compatible string. Please remove\n",
			child);
		return true;
	}

	if (!of_find_property(child, "compatible", NULL))
		return true;

	return false;
}
```

其实就是通过匹配 `compatible` 中的字段来看看是否是 PHY 设备，这里我们的 `"ethernet-phy-ieee802.3-c22"` 匹配上了，返回 `true`。

接下来就会触发 `of_mdiobus_register_phy()` 来注册 PHY 设备了。内部又调用了 `fwnode_mdiobus_registere_phy()`：

```c
/* drivers/net/mdio/fwnode_mdio.c */
int fwnode_mdiobus_register_phy(struct mii_bus *bus,
				struct fwnode_handle *child, u32 addr)
{
    ...
    /* 进行 phy_device 的数据组装 */
    rc = phy_device_register(phy); /* 向内核注册 PHY 设备 */
}
```

细节咱们就先不就结了，这里就完成了 MDIO 总线上 PHY 设备的注册，注意这里的 PHY 设备地址是在设备树中配置好了的。

因为接下来有一段代码就是专门处理那些 `reg` 属性为空的 PHY 设备节点：

```c
	/* auto scan for PHYs with empty reg property */
	for_each_available_child_of_node(np, child) {
		/* Skip PHYs with reg property set */
		if (of_find_property(child, "reg", NULL))
			continue;

		for (addr = 0; addr < PHY_MAX_ADDR; addr++) {
			/* skip already registered PHYs */
			if (mdiobus_is_registered_device(mdio, addr))
				continue;

			/* be noisy to encourage people to set reg property */
			dev_info(&mdio->dev, "scan phy %pOFn at address %i\n",
				 child, addr);

			if (of_mdiobus_child_is_phy(child)) {
				/* -ENODEV is the return code that PHYLIB has
				 * standardized on to indicate that bus
				 * scanning should continue.
				 */
				rc = of_mdiobus_register_phy(mdio, child, addr);
				if (!rc)
					break;
				if (rc != -ENODEV)
					goto unregister;
			}
		}
	}
```

首先跳过了有 `reg` 属性的节点，然后再扫描 32 个地址看是否注册了设备，如果注册了就跳过。如果发现了是 PHY 设备但是却没有注册(就是没有配置 `reg`)，那么这里就会兜底进行注册。

到这里我们总算是跟完了 `of_mdiobus_register()` 这个 MDIO 总线注册的核心流程。现在再次退回上一级，也就是 `stmmac_mdio_register()` 中。

### 5.1.3. 另一些兜底

其实在 `of_mdiobus_register()` 中已经完成了对总线和 PHY 设备的注册，但是这后面还有一些代码，这些代码应该是做一些防御性编程和兜底。

**不过现在看来剩下的代码都是纯冗余**，如果使用标准的 设备树节点的话，会直接跳过。

首先就是关于 `fixe-link` 的子节点。内核设备树似乎规定了这类节点应该是放在 MAC 节点下，但是依然有可能在某些情况下错误地放在了 MDIO 字节点中，所以这里有：

```c
    struct fwnode_handle *fwnode = of_fwnode_handle(priv->plat->phylink_node);

	/* If fixed-link is set, skip PHY scanning */
	if (!fwnode)
		fwnode = dev_fwnode(priv->device);

	if (fwnode) {
		fixed_node = fwnode_get_named_child_node(fwnode, "fixed-link");
		if (fixed_node) {
			fwnode_handle_put(fixed_node);
			goto bus_register_done;
		}
	}
```

这里就是去检查 MDIO 下是否有这样的错误节点，如果的确是发现了这样的节点，那么就会直接 `goto` 到末尾，不去执行接下来的手动扫描 MDIO 总线。

然后这里还有一个判断：

```c
	if (priv->plat->phy_node || mdio_node)
		goto bus_register_done;
```

这里显然我们如果是使用的标准设备树节点，也会 `goto` 到 `bus_register_done`。不会触发下面那一段手动扫描总线：

```c
	found = 0;
	for (addr = 0; addr < max_addr; addr++) {
        ...
    }
```

最后直接将 `priv->mii = new_bus`。将 mdio 设备结构传递给 `priv`。这样就真正完成了 MDIO 总线的注册。

## 5.2. 调试问题

注意，在后面调试 marvell 88E1512 PHY 时遇到了一些问题，具体表现为内核日志输出：

```
[   19.041829] rk_gmac-dwmac fe010000.ethernet end1: Register MEM_TYPE_PAGE_POOL RxQ-0
[   19.042591] rk_gmac-dwmac fe010000.ethernet end1: in stmmac_init_phy(): fwnode's compatible of_node is: ethernet
[   19.042726] rk_gmac-dwmac fe010000.ethernet end1: in stmmac_init_phy(): phy_fwnode's compatible of_node is: phy
[   19.042746] rk_gmac-dwmac fe010000.ethernet end1: gmac driver run this way: phylink_fwnode_phy _connect()
[   19.042771] Generic Clause 45 PHY stmmac-1:01: mdio driver is Generic Clause 45 PHY
[   19.043931] rk_gmac-dwmac fe010000.ethernet end1: phy_attach_direct() return: 0
[   19.044002] rk_gmac-dwmac fe010000.ethernet end1: validation of  with support 00000000,00000000,00006000 and advertisement 00000000,00000000,00000000 failed: -EINVAL
[   19.044029] rk_gmac-dwmac fe010000.ethernet end1: phylink_bringup_phy() return: -22
[   19.044186] rk_gmac-dwmac fe010000.ethernet end1: __stmmac_open: Cannot attach to PHY (error: -22)
```

具体可以查看 [WAP2001调试](./16-WAP2001-2调试日志.md)。这里给一个参考资料

[phy驱动link up流程分析](https://www.cnblogs.com/dongxb/p/17365061.html)

## 5.3. PHY 设置

在完成了 MDIO 总线和总线上 PHY 设备的注册后，自然是要对它进行一些配置，这就是通过 `stmmac_phy_setup()` 来完成的。

这里有一个很关键的概念 `phylink`。在整个函数中就是在配置 `priv->phylink_config`，并且在最后根据配置创建了 `struct phylink` 结构体，存放到 `priv->phylink` 中。

### 5.3.1. phylink

这两个结构体是 Linux 内核中 `phylink` 框架的核心结构：

* `phylink_config` 静态地描述了 MAC 硬件能力(能支持多块的速度、接口模式)和他提供的操作接口.
* `phylink`则是一个运行时对象，负责处理链路协商、状态机切换，并在必要时使用操作接口。

`phylink` 框架把复杂的自动协商状态机、SFP 热插拔检测或者固定链路模拟机制从 `stmmac` 驱动中独立了出来。驱动中只负责实现一些操作函数和配置，剩下的直接交给 `phylink` 框架去执行。

# 6. 网络设备注册

在 `stmmac_dvr_probe()` 的最后，将配置完成的 `struct net_device ndev` 进行注册，内核中有专门的接口：

```c
/**
 *	register_netdev	- register a network device
 *	@dev: device to register
 *
 *	Take a completed network device structure and add it to the kernel
 *	interfaces. A %NETDEV_REGISTER message is sent to the netdev notifier
 *	chain. 0 is returned on success. A negative errno code is returned
 *	on a failure to set up the device, or if the name is a duplicate.
 *
 *	This is a wrapper around register_netdevice that takes the rtnl semaphore
 *	and expands the device name if you passed a format string to
 *	alloc_netdev.
 */
int register_netdev(struct net_device *dev)
{
	int err;

	if (rtnl_lock_killable())
		return -EINTR;
	err = register_netdevice(dev);
	rtnl_unlock();
	return err;
}
```

```c
/**
 * register_netdevice() - register a network device
 * @dev: device to register
 *
 * Take a prepared network device structure and make it externally accessible.
 * A %NETDEV_REGISTER message is sent to the netdev notifier chain.
 * Callers must hold the rtnl lock - you may want register_netdev()
 * instead of this.
 */
int register_netdevice(struct net_device *dev);
```

这个代码我有点不太看得懂，大概了解了一下过程。由于这是 `probe` 的最后一步了，前面已经把大部分繁琐的工作都已经完成了，

接下来就是进行进一步的微调，就是 `dev->netdev_ops->ndo_init`，这个是一个钩子，复杂的驱动可能需要在这里使用它来完成一些操作，不过 `stmmac` 通用驱动并没有定义这个函数指针。

然后就是分配一个 `ifindex` (网络接口索引，`ifconfig`看到的)，使用 `dev_new_index()`。这是内核协议栈识别该设备的数字 ID。

在之前我们通过查看设备树属性并读取硬件的特性寄存器来合并得到硬件能够支持的功能，这里就是再将硬件支持功能与驱动想要的功能进行统一，并最终继承给 VLAN 或者 隧道子设备(mpls)。

接着就是我们比较熟悉的向 `sysfs` 注册设备：`netdev_register_kobject()`。其中就由我们比较熟悉的 `deevice_add()`。不过需要注意的是，此时只是在 `sysfs` 中可见设备，**但是内核协议栈还没有将他加入通信列表**。

`list_netdevice()` 才是最关键的一步。将设备节点插入到全局链表 `dev_base_head` 和哈希表中。完成这一步， `ip link show` 才能够看到设备。

最后广播有新的网卡：`call_netdevice_notifiers()`。

我们先大致了解一下流程就好。这里面涉及到了太多复杂概念，并且相互交叉。


# 7. phylink 是什么？



# 8. 关键结构体组织方式

一个网口驱动从 GMAC 厂商适配层到通用驱动，再到 GMAC 硬件，MDIO 总线。其数据结构的关联关系有一些复杂。这里我希望能够稍微理一下。

首先这里列出一下一个完整网口驱动的一些关键结构体：

```c
struct platform_device *pdev; /* GMAC 网卡平台设备对应的 device */

struct plat_stmmacenet_data *plat_dat; /* GMAC 网卡对应驱动数据 */



```