虽然看了前辈的 `smi_bus` 驱动代码，自己也仿写了一个 `max6635` 抽象字符驱动的代码，但是对于其中的细节还是知其然不知其所以然。

因此想要单开一个文档，专门来讲讲字符驱动的事情。顺便带着看看字符驱动是怎么被使用的，以及所谓的 `udev` 是什么。

另外我一直是有一个困惑的，就是关于驱动的生命周期绑定啊，结构体之间的关联这些，因为内核做了很多工作，我自己在写驱动时就不清楚哪些是我该做的，哪些是我不用做的...