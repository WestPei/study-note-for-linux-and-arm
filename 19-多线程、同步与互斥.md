- [1. Linux 多线程](#1-linux-多线程)
  - [1.1. 多进程的作用](#11-多进程的作用)
- [2. 同步与互斥再理解](#2-同步与互斥再理解)
  - [2.1. 信号量](#21-信号量)
  - [2.2. 锁](#22-锁)

---

# 1. Linux 多线程

在 Linux 内核中，其实并没有严格区分线程与进程，在内核代码中都是使用 `task_struct` 结构体进行管理的，真正区分线程与进程的实际上在于**它们之间是否共享资源**。

一个 Linux 的应用程序，可以创建多进程，也可以创建多线程：

* `pthread` 库是多线程的 API；
* `fork()`、`exec()`、`wait()`则是多进程的 API。

一般一个进程(process)下可以包含多个线程(thread)，不同进程之前几乎相互隔离，而多个线程之间则会共享大部分资源。

```
进程
│
├── Thread 1 ── task_struct
├── Thread 2 ── task_struct
└── Thread 3 ── task_struct

共享：
    ├── 虚拟地址空间
    ├── 全局变量
    ├── heap
    ├── 打开的文件描述符
    └── 其他进程资源
```

```
Process A
│
└── task_struct


Process B
│
└── task_struct

资源相互独立：
    ├── 虚拟地址空间不同
    ├── 全局变量不同
    ├── heap 不同
    └── 文件描述符表默认独立（fork 后有继承关系）
```

* 线程 = 多个执行流之间共享一个进程的资源。
* 进程 = 多个相互隔离的资源空间。

因此执行单元之间的关系将直接决定我们在编码时的选择。

## 1.1. 多进程的作用

我目前只涉及到了多线程，我觉得它已经很好用了，那么多进程的优势是什么呢？

1. **故障隔离**：线程之间由于资源共享，所以一个线程崩溃很可能导致其他线程一起崩溃。进程间的隔离保证了多个进程的运行不会相互干扰。
2. **执行其他程序**：另一个多进程的优势就是，可以通过 `fork()` 来执行其他的程序。线程则一般都是执行本程序下的函数。
3. **权限隔离**：对于服务器程序而言这个非常重要，不同的进程的运行权限可以不同，控制进程可以用 root 权限，而工作进程可以使用普通用户权限。
4. **独立地址空间**：如果多线程中定义全局变量，那么该进程下的所有线程看到的都是同一个变量，因此线程先不需要复杂的通信。而多进程见地址空间是相互独立的，所以需要依赖 IPC 来实现数据交换。


---

# 2. 同步与互斥再理解

在 C 语言，特别是 Linux内核，同步与互斥还挺关键的，这里主要有几个细分的概念。同步与互斥本质上是两个意思。

* 同步：不同进程进行数据的交互需要有先后顺序，需要所谓的同步。
* 互斥：不同进程对同一数据的访问/操作时需要限制，不然会出现静态条件，导致数据错误。

虽然有一些些微的差别，但是本质上都是讲的多线程对同一个数据操作的时候，需要有一定的规则限制，需要按照顺序，并且同一时间只允许一个线程进行写操作。

**对于数据需要按照顺序来操作，衍生出了 “信号量” 也就是 `semaphore` 。**该机制的作用是保证一个数据的 “生产者” 和 “消费者” 能够按照顺序来访问数据，不然 “消费者” 在数据还未产生时就去读取数据了，肯定会导致错误。

**而为了保护数据的原子访问**，则产生了 “锁” 的概念，也就是 `mutex` 以及 `spin-lock` ，还有更复杂的读写锁。

## 2.1. 信号量

信号量的典型用途就是让一个 “生产者” 和 “消费者” 不同的数据类型被访问时能够按照顺序。这个很好理解，就是一个进程想要去使用另一个进程产生的数据，首先得保证这个数据已经产生了。那么我们就可以将信号量初始化为 `0`， 然后 “生产者” 生成数据后进行 `sem_post()` 让信号量 +1；而 “消费者” 进程则是调用 `sem_wait()` 尝试获取该数据，当 “生产者” 完成数据封装后， “消费者” 被唤醒并访问数据，此时 `sem_wait()` 成功并让信号量 -1，回到 `0`。

当然信号量还有其他用途，比如限制数据的访问线程数量，这个暂时不知道具体的用法。

但是我们可以注意到，信号量不在乎是谁让自己加或减，只是一个门卫，进一个就 -1，出去一个就 +1。并没有和锁类似的 “持有者” 的概念。

## 2.2. 锁

既然信号量对应了同步，那么锁就是用于互斥。锁的概念很好理解，就是对数据上了锁，谁要访问数据，谁就需要拿钥匙。这是很通俗的说法。

实际上是数据会对应一把锁，线程想要访问该数据时会尝试获取该锁，如果没有人占用锁，那么就成功获取到锁。如果此刻有人已经持有了锁，那么不同类型的锁会使线程进入不同的行为：

* 互斥锁，读写锁是**睡眠锁**，即线程会被阻塞挂起，等待锁的释放。某些线程处于不能睡眠的情况，如中断上下文，是不可以使用这类锁的。
* 自旋锁则不会阻塞进程，他实际上会进入一个 `while` 循环，等待锁释放，这种一般适用于等待时间很短，或者是不能睡眠的情况。

锁有一个很重要的点，就是**谁拿锁，谁释放**，确保一次进入临界区的线程为锁规定的。
