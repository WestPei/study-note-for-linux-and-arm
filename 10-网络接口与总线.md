- [1. PHY](#1-phy)
- [2. MII 与 MDIO](#2-mii-与-mdio)
  - [2.1. MII](#21-mii)
  - [2.2. MDIO/SMI](#22-mdiosmi)
  - [2.3. 两者关系](#23-两者关系)
- [3. 交换机/交换芯片](#3-交换机交换芯片)
  - [3.1. 常见单对单连接](#31-常见单对单连接)
  - [3.2. 交换机/交换芯片概念解析](#32-交换机交换芯片概念解析)
  - [3.3. 两种主流的结构](#33-两种主流的结构)

---

参考资料：

1. [网卡构造：MAC与PHY的关系，GMAC介绍](https://blog.csdn.net/chenhuanqiangnihao/article/details/128287341)

# 1. PHY

PHY 芯片负责的是将以太网帧转换成物理信号发出去，而 MII 所谓的介质无关其实就对应的 PHY的类型和接口。

|  | 电口 | 光口 |
| --- | --- | --- |
| **物理连接** | RJ45 接口，双绞线连接(网线) | SFP/SFP + 插槽，插光模块，连光纤 |
| **传输信号** | 电信号(差分电压) | 光信号 |
| **PHY 信号** | 电口 PHY (输出直接接网络变压器到 RJ45) | 光口 PHY，或 电口 PHY + 光模块组合 |
| **典型距离** | 100米以内 | 几百米到几十公里 |

因此，介质的不同可以说是 PHY 芯片不同(电口 PHY 直接推电信号，而光口 PHY 要驱动光模块)，也可以说是 PHY 后面的数据线/介质不同。

PHY芯片如何将以太网帧转换成物理信号这一块一般情况下不需要了解，由 PHY 自助完成，我们一般关注通过 GMAC 芯片的 MDIO 控制器通过 MDIO 总线来配置 PHY的寄存器。

---

# 2. MII 与 MDIO

## 2.1. MII

MAC 与 PHY 芯片之间的数据接口统称为 MII (Media Independent Interface，介质无关接口)。它的核心作用就是让 MAC 芯片不用关心下层是电口、光口还是其他物理介质，使用统一的数据接口。

而随着接口发展，又出现了几种变体

| 接口 | 全称 | 用途/特点 |
| ---- | ---- | -------- |
| **MII** | Media Independent Interface | 经典 10/100M 并行接口，引脚多(约16跟数据+控制)，现在很少用了 |
| **RMII** | Reduced MII | 引脚减半的 10/100M 并口，嵌入式中较为常见(50MHz时钟) |
| **GMII** | Gigabit MII | 千兆并口，8根数据线，125MHz，引脚更多 |
| **RGMII** | Reduced Gigabit MII | **简化千兆接口**，在时钟双沿各传一次数据，4根数据线实现千兆，非常常用 |
|  **SGMII** | Serial Gigabit MII | **串行千兆接口**，彻底变成串行差分线(1.25Gbps，8/10b 编码)，只需一对收，一对发，常接光纤PHY或者交换机芯片 |

**SGMII 与 RGMII 都是 MII 这个大家族里的具体实现方式**：

* RGMII 并行 + 双沿采样，节省了引脚。
* SGMII 串行 + 差分，用 SerDes 传输，实现远距离/少接线。

> SerDes 是 Serializer(串行器) 和 Deserializer(解串器) 的合称。它是一种主流的**高速点对点通信技术**。
> 在发送端将低速的并行数据转换成告诉串行数据(串行器)，通过信道传输后，在接收端再将高速串行数据恢复为低速并行数据(解串器)。

## 2.2. MDIO/SMI

MII 是数据接口，而 MDIO 就是管理总线。

MII 接口上跑的是以太网的数据帧(bit流)，而 MDIO 则不是。MDIO (Management Data Input/Output)是一套完全独立于 MII 接口的两线串行总线。

* MDC：时钟
* MDIO：双向数据(类似 I2C，但是协议不同)

MDIO 的作用是 **配置、管理和读取 PHY 芯片的状态寄存器**，比如：

* 设置速率/双工模式
* 开启/关闭自协商
* 读取 Link 状态

有些时候我们会看到 SMI 总线的提法。其实在绝大多数语境下，SMI 总线和 MDIO 总线可以认为是同一种东西。它是 IEEE 802.3以太网标准中定义的管理接口，专用于配置和控制 PHY 或交换芯片。SMI 和 MDIO 这两个名字之所以混用，其实是同一种接口在不同层面上的描述：

| 名称 | 全称 | 描述侧重 |
| ---- | ---- | ------- |
| **SMI** | **串行管理接口** (Serial Management Interface) | 侧重于接口的类型和功能（串行管理），是“干什么的” |
| **MDIO** | **管理数据输入/输出** (Management Data Input/Output) | 侧重于具体的信号线，常与MDC时钟线并提，是“怎么实现的” |

不过需要留意的是，虽然 MDIO/SMI 是标准，但是有一些厂商会为其产品实现一些特殊的驱动，但是其底层物理总线依旧是 MDIO。



## 2.3. 两者关系

首先需要区分 MII 与 MDIO，这是两个完全不同的硬件接口/总线。但是在 MAC 芯片与 PHY 芯片之间两者是同时存在并且各司其职的。**MII** 是数据通道，而 MDIO 则是**管理通道**。

---

# 3. 交换机/交换芯片

## 3.1. 常见单对单连接

我们平时的电脑或者开发板一般都是自带一对或两对 "GMAC + PHY"的组合。它们从硬件上就是两组网口，我们可以同时使用也可以单独使用。但是对每一个网口来说，它们的信息流都是：

```
              MII/RGMII (数据)
            /                 \
SoC -> GMAC                     PHY -> 网线 -> 对端 PHY -> 对端 GMAC
            \                 /
                MDIO (管理)
```

也就是说，终端设备都是通过一对 "GMAC + PHY" 组合通过网线与其他设备实现通信的。这样的单对单连接的信息流非常明确：

* GMAC 通常是 SoC 内部的一个平台设备(Platform Device)，通过 DMA/中断与 CPU 进行交互。
* 数据流：GMAC 把以太网帧通过 MII 类接口交给 PHY，PHY 发送到物理介质中。
* 管理流：GMAC 里集成了一个 MDIO 控制器，Linux会生成一条 MDIO 总线，上面挂着一个 PHY 设备，用相应的 PHY 驱动去管理。

## 3.2. 交换机/交换芯片概念解析

很多困惑都是概念的不清晰导致的，我们先搞清楚交换机与交换芯片之间的关系：

* 交换芯片：一个集成电路，提供数据包的二层转发功能。它是一个**组件**。
* 交换机：一个完整的产品。拆开一台交换机，里面通常会包含：**交换芯片 + 主控CPU + 内存 + PHY芯片 + 电源 + 各种接口**。

一个简单的嵌入式板子上可能只有一颗交换芯片，而一个企业级的交换机，则是一个复杂的系统。

## 3.3. 两种主流的结构

我的困惑很多时候来自于将两种不同的结构混在一起。首先说说我的困惑吧，仔细想想都是因为那个楠菲微的NF5180，作为我接触的第一款交换芯片，它就不太常规。

NF5180在纯交换芯片上集成了双核 A53 CPU，上面可以运行一个完整的 Linux 内核系统，官方还提供了 SDK 和系统镜像。我原以为它的使用方法与纯交换芯片一致，会通过总线连接到一个主控芯片上，主控芯片来控制它。**当然这种方法是可行的**，D老师告诉我可以通过 PCIe总线的 DMA 来互通数据包，通过 PCIe或者网络(SSH)的方式来控制。但是这种方式比较高阶，不是现阶段的我能玩的。

而在我与前辈交流的过程中，他告诉我一般的交换芯片是通过 "MAC 对 MAC"，通过 DSA 架构，交换芯片上的对外端口被抽象成 `lanx`，控制方法与单对单类似，数据流通过 MII 类接口，控制总线则是利用 MDIO 从设备接口或 SPI 从设备接口。这就造成了困扰，我试图将这个组织方式直接套用到 NF5180 上，于是就懵了。如果是用 "MAC 对 MAC"的方式组织，那么交换芯片上跑的内核有什么用？

然后就进行了一系列查阅资料和学习，才明白这基本上就是两种交换机的结构。一种是将主控芯片与交换芯片直接集成在一起，形成一个可以运行 Linux 内核的高性能交换芯片；另一种就是主控芯片+交换芯片，通过 DSA 架构来组织。

两种组织形式的核心其实还是主控芯片CPU 与交换芯片的连接，以及交换机的软件架构。在我看来，核心就是两点：

* 交换芯片上的多个物理端口是如何被主控芯片配置和管理的？
* 主控芯片CPU 与交换芯片间是如何连接的？数据如何流通？

说人话就是，CPU  与交换芯片走的什么总线？与单对单一样吗？有没有什么特殊的框架？毕竟从一对一变成了一对多。有的兄弟，有的。那么我们先来了解一下所谓的 DSA 架构吧。

DSA(Distributed Switch Architecture) 分布式交换机架构，特指 Linux 内核网络领域的一种独特的软件框架。

* **解决问题**：传统架构中，CPU 通过一个端口连接到一个多端口的交换芯片时，Linux 系统无法直接看到和管理交换芯片上的各个独立的物理端口，导致配置和管理非常不便。
* **核心思想**：DSA 框架的核心在于 "分发(Distributed)":
  1. **接口分发**：它不改变硬件，而是在**软件层面**为交换芯片上的每一个物理端口，在 Linux 内核中创建处一个独立的虚拟网络接口(如 `lan0`，`lan1`)。
  2. **能力兼顾**：这些虚拟接口看起来和普通的网卡无异，我们依然可以用标准的 `ifconfig`、`bridge`等命令来管理它们。同时，端口间数据包的**硬件转发**能力也被完整保留，无需经过 CPU 处理，**兼具软件标准型与硬件极致性能**。
  3. **集中管理**：该设计同样支持级联多个交换芯片，并将它们作为一个"交换机树(switch tree)"来统一管理，体现了"分布式"的命名。

这里有提到级联多个交换芯片，是如何做到的呢？

首先我们需要了解到 CPU 与 交换芯片之间、交换芯片之间如何连接。之前说到，正常的设备单对单连接都是"MAC-PHY-网线-PHY-MAC"。而 CPU 和交换机以及交换机之间是可以直接通过"MAC-MAC"进行连接，**这就是交换芯片级联的关键**。此时 MAC 端口只提供 MAC 层的数据转发功能，没有 PHY (物理层)。而要注意的就是，交换芯片除了有"MAC-PHY"这样设计好的用于连接中断设备的网口以外，都会有单独的 MAC 端口用于拓展(就是所谓的 **CPU Port**)，而一般SoC 都是自带 GMAC 芯片的，底板设计时就会考虑是直接连接 PHY 还是连接交换芯片进行拓展。

交换芯片与 SoC 使用 CPU Port 直连时，常见的数据接口为：

* RGMII：千兆并行接口，成本、功耗和性能的平衡。
* SGMII：串行千兆接口，适合空间紧凑型设计。
* RMII：百兆级别的简化接口，适合低端应用场景。

而控制接口也是一样的，因为 MAC 芯片中一般都会集成一个 MDIO 控制器，所以使用的还是 MDIO 总线。

OK，到这里是不是就对所谓的交换芯片与 SoC 的连接方式以及数据流转的方式有了一个初步的了解了。