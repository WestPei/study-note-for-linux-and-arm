- [1. 系统镜像构成概览](#1-系统镜像构成概览)
- [2. 内核编译与配置](#2-内核编译与配置)
  - [2.1. 手动配置内核](#21-手动配置内核)
    - [2.1.1. 标准流程](#211-标准流程)
    - [2.1.2. 关键概念区分](#212-关键概念区分)
  - [2.2. Yocto 的 cfg 配置碎片机制](#22-yocto-的-cfg-配置碎片机制)
    - [2.2.1. 将 cfg 固化到源码](#221-将-cfg-固化到源码)
    - [2.2.2. 补丁的处理](#222-补丁的处理)
- [3. `init` 进程](#3-init-进程)
  - [3.1. `Busybox`](#31-busybox)
  - [3.2. yocto 选择](#32-yocto-选择)
- [4. Rootfs 构建](#4-rootfs-构建)
  - [4.1. 了解根文件系统](#41-了解根文件系统)
  - [4.2. 编写 rootfs 镜像配方](#42-编写-rootfs-镜像配方)
    - [4.2.1. 如何找到这些关键代码](#421-如何找到这些关键代码)
    - [4.2.2. 核心制作命令](#422-核心制作命令)
  - [4.3. 权限问题](#43-权限问题)
  - [4.4. 核心工具集的选择](#44-核心工具集的选择)
- [5. Initramfs](#5-initramfs)
  - [5.1. 概念与由来](#51-概念与由来)
  - [5.2. 制作 initramfs 镜像](#52-制作-initramfs-镜像)
  - [5.3. 启动方式与配置](#53-启动方式与配置)
    - [5.3.1. 通过 extlinux 启动](#531-通过-extlinux-启动)
    - [5.3.2. 通过 booti 命令启动](#532-通过-booti-命令启动)
    - [5.3.3. extlinux 启动 vs 裸启动](#533-extlinux-启动-vs-裸启动)
  - [5.4. 常见问题](#54-常见问题)
    - [5.4.1. 登录卡住](#541-登录卡住)
    - [5.4.2. init 符号链接缺失](#542-init-符号链接缺失)
    - [5.4.3. rockchip-image 类冲突](#543-rockchip-image-类冲突)
- [6. Yocto 工程启动链](#6-yocto-工程启动链)
  - [6.1. 原教旨主义 Boot（手动启动）](#61-原教旨主义-boot手动启动)
  - [6.2. 厂商官方 Boot（Rockchip boot.img）](#62-厂商官方-bootrockchip-bootimg)
  - [6.3. extlinux Boot](#63-extlinux-boot)
  - [6.4. wic 镜像启动](#64-wic-镜像启动)
    - [6.4.1. 启动方式对比](#641-启动方式对比)
- [7. wks 文件语法解析](#7-wks-文件语法解析)
  - [7.1. 全局指令](#71-全局指令)
  - [7.2. part 命令核心选项](#72-part-命令核心选项)
  - [7.3. 关键分区解析](#73-关键分区解析)
    - [7.3.1. idblock 分区（不含分区表）](#731-idblock-分区不含分区表)
    - [7.3.2. U-Boot 分区（偏移 4M）](#732-u-boot-分区偏移-4m)
    - [7.3.3. U-Boot 环境变量分区](#733-u-boot-环境变量分区)
    - [7.3.4. Boot 分区（启动分区）](#734-boot-分区启动分区)
    - [7.3.5. Rootfs 分区](#735-rootfs-分区)
  - [7.4. 创建新分区](#74-创建新分区)
  - [7.5. 更进一步？](#75-更进一步)
- [8. parameter 分区表与分区管理](#8-parameter-分区表与分区管理)
  - [8.1. 格式解析](#81-格式解析)
  - [8.2. 分区大小换算](#82-分区大小换算)
  - [8.3. 新增分区（以日志分区为例）](#83-新增分区以日志分区为例)
    - [8.3.1. 步骤 1：修改 parameter](#831-步骤-1修改-parameter)
    - [8.3.2. 步骤 2：进入系统后创建文件系统并挂载](#832-步骤-2进入系统后创建文件系统并挂载)
    - [8.3.3. 步骤 3：配置开机自动挂载](#833-步骤-3配置开机自动挂载)
- [9. 日志分区的创建与挂载](#9-日志分区的创建与挂载)
  - [9.1. rsyslog.service](#91-rsyslogservice)
    - [9.1.1. rsyslog 历史渊源](#911-rsyslog-历史渊源)
    - [9.1.2. rsyslog 内核文件](#912-rsyslog-内核文件)
  - [9.2. systemd-journald](#92-systemd-journald)
    - [9.2.1. journald 内核文件](#921-journald-内核文件)
  - [9.3. 两个日志服务的关系](#93-两个日志服务的关系)
  - [9.4. 新建日志分区并挂载](#94-新建日志分区并挂载)
    - [9.4.1. 前辈的方法](#941-前辈的方法)
    - [9.4.2. 更简单的做法](#942-更简单的做法)
- [10. uboot-env 分区配置](#10-uboot-env-分区配置)
- [11. Rockchip 烧写指南](#11-rockchip-烧写指南)
  - [11.1. 烧写工具](#111-烧写工具)
  - [11.2. Boot 分区烧写](#112-boot-分区烧写)
    - [11.2.1. 带文件系统的 boot 分区](#1121-带文件系统的-boot-分区)
    - [11.2.2. 裸镜像启动（带 Android 头）](#1122-裸镜像启动带-android-头)
    - [11.2.3. bootable 标记](#1123-bootable-标记)
  - [11.3. 波特率问题](#113-波特率问题)
  - [11.4. 关于烧写地址与 miniLoader](#114-关于烧写地址与-miniloader)
- [12. 附录：Yocto 编译命令速查](#12-附录yocto-编译命令速查)
  - [12.1. 常用 bitbake 命令](#121-常用-bitbake-命令)
  - [12.2. 关键 flag 说明](#122-关键-flag-说明)

---

# 1. 系统镜像构成概览

在我的初步认知中，Yocto 工程就是可以简化我们的嵌入式 Linux 系统镜像的构建，能够通过配方+配置的方式自动化下载源码并按需嵌入系统中实现系统镜像的构建。这个说法很宽泛，一点也不具体。比如最后构建出来的系统镜像是什么样的？是一个文件吗？

显然不是那么简单。一个可烧录到板子上运行的"系统级镜像"并非单一文件，而是多个关键部件的集合：

1. **Bootloader 引导程序**：类似 PC 的 BIOS/UEFI，上电后最先执行，用于初始化硬件并加载内核。嵌入式系统最常用的是 U-boot。
2. **Linux 内核**：操作系统的核心，编译内核源码后生成的特定格式文件。
3. **Root Filesystem 根文件系统**：包含操作系统运行所需的所有库、系统命令、配置文件和应用程序。
4. **设备树 (Device Tree, .dtb)**：描述硬件拓扑结构的二进制文件，内核通过它了解板载外设。

Yocto 工程生成的最终系统镜像，就是将编译好的这些组件按照目标板需求打包在一起。

**为什么需要单独构建组件而非只构建最终镜像？** 在系统开发调试阶段（尤其是调试 U-boot 和内核驱动），如果每次修改都走完整 Yocto 构建，耗时过长。单独针对修改部分进行编译可以快速验证修改，提升效率。Yocto 保留了中间文件和展开的源码，方便开发者阅读、调试和打补丁。

---

# 2. 内核编译与配置

## 2.1. 手动配置内核

内核源码的 `arch/<架构>/configs/` 目录下存放着适配各架构的 `defconfig` 配置文件，用于告诉编译器内核需要支持哪些功能。

### 2.1.1. 标准流程

```bash
# 1. 基于 defconfig 生成 .config
make ARCH=arm64 <芯片名>_defconfig

# 2. 图形化修改配置 (基于当前 .config)
make menuconfig

# 3. 保存修改为精简的 defconfig
make savedefconfig

# 4. 编译
make -j$(nproc)
```

### 2.1.2. 关键概念区分

| 文件 | 说明 |
|:---|:---|
| `.config` | 根目录下的**最终配置文件**，整合了内核大量默认配置和特定架构的 defconfig，编译时直接使用 |
| `defconfig` | **模板文件**，仅包含与默认值不同的配置项。通过 `make savedefconfig` 从 `.config` 生成 |
| `arch/<架构>/configs/*_defconfig` | 架构特定的配置模板，供 `make *_defconfig` 使用 |

`make savedefconfig` 生成的 `defconfig` 比原始 `defconfig` 少很多配置项，这是因为与默认值重复的项被省略了。两者分别生成 `.config` 后对比，**结果是一样的**。

得到精简的 `defconfig` 后，将其重命名放入对应架构目录，并修改 Makefile 使编译时使用新配置。

## 2.2. Yocto 的 cfg 配置碎片机制

在我们初步改造前辈工程的时候遇到了这样的一个问题：前辈的内核完全由 Yocto 机制进行构建，比如使用 `git+patch` 的方式来为内核代码添加补丁，还有使用 `cfg/fragment` 的方式来修改内核编译时的基础配置。这些方法都是 Yocto 支持的独特机制，在编译内核时通过 `do_patch` 和 `do_configure` 任务来将这些改动打入内核源码中。

Yocto 支持将不同功能的配置分别写为 `cfg` 文件，放在内核配方目录下。在 `do_configure` 任务中，bitbake 会自动将这些配置碎片与架构默认的 `defconfig` 整合，在构建目录 `${B}` 下生成最终的 `.config`。

这种机制便于模块化管理内核配置——一个功能一个 `cfg` 文件。但是这种使用方法和软件室自己的使用习惯不太匹配，最好是将这些改动直接固化在源码中，以后的改动也都是直接改动源码。然后将源码上传至 SVN 来进行管理。

那么我们就需要知道这些改动究竟做了些什么。他们被打入了源码的具体哪一个位置？我们从 Yocto 工程中获得的在 `${S}` 目录下的源码是否包含了这些改动？如果包含了那就可以在配方中移除这些改动避免重复。如果没有，我们该怎么手动将这些改动添加进源码？

### 2.2.1. 将 cfg 固化到源码

如果团队习惯直接在源码中管理内核配置（而非通过 Yocto 的 cfg 碎片机制），需要将配置直接固化到源码：

1. 在 `bitbake` 完成 `do_configure` 后，进入 `${B}` 目录（`do_configure` 已经将所有 `.cfg` 碎片与 `defconfig` 合并生成了完整的 `.config`）
2. 执行 `make savedefconfig` 将当前 `.config` 中的改动固化为精简模板
3. 重命名并放入源码 `arch/arm64/configs/` 目录
4. 在内核配方中修改 `KBUILD_DEFCONFIG` 指向新的 defconfig：

```bash
KBUILD_DEFCONFIG:${MACHINE} ?= "*-defconfig"
```

5. 在 `.bbappend` 中移除原 `SRC_URI` 中的 cfg 文件引用
6. 如补丁也已固化，添加 `do_patch[noexec] = "1"`

**注意**：步骤 1 必须在 bitbake 完成 `do_configure` 之后执行，而非在源码目录中手动 `make defconfig`。因为 `do_configure` 会将所有 cfg 碎片整合进 `.config`，此时的 `.config` 才包含了完整的依赖项。如果直接从源码目录基于初始 `defconfig` 生成 `.config` 再做 `savedefconfig`，可能会缺少某些 cfg 碎片中才有的配置项，导致编译时依赖组件缺失。

另外注意，`do_configure` 任务会把 cfg 配置碎片文件合并在一起输出到构建目录下（`${B}`），生成一个 `.config` 文件，在编译时 bitbake 才会使用它，也就是说它并不会被放进源码中，与 `patch` 不同。

之前我有疑惑为什么 `make savedefconfig` 保存的 `defconfig` 比起最初生成 `.config` 时使用的比如 `rk3568_*_defconfig` 要少很多配置。我分别用这两个 `defconfig` 来生成了 `.config` 并比较，**发现他俩是一样的**。究其原因是因为内核配置中有很多默认项配置，这些配置与 `rk3568_*_defconfig` 中的配置项重合了，所以被省略了。我们最后只需要使用精简后的 `defconfig` 就好。

> Yocto 的 cfg 碎片机制本身是一种很好的配置管理方式——一个功能一个 `cfg` 文件，模块化管理。固化到源码主要是在团队有统一源码管理习惯时的折中做法。

### 2.2.2. 补丁的处理

有关补丁包 `patch` 其实比较简单，因为它作为源代码直接打入了内核源码内部，我们获得的源码中会直接包含这些补丁。因此上传 SVN 后可以直接跳过 `do_patch` 任务：

```bash
do_patch[noexec] = "1"
```

跳过的原因有两个：
1. 如果是在原 `linux-*.bb` 基础上修改的 `.bbappend` 文件，可以修改 `SRC_URI`，但无法很好地移除原配方中 `SRC_URI:append` 的内容（`:append` 的处理逻辑与 `+=` 不同，会在所有赋值语句处理完成之后再处理）
2. Yocto 原生支持的是 git，补丁也与 git 息息相关。上传 SVN 和从 SVN 拉取时会丢失 `.git` 隐藏目录，使用 SVN 拉取的代码如果不跳过 `do_patch` 就会出现报错

以后遇到在 `do_patch` 步骤卡住也可以借鉴这里。

关于 u-boot 和 kernel 的编译还有一些不太一样的地方。u-boot 在更改了 `SRC_URI` 后就可以直接用 SVN 源码过编译。但是 kernel 要更复杂一些。对于补丁，我们初次编译应该就将补丁加入源码了，因此在 `.bbappend` 中需要跳过 `do_patch` 任务来保证编译通过。

而对于 `configure`，我们可能会需要根据报错的提示，将原来配方文件中提及的配置 `fragment` 文件从原配方所在目录整体拷贝到 `.bbappend` 配方目录下，并且在 `.bbappend` 中添加：

```bash
FILESEXTRAPATHS:prepend := "${THISDIR}/${PN}:"
```

至于为什么这么做，其实都是因为原配方中都是利用 `SRC_URI +=` 的方式添加的，会被 `.bbappend` 给覆盖掉。因此还需要找到对应的原配方中的代码，拷贝到我们的 `.bbappend` 中。这样才能够保证 bitbake 能够顺利执行 `do_configure` 任务。

---

# 3. `init` 进程

`init` 进程是 Linux 内核启动后的“第一个进程”(PID=1)，是所有其他用户空间进程的“祖先”。而 `systemd` 是一种 `init` 程序的实现。

简单理解，`init` 进程是一个“职位”(PID 为 1 的进程)，`systemd` 是这个职位的“任职者”之一，从 Linux 内核的角度看，它运行的就是 PID 1 的 `init` 进程。`systemd` 并非唯一实现，历史上还存在过 `SysVinit`(最古老的一种)和 Upstart(基于事件驱动)等。

## 3.1. `Busybox`

`Busybox` 的核心思想就是“单个二进制文件，提供数百个常用命令”，它几乎包含了 Linux 系统所需的所有基础工具。

而 `Busybox` 与 `init` 进程的联系在于，它的内部也实现了一个功能非常精简的 `init` 程序。一般在嵌入式 Linux 开发中，在制作根文件系统时， `BusyBox` 不仅提供了 `ls`、`cp`等常用工具，它的 `init` 程序也是系统启动的首选之一。

这里提到了 `Busybox` 的主要功能还是集中在嵌入式，属于是“一键式大礼包”，不仅提供了根文件系统所需要的 `init` 程序，还有基本命令行工具。这是嵌入式设备一般资源紧张，而对于桌面版的 Linux，性能更强，资源更丰富，因此使用的也是各个独立的功能全面的 GNU 工具，它们作为独立的二进制可执行文件放在 `/usr/bin` 目录下。

## 3.2. yocto 选择

我们在 yocto 中可以在我们的 `distro` 文件中修改 `INIT_MANAGER = 'systemd'` 来配置 init 进程。

不过我注意到还有一个叫做 `POKY_INIT_MANAGER` 的变量同样也可以配置系统的 init 进程。

这两个变量的区别，可以总结为 **`INIT_MANAGER` 是现代、通用且文档化的推荐方式，而 `POKY_INIT_MANAGER` 是一个历史遗留变量，仅用于特定场景**：

| 特性 | **`INIT_MANAGER`** | **`POKY_INIT_MANAGER`** |
| :--- | :--- | :--- |
| **核心作用** | 由 **OpenEmbedded-Core (OE-Core)** 引入的**通用变量**，用于在**所有发行版（不限于Poky）** 中选择初始化管理器。 | **历史遗留的内部变量**，由 **Poky 发行版** 引入并内部使用，现已不推荐直接设置。 |
| **作用范围** | **通用**，适用于任何使用 OE-Core 构建的发行版。 | **特定**，仅在使用 `poky` 发行版时相关。 |
| **文档状态**| **有正式文档**，是官方推荐的配置方法。 | 文档中**未直接提及**，它主要用于内部逻辑。 |
| **优先级/使用场景** | **高**。直接在 `local.conf` 或 `distro` 配置文件中设置 `INIT_MANAGER`，是用户配置 init 系统的**首选方式**。 | **低**。它通常在 Poky 发行版的 `poky.conf` 文件中被设置。**用户不应直接修改它**。它的主要目的是为了方便 `local.conf` 覆盖 `INIT_MANAGER` 的值。 |

---

# 4. Rootfs 构建

## 4.1. 了解根文件系统

内核启动后第一个要运行的程序是根文件系统中的 `init` 进程。此外，根文件系统还提供常用的命令行工具（`ls`、`cd` 等）——内核本身只负责提供系统调用。

## 4.2. 编写 rootfs 镜像配方

制作 `rootfs.ext4` 镜像的核心步骤，拆解自 Yocto 的 `do_image_ext4` 任务。

### 4.2.1. 如何找到这些关键代码

如果要从 Yocto 的复杂任务链中提取出制作镜像的核心逻辑，可以按以下步骤追踪：

1. **查看任务列表**：`bitbake <image-recipe> -c listtasks`
2. **生成任务依赖图**：`bitbake <image-recipe> -g`（`task1 -> task2` 表示 task2 完成后才能执行 task1；依赖链为 `do_rootfs → do_image → do_image_ext4`）
3. **查看任务执行日志**：进入 `${WORKDIR}/temp/` 目录，查看 `run.do_image_ext4` 文件（记录了实际执行的 shell 命令）

在 `run.do_image_ext4` 中可以看到核心调用：

```bash
do_image_ext4() {
    oe_mkext234fs ext4 -i 4096
    ...
}
```

`oe_mkext234fs()` 是 Yocto 封装的函数（定义在 `poky/meta/classes-recipe/image_types.bbclass` 中），其中 `-i 4096` 表示每 4096 字节创建一个 inode。

### 4.2.2. 核心制作命令

```bash
# 1. 创建一个空的 ext4 稀疏镜像
dd if=/dev/zero of=${IMGDEPLOYDIR}/${IMAGE_NAME}.ext4 \
   seek=$ROOTFS_SIZE count=$COUNT bs=1024

# 2. 将 rootfs 目录写入镜像
mkfs.ext4 -F $extra_imagecmd ${IMGDEPLOYDIR}/${IMAGE_NAME}.ext4 \
   -d ${IMAGE_ROOTFS}

# 3. 检查镜像
fsck.ext4 -pvfD ${IMGDEPLOYDIR}/${IMAGE_NAME}.ext4 || [ $? -le 3 ]
```

将这些关键代码放入自定义的 `rootfs_*.bb` 配方中，配合手动设置的环境变量即可生成完整的 `*.ext4` 根文件系统镜像。

rootfs 配方一般放在 `meta-sdk/recipes-core/rootfs/` 下。

## 4.3. 权限问题

BitBake 在用户态构建，默认生成的 rootfs 中文件所有者是 `user:user` 而非 `root:root`。标准 Yocto 构建的 `do_image_ext4` 通过 `pseudo` / `fakeroot` 机制——在执行过程中始终模拟是 `root` 用户在操作，因此打包出的镜像中 `uid:gid` 自动为 `root:root`。

自定义配方不存在 `pseudo` 环境，即使手动添加 `fakeroot` 或 `pseudo` 到配方中也不生效（因为这是 bitbake 的任务级特性，不是简单的依赖）。

在配方内直接使用 `chown` 或 `sudo` 都无法解决问题：
- 修改源码目录权限：`do_unpack` 阶段因无 root 权限而直接失败
- 在打包前修改 `${S}` 目录权限：bitbake 自身权限不足
- `mount + chown` 修改制作好的镜像：需要 `sudo`，bitbake 不允许
- 根文件系统中存有指向 `/` 目录的软链接，这些链接在真正挂载前是无效的，`chown -Rh` 遇到无效链接会报 `invalid argument`

**折中方案**：在配方任务中输出一个辅助脚本（包含 `${IMGDEPLOYDIR}`、`${IMAGE_NAME}` 等路径变量），构建完成后在终端中用 `sudo` 执行该脚本来修改镜像内部权限。核心思路是让脚本 `sudo mount -o loop` 挂载镜像 → `chown` → `umount`，将需要 root 的操作从 bitbake 中剥离出来。

另外，使用 `cpio --owner root:root` 制作 initramfs 可以从源头避免权限问题（见第 4 节）。

## 4.4. 核心工具集的选择

我们的 yocto 镜像配方例如 `cetca-avod-minimal.bb` 中一般只会声明根文件系统中还需要添加哪些额外的工具软件，但是最基本的工具软件选择不会这在这里看到。

比如我们到底是使用 `busybox` 还是 GNU 的完整工具包？我们应该在哪里去配置呢？

这个配置一般是放在 `.conf` 中的，至于具体是在 `distro`，`machine` 还是本地的 `local.conf` 中则完全取决于我们的设置是与哪一块绑定的，yocto 的配置解析是有顺序的，后解析的会覆盖先解析的文件：

```
layer.conf -> bitbake.conf -> machine.conf -> distro.conf -> local.conf
```

yocto 在设计上认为 发行版(Distro) 是“最终权威”，它有最高的优先权去覆盖包括硬件板卡在内的所有配置。但是实际解析时本地配置 `local.conf` 是最后解析的，但是这个更多是本地临时配置。

在我们自己的配方和配置中没有找到相关的配置，但我们可以通过：

```bash
public@public-virtual-machine:~/avod-project/yocto/build-avod$ bitbake cetca-avod-minimal -e | grep ^PREFERRED_PROVIDER_virtual/base-utils
PREFERRED_PROVIDER_virtual/base-utils="busybox"

public@public-virtual-machine:~/avod-project/yocto/build-avod$ bitbake cetca-avod-minimal -e | grep ^VIRTUAL-RUNTIME_base-tuils
VIRTUAL-RUNTIME_base-utils="busybox"
```

来查看当前的一个默认配置是 `busybox`，这也与实际板子上使用的工具匹配。

我们想要修改为 GNU 工具集的话，根据情况，在对应的 `.conf` 中添加：

```bash
PREFERRED_PROVIDER_virtual/base-utils = "packagegroup-core-base-utils"
VIRTUAL-RUNTIME_base-utils = "packagegroup-core-base-utils"
```

注意最好是同时修改这两个变量，它们有一些些微的区别：

* `PREFERRED_PROVIDER...`：确保整个构建系统(尤其是构建时的依赖)都知道我们切换到了`packagegroup-core-base-utils`。
* `VIRTUAL-RUNTIME...`：确保最终的跟文件系统(rootfs)中包含我们想要的完整工具包。

两者结合才能够保证从编译到运行，整个系统都一致地使用我们选择的工具集。

---

# 5. Initramfs

## 5.1. 概念与由来

Initramfs 解决的是"先有鸡还是先有蛋"的问题：
- 内核挂载根文件系统需要存储介质驱动
- 存储介质驱动存放在根文件系统中

Initramfs 是一个基于内存的临时根文件系统，在真正的磁盘根文件系统之前加载。它包含挂载根文件系统所需的驱动程序，使得内核可以直接从内存中获取驱动，再引导真正的根文件系统。

与 `ramdisk` 的区别：

| | ramdisk | initramfs |
|:---|:---|:---|
| 实现 | 模拟块设备，需要文件系统 | 基于 tmpfs，无需文件系统 |
| 大小 | 固定 | 动态 |
| 格式 | 需要格式化 | 直接打包为 cpio |
| 缓存 | 双重缓存（块设备 + 文件） | 单层缓存 |

现代嵌入式设备即使板载存储驱动已固化，仍然使用 initramfs 的原因：
- **只读**：契合嵌入式产品需求
- **快速**：直接从内存加载
- **无驱动依赖**：便于快速开发验证

## 5.2. 制作 initramfs 镜像

参考 `poky` 中的 `core-image-minimal-initramfs.bb`，核心是将根文件系统目录打包为 `cpio` 格式（内核只能识别 cpio 格式的 initramfs）。

可以在配方中定义任务来自行打包：

```bash
# 使用 cpio 打包，并设置所有者为 root:root
cd ${IMAGE_ROOTFS} && find . | cpio --owner root:root -o -H newc | \
    gzip -9 > ${IMGDEPLOYDIR}/${MACHINE}.initramfs.cpio.gz
```

这样制作的 initramfs 可直接被内核识别并启动。

## 5.3. 启动方式与配置

### 5.3.1. 通过 extlinux 启动

修改 `<machine>.conf` 中的相关变量：

```bash
UBOOT_EXTLINUX_INITRD:default = "../*.cpio.gz"
UBOOT_EXTLINUX_ROOT:default = "root=/dev/ram0 rw"
```

将 initramfs 镜像加入到 `boot.img`：

```bash
IMAGE_BOOT_FILES ?= " \
    ... \
    *.cpio.gz \
"
```

配方会自动从 `deploy` 目录下寻找对应文件。

### 5.3.2. 通过 booti 命令启动

在 U-boot 命令行中手动启动。首先需要知道内存加载地址——这些通常在 U-boot 环境变量中预定义，可以用 `printenv` 查看：

```bash
# 查看预定义的内存地址
printenv kernel_addr_r      # 内核加载地址
printenv fdt_addr_r         # 设备树加载地址
printenv ramdisk_addr_r     # initramfs/ramdisk 加载地址
```

对于 rk3568，典型值为（具体以实际 U-boot 环境为准）：
- `${kernel_addr_r}`：如 `0x00280000`
- `${fdt_addr_r}`：如 `0x0a100000`
- `${ramdisk_addr_r}`：如 `0x0a200000`

然后手动加载并启动：

```bash
# 加载镜像到内存
fatload mmc 0:1 ${kernel_addr_r} Image
fatload mmc 0:1 ${fdt_addr_r} rk3568-avod.dtb
fatload mmc 0:1 ${ramdisk_addr_r} initramfs.cpio.gz.u-boot

# 启动（${filesize} 是 fatload 后自动设置的变量）
booti ${kernel_addr_r} ${ramdisk_addr_r}:${filesize} ${fdt_addr_r}
```

**注意**：`booti` 需要 U-boot 格式的 initramfs 才能正确识别大小。生成 U-boot 格式有两种方法：

**方法一：配方中手动调用 mkimage**

```bash
# 配方中依赖 mkimage
DEPENDS += "u-boot-mkimage-native"

# 在自定义任务中制作 u-boot 格式的 initramfs
mkimage -A arm64 -O linux -T ramdisk -C gzip \
    -d ${MACHINE}.initramfs.cpio.gz ${MACHINE}.initramfs.cpio.gz.u-boot
```

**方法二：通过 IMAGE_FSTYPES 让 bitbake 自动生成**

如果配方继承了 `core-image` 类，可直接添加：

```bash
IMAGE_FSTYPES = "cpio.gz.u-boot cpio.gz"
```

bitbake 会自动调用 mkimage 生成带 U-boot 头的镜像。注意这要求配方继承了 image 类（自定义的 rootfs 打包配方通常不继承，所以方法一更常用）。

### 5.3.3. extlinux 启动 vs 裸启动

| | 通过 extlinux 启动 | 裸镜像 booti 启动 |
|:---|:---|:---|
| 镜像格式 | `cpio.gz` | `cpio.gz.u-boot` (带 U-boot 头) |
| 配置方式 | `extlinux.conf` 中声明 | U-boot 命令行手动指定 |
| 适用场景 | 自动化部署 | 调试/手动启动 |

## 5.4. 常见问题

### 5.4.1. 登录卡住

使用标准流程制作的 initramfs 可能因 `shadow` 文件中 root 密码占位符 `*` 与 busybox 的识别逻辑冲突，导致登录界面不断循环（输入 `root` 回车后又回到登录提示）。解决：在镜像配方中保留 `empty-root-passwd` 的 `IMAGE_FEATURES`（该功能在 `poky/meta/classes-recipe/rootfs-postcommands.bbclass` 中定义），它会清除 root 密码占位符。

验证方法：
```bash
cat /etc/passwd
cat /etc/shadow  # 检查 root 行是否有 * 占位符
```

### 5.4.2. init 符号链接缺失

initramfs 中内核优先找 `/init`，如果不存在则找 `/sbin/init`。磁盘根文件系统中 `/sbin/init` 由 systemd/udev 启动后自动挂载 `proc`/`sys` 并创建设备节点；但 initramfs 中 `/sbin/init` 执行前无任何挂载，直接报错"找不到 `/proc/self`"而**卡死**（不是提示登录，是启动直接卡住），控制台无任何输出。

两个问题的本质区别：

| 对比项 | 磁盘根文件系统 (正常) | initramfs 内存根文件系统 (失败) |
|:---|:---|:---|
| 启动入口 | 内核找 `/sbin/init`（systemd/busybox init） | 内核优先找 `/init`（不存在），再找 `/sbin/init`，但其依赖未就绪 |
| 核心文件系统挂载 | `/sbin/init` 启动后 systemd/udev 自动挂载 `proc`/`sys` | 执行 `/sbin/init` 前无任何挂载，执行后直接报错 |
| 设备节点创建 | udev 自动创建 `/dev/console`/`/dev/ram0` | 无 udev，缺少这些节点 |

解决：在 rootfs 源码目录中创建软链接：
```bash
ln -s sbin/init ./init
```

> **注意**：这里链接目标是 `sbin/init`，而非一些教程说的 `sbin/sh`。具体取决于你的 initramfs 中 `/sbin/init` 是哪个程序（busybox 提供的 init 或 systemd）。

### 5.4.3. rockchip-image 类冲突

`rockchip-image` 类定义了厂商完整镜像制作流程。如果在 `local.conf` 中启用了该类：

```bash
INHERIT:append = " rockchip-image"
```

制作 initramfs 时可能与该类的任务注入冲突。且**无法在单个配方中用 `INHERIT:remove` 取消**——因为 `rockchip-image` 的任务已经被注入到配方任务链中，`INHERIT:remove` 只是从变量值中移除，不影响已注入的任务。同时还因为制作 `wic` 镜像的 `wks` 文件依赖 `rockchip-image` 中定义的环境变量，必须在全局继承。临时方案是在构建 initramfs 时注释掉 `local.conf` 中的 `INHERIT:append` 行。

---

# 6. Yocto 工程启动链

## 6.1. 原教旨主义 Boot（手动启动）

最传统的方式——直接烧写裸镜像，在 U-boot 命令行中手动加载启动：

```
烧写: uboot.img | Image | dtb | rootfs.ext4
U-boot: fatload + booti
```

完全不依赖于厂商定制流程，灵活度最高。系统一定需要的组件就四个：**U-boot**、**内核镜像（Image/zImage）**、**设备树（dtb）**、**根文件系统（rootfs/initramfs）**。除 rootfs 外，其他三个都可通过编译源码直接得到二进制文件。

具体将内核和 dtb 烧录到 eMMC 的哪个位置、加载到内存的哪个地址，参考厂商提供的 U-boot 环境变量中预定义的内存地址（用 `printenv` 查看），如 `${kernel_addr_r}`、`${fdt_addr_r}`、`${ramdisk_addr_r}`。U-boot 在自动启动时也会将文件加载到这些地址——手工启动只是手动执行相同的操作。

## 6.2. 厂商官方 Boot（Rockchip boot.img）

Rockchip 的 `bbclass` 中内置了制作 `boot.img` 的逻辑。该镜像带有 **Android 头**：

```
$ file boot.img
boot.img: Android bootimg, kernel (0x10008000), second stage (0x10f00000), page size: 2048
```

在 U-boot 中使用 `boot_android` 命令启动。这种方式需要手动设置 `bootargs`：

```bash
setenv bootargs "root=/dev/mmcblk2p4 rootwait rw console=tty2,115200"
saveenv
boot_android mmc 0:3
```

**注意**：不要与 wic 镜像中带文件系统的 `/boot` 分区混淆——两者都叫 "boot"，但是不同的概念。

## 6.3. extlinux Boot

U-boot 启动内核的标准模式。将 `extlinux.conf`、`Image`、`*.dtb` 以及可选的 `initramfs` 打包到一起形成 `/boot` 分区。U-boot 在 `bootcmd` 中扫描存储介质，找到该分区后读取 `extlinux.conf` 并按其配置启动。

Yocto 支持通过 `UBOOT_EXTLINUX_*` 系列变量配置 extlinux 启动参数，这些变量定义在 `poky/meta/classes-recipe/uboot-extlinux-config.bbclass` 中。

## 6.4. wic 镜像启动

通过 `wks` 脚本将全部组件打包成一个 `*.wic` 镜像，仅需烧录这一个文件即可启动。详见第 6 节。

### 6.4.1. 启动方式对比

| 启动方式 | 复杂度 | 灵活度 | 适用场景 |
|:---|:---|:---|:---|
| 原教旨 (裸镜像 + booti) | 低 | 最高 | 调试、开发 |
| 厂商 Boot (boot_android) | 中 | 中 | 快速适配厂商方案 |
| extlinux Boot | 中 | 中 | 标准化部署 |
| wic 镜像 | 高（配置复杂） | 低 | 生产环境一键烧录 |

---

# 7. wks 文件语法解析

在 Yocto 工程中，Rockchip 推荐的官方镜像制作方法是制作一个 `wic` 大镜像，里面会囊括所有的组件。而 `wks` 文件就是指导制作 `wic` 镜像的说明书/蓝图。它使用的语法是 `kickstart` 语法（和 Redhat 系统有关系，我没有深究）。

Yocto 中通常以 `.wks.in` 模板形式存在，其中的 `${变量}` 由 BitBake 替换。

我们以已有的 `wks.in` 模板文件为基准来分析如何写一个 `wks` 文件：

```bash
# 指定 GPT 分区表，适配 emmc
bootloader --ptable gpt
# idblock 分区
part --source rawcopy --sourceparams="file=idblock.img" --align 32 --no-table
# U-boot 分区：4MB，rawcopy 烧录，4MB 偏移起始
part --source rawcopy --sourceparams="file=uboot.img" --part-name uboot --offset 4M --align 4
# U-boot-env 分区：2MB，无文件系统
part --part-name uboot-env --fstype=none --align 4 --fixed-size 2M
# Boot 分区：256MB，ext4 文件系统，挂载 /boot，8MB 对齐，启动分区
part /boot --source bootimg-partition --fstype=ext4 --label boot --active \
    --mkfs-extraopts "${RK_ROOTFS_EXTRAOPTS}" --align 8192 --fixed-size 256M \
    --sourceparams="loader=u-boot" --use-uuid
# Rootfs 分区：512MB，根文件系统，8MB 对齐
part / --source rootfs --fstype ${RK_ROOTFS_TYPE} --part-name rootfs \
    --uuid ${RK_ROOTDEV_UUID} \
    --mkfs-extraopts "${RK_ROOTFS_EXTRAOPTS} -e continue" \
    --align 8192 --fixed-size 512M
```

## 7.1. 全局指令

```bash
bootloader --ptable gpt
```

- `bootloader`：声明要生成一个带有引导加载程序的镜像，并指定了属性
- `--ptable gpt`：明确使用 **GUID 分区表 (GPT)**，而不是传统的 MBR。这是现代嵌入式系统推荐的方式，尤其适合大容量 eMMC，能够提供冗余分区表、分区名、UUID 等

## 7.2. part 命令核心选项

| 选项 | 含义与作用 | 典型用法 |
|:---|:---|:---|
| `--source` | **内容来源插件**，告诉 wic 如何生成该分区的数据 | `rawcopy`（原样拷贝镜像），`bootimg-partition`（自动生成启动分区），`rootfs`（拷贝根文件系统） |
| `--sourceparams` | **传递给插件的参数** | `file=uboot.img` 指示 `rawcopy` 插件去拷贝指定文件的内容 |
| `--part-name` | **GPT 分区名**，用于 U-Boot、fastboot 等按名称查找分区 | `uboot`, `trust`, `rootfs` 等 |
| `--fstype` | **文件系统类型** | `ext4`, `vfat` 或 `none`（表示分区不创建文件系统，用于存放裸二进制数据） |
| `--label` | **文件系统卷标**（与 GPT 分区名不同） | `boot`，系统挂载后会显示为 `/dev/disk/by-label/boot` |
| `--fixed-size` | **精确分区大小**，单位 M、K 等 | `--fixed-size 256M` |
| `--align` | **分区起始和大小对齐边界**，单位 KiB。目的是优化存储性能（如 eMMC 的写放大） | 大部分 4 (4KiB) 或 8192 (8MiB)。8MiB 对齐是 eMMC 典型的最佳实践 |
| `--offset` | **分区起始的绝对偏移**，单位 K、M 等。用于固定分区物理位置 | `uboot` 分区从偏移 4M 开始 |
| `--no-table` | 告诉 wic **不要将此分区加入分区表** | `idblock` 分区使用了此选项 |
| `--active` | 在分区表中**标记活动标志**（legacy bootable），使得 U-Boot 可以识别为启动分区 | `boot` 分区启用了此标志 |
| `--use-uuid` | 在 `/etc/fstab` 中使用分区的 **UUID** 来挂载，而非设备名（如 `/dev/mmcblk0p5`），保证挂载的稳定性 | `rootfs` 和 `boot` 都用了 |
| `--uuid` | **直接指定 UUID** 值，而不是让系统随机生成 | `${RK_ROOTDEV_UUID}` 是变量，保证构建的一致性 |
| `--mkfs-extraopts` | **创建文件系统时传递额外参数**给 `mkfs.*` 命令 | `${RK_ROOTFS_EXTRAOPTS}` 是一个变量，可能包含 `-E lazy_itable_init` 等优化选项 |

## 7.3. 关键分区解析

### 7.3.1. idblock 分区（不含分区表）

```bash
part --source rawcopy --sourceparams="file=idblock.img" --align 32 --no-table
```

- **作用**：Rockchip 的 IDBLOCK 是 eMMC 启动时最先被芯片内部 BootROM 读取的块，包含 DDR 初始化、时钟配置等关键信息
- **`--no-table`**：它不在 GPT 分区表中出现，因为 BootROM 只认固定物理地址（通常是 eMMC 的扇区 64 或 0），不能被常规操作系统干扰
- **`--align 32`**：32KiB 对齐，确保其在预期的物理位置

### 7.3.2. U-Boot 分区（偏移 4M）

```bash
part --source rawcopy --sourceparams="file=uboot.img" --part-name uboot --offset 4M --align 4
```

- **`--offset 4M`**：强制此分区起始于 eMMC 的 4MiB 偏移处。这通常是 Rockchip 平台 U-Boot SPL/TPL 的存放位置（紧接 IDBLOCK 之后）
- **`--align 4`**：4KiB 对齐

### 7.3.3. U-Boot 环境变量分区

```bash
part --part-name uboot-env --fstype=none --align 4 --fixed-size 2M
```

- **`--fstype=none`**：此分区存储的是 U-Boot 环境变量的二进制数据，不需要文件系统
- **目的**：将环境变量存储在独立的分区，方便升级 U-Boot 时保留配置，或通过读写分区来恢复出厂设置

### 7.3.4. Boot 分区（启动分区）

```bash
part /boot --source bootimg-partition --fstype=ext4 --label boot --active \
    --mkfs-extraopts "${RK_ROOTFS_EXTRAOPTS}" --align 8192 --fixed-size 256M \
    --sourceparams="loader=u-boot" --use-uuid
```

- **`--source bootimg-partition`**：特殊插件，会自动将内核、设备树、ramdisk 等文件按格式整合到此分区，通常配合 `loader=u-boot` 参数使用，指示生成 U-Boot 可识别的启动镜像格式（如 Android boot image 或 FIT）
- **`--active`**：标记为可引导分区，这样 U-Boot 的 distro boot 流程才能自动找到它

### 7.3.5. Rootfs 分区

```bash
part / --source rootfs --fstype ${RK_ROOTFS_TYPE} --part-name rootfs \
    --uuid ${RK_ROOTDEV_UUID} \
    --mkfs-extraopts "${RK_ROOTFS_EXTRAOPTS} -e continue" \
    --align 8192 --fixed-size 512M
```

- **`--source rootfs`**：直接拷贝 Yocto 构建生成的根文件系统目录内容到此分区
- **`-e continue`**：`mkfs.ext4` 的选项，表示当遇到错误时，文件系统继续挂载而不是触发 panic，这对嵌入式产品容错很重要
- **`--fixed-size 512M`**：强制根文件系统分区固定为 512MiB

文件中出现的 `${RK_ROOTFS_TYPE}`、`${RK_ROOTDEV_UUID}`、`${RK_ROOTFS_EXTRAOPTS}` 都是在 `rockchip-image.bbclass` 或配方中定义好的 BitBake 变量。这样一份 `.wks.in` 模板可以通过不同机器配置文件传递不同参数，实现复用。

## 7.4. 创建新分区

由于 wks 文件比起 `parameter` 更加强大，可以直接通过配置文件来实现新增分区，设置文件系统和自动挂载。所以这里尝试配置一下 wks 文件来实现新增分区。

我们还是和 `parameter`一样吧，新增一个 `testfs` 分区，我们只需要在 wks 文件最后新增一个：

```bash
part --part-name testfs --fstype=ext4 --fixed-size 1024M --align 8192 
```

这样我们就仅仅是在最后新增了一个大小为 1G 的独立空白分区，并且有 `ext4` 文件系统。这样我们重新构建我们的 `wic` 镜像，再烧录到板子上，启动系统可以看到：

```bash
root@rk3568-avod:~# blkid
/dev/mmcblk0p5: UUID="7d55601a-8795-45da-b0be-ef2ad1d4d6d1" BLOCK_SIZE="1024" TYPE="ext4" PARTLABEL="testfs" PARTUUID="8d427b80-6cfb-4556-bf39-f8de4e0e73e8"
/dev/mmcblk0p3: LABEL="boot" UUID="43ea75b8-b7d5-4312-b5fa-dbb49703ab23" BLOCK_SIZE="4096" TYPE="ext4" PARTLABEL="boot" PARTUUID="e3b20a66-7c8b-4c85-a426-7290e1af9c30"
/dev/mmcblk0p1: PARTLABEL="uboot" PARTUUID="a4eb0ae5-60ba-4502-90f2-980ca9691fde"
/dev/mmcblk0p4: UUID="9aeb91bf-388f-4763-adaa-5c47549fd7fc" BLOCK_SIZE="4096" TYPE="ext4" PARTLABEL="rootfs" PARTUUID="614e0000-0000-4b53-8000-1d28000054a9"
/dev/mmcblk0p2: PARTLABEL="uboot-env" PARTUUID="2a5cabfe-3597-46ed-a0d5-bc5526ac9267"
```
使用 `blkid` 可以看到分区的一些信息，比如 `UUID` 和 `PARTLABEL`，这里可以看到 `mmcblk0p5` 的名字就是我们设置的 `testfs`。

```bash
root@rk3568-avod:~# lsblk -l
NAME         MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS
mmcblk0      179:0    0 28.9G  0 disk
mmcblk0p1    179:1    0    4M  0 part
mmcblk0p2    179:2    0    2M  0 part
mmcblk0p3    179:3    0  256M  0 part /boot
mmcblk0p4    179:4    0  512M  0 part /
mmcblk0p5    179:5    0  256M  0 part
mmcblk0boot0 179:32   0    4M  1 disk
mmcblk0boot1 179:64   0    4M  1 disk
```

```bash
root@rk3568-avod:~# lsblk -f
NAME         FSTYPE FSVER LABEL UUID                                 FSAVAIL FSUSE% MOUNTPOINTS
mmcblk0
|-mmcblk0p1
|-mmcblk0p2
|-mmcblk0p3  ext4         boot  43ea75b8-b7d5-4312-b5fa-dbb49703ab23  186.8M    12% /boot
|-mmcblk0p4  ext4               9aeb91bf-388f-4763-adaa-5c47549fd7fc  249.6M    40% /
`-mmcblk0p5  ext4               7d55601a-8795-45da-b0be-ef2ad1d4d6d1
mmcblk0boot0
mmcblk0boot1
```

使用 `lsblk -f` 可以看到每个分区的文件系统类型。这里我们直接为 `p5` 分区设置好了 `ext4` 文件系统。那么接下来我们可以直接将其挂载到一个特定的目录下了。接下来的操作就和 `parameter` 时一样了，可以直接参考 [`parameter`新增分区](#73-新增分区以日志分区为例)。

## 7.5. 更进一步？

不过我们的需求可以更进一步：**我们是否可以将 rootfs 中的个别目录拆分出来，单独挂载到新的分区中**？比如我们将 `/home` 目录拆出来单独进行挂载？

答案当然是可以的，但是需要做的操作可能会更复杂一些。不过需要注意的是这里我们都是使用的 `wic` 镜像，不考虑使用组件镜像的情况。

wks 文件中的 `part / --source rootfs ...` 命令就是告诉 `wic`：请把整个根文件系统全部放入这一个分区中，如果我们定义两个 `--source rootfs` 分区，它们都会包含完整的根文件系统，这显然不对。

我考虑过使用D老师给出的一个方案：

> 使用 `--exclude-path` 和 `--rootfs-dir` 选项来从根文件系统中提取子目录放到独立分区中。不过这个尝试虽然在 `wic` 镜像构建时没有问题，但是内核启动时会出现问题。可能是配置没有完整？
> D老师随后告诉我这个方案其实并不是 Yocto 主流版本的做法，它并不稳定。

比较可靠的办法其实是将我们需要独立挂载的目录直接单独拆出来，构建一个独立的镜像。这样我们需要为每个镜像单独编写 `.bb` 文件。比如我们想要拆分 `/usr` 目录，这个目录下都是安装好的软件包，那么我们可能需要单独编写 `core-image-base.bb` 和 `core-image-usr.bb` ，最后需要将它们合并到一个完整的 `wic` 中的 `core-image-merge.bb`。这样我们相当于将根文件系统镜像拆分了，在 wks 文件中自然也就可以分别进行引用了。这个方法可能是一个比较标准的做法。

将 `/usr` 目录拆分出来倒是很容易，我们只需要在我们的 `wic` 镜像配方中加入一个 `rootfs` 的后处理就行了，这里给一个简单的示例：

```bash
# 在 rootfs 构建完成后执行拆分操作

split_usr_dir() {
    # 确保临时目录是干净的
    rm -rf ${USR_ROOTFS_DIR}
    # 将整个 /usr 目录复制到临时目录
    cp -a ${IMAGE_ROOTFS}/usr ${USR_ROOTFS_DIR}
    # 情况原 rootfs 里的 /usr (保留空目录，确保挂载点存在)
    find ${IMAGE_ROOTFS}/usr -mindepth 1 -delete
}
ROOTFS_POSTPROCESS_COMMAND += "split_usr_dir;"
```

然后在 wks 文件中配置一下：

```bash
part / --source rootfs --rootfs-dir=${IMAGE_ROOTFS} --fstype=ext4 --label rootfs --uuid ${RK_ROOTDEV_UUID} --mkfd-extraopts "${RK_ROOTFS_EXTRAOPTS -e continue" --align 8192 --fixed-size 256M
part /usr --source rootfs --rootfs-dir=${USR_ROOTFS_DIR} --fstype=ext4 --label usr --align 8192 --size 512M
```

但是这里有一个更加严重的问题：**`/usr`分区在系统初始阶段是没有挂载的！**。我的想法是将挂载命令放到 `extlinux` 中，但是被D老师否决了。`extlinux` 的确是 U-boot 启动流程中很关键的配置，但是它无法自动挂载一个独立分区。我们的 `/usr` 需要在**根文件系统挂载后、`init` 进程启动之前**挂载就绪。而 `extlinux` 能够完成的工作是让 U-boot 读取其中的配置，然后加载内核、设备树并向内核传递启动参数。他也只能完成这些工作，**更多复杂的工作不归他管**。

这里就必须要引入 `initramfs`。这里算是要用到 `initramfs` 最原始的用途了。也就是说我们需要使用 `initramfs` 来完成根文件系统的挂载，然后挂载 `/usr`，再移交给完整的根文件系统。这里就提这么一个方法吧，mark 一下，之后有机会再去研究。

当然最后 `parameter` 分区使用的则是我们使用组件单独烧录时的了，这个方法更自由，不过挂载情况我们可能需要进入内核后使用脚本或者自己敲命令来完成了。使用 `parameter` 想要实现目录分别挂载也许还需要进一步研究一下。

---

# 8. parameter 分区表与分区管理

`parameter` 文件是 Rockchip 特有的一种分区描述文件，用于指导系统划分和管理存储设备。它一般是由 SDK 或者其他构建工程自动生成。我们主要是关注其中的 `CMDLINE`，它是**核心分区信息**，包含传递给内核的命令行参数以及 `mtdparts` 定义。

## 8.1. 格式解析

parameter 格式类似：

```
# IMAGE_NAME: cetca-avod-minimal-rk3568-avod.rootfs-20251216070241.wic
FIRMWARE_VER: 1.0
TYPE: GPT
CMDLINE: mtdparts=rk29xxnand:0x00002000@0x00002000(uboot),0x00001000@0x00004000(uboot-env),0x00080000@0x00008000(boot:bootable),-@0x00088000(rootfs:grow)
uuid: rootfs=614e0000-0000-4b53-8000-1d28000054a9
```

`CMDLINE` 字段是分区的核心，它使用 `mtdparts` 格式定义：

```
size@offset(name:tag)
```

- **大小 (`size`)** 和**起始地址 (`offset`)** 单位为**扇区 (Sector)**，1 扇区 = 512 字节
- **保留区域**：eMMC 最前端空间用于保护启动代码和数据
- **自动增长分区**：`-@` 作为 size，`name:grow` 表示占用所有剩余空间
- **`bootable` 标签**：标识可启动分区，U-boot 依赖此标签找到内核

## 8.2. 分区大小换算

```
1 GB = 1024 × 1024 × 1024 Bit
1 Sector = 512 Bit
1 GB = 2 × 1024 × 1024 Sector = 0x20 0000 Sector
```

## 8.3. 新增分区（以日志分区为例）

可以看到原来的分区表中每一个分区镜像都是比较紧凑的，然后将 `rootfs` 分区设置为自动增长，相当于将之后所有的 eMMC 都给了这个分区。我们如果想要在后面增加一个用于存放日志的分区，需要进行两步操作：

1. 修改 `parameter` 分区表
2. 进入小系统，添加文件系统并挂载

### 8.3.1. 步骤 1：修改 parameter

将 `rootfs` 从自动增长改为固定大小，并在其后新增分区：

```
CMDLINE: mtdparts=rk29xxnand:0x00002000@0x00002000(uboot),0x00001000@0x00004000(uboot-env),0x00080000@0x00008000(boot:bootable),0x200000@0x00088000(rootfs),0x80000@0x288000(testfs)
```

由于我们只是新增分区，没有新的镜像，因此只需加载新的 `parameter` 重新烧录一下就好了。

### 8.3.2. 步骤 2：进入系统后创建文件系统并挂载

进入内核后我们可以通过 `lsblk -l` 查看分区情况：

```bash
root@rk3568-avod:/mnt/emmc_p5# lsblk -l
NAME         MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS
mmcblk0      179:0    0 28.9G  0 disk
mmcblk0p1    179:1    0    4M  0 part
mmcblk0p2    179:2    0    2M  0 part
mmcblk0p3    179:3    0  256M  0 part
mmcblk0p4    179:4    0    1G  0 part /
mmcblk0p5    179:5    0  256M  0 part
mmcblk0boot0 179:32   0    4M  1 disk
mmcblk0boot1 179:64   0    4M  1 disk
```

可以发现多了一个 `mmcblk0p5`，这就是新增的分区，是一个空白的裸分区。现在通过工具为其增加文件系统并挂载到一个固定目录：

```bash
# 格式化为 ext4
mkfs.ext4 /dev/mmcblk0p5
```

该操作会**永久删除**该分区中的所有数据。执行成功后会输出关于文件系统的信息：

```bash
root@rk3568-avod:~# mkfs.ext4 /dev/mmcblk0p5
mke2fs 1.47.0 (5-Feb-2023)
Discarding device blocks: done
Creating filesystem with 262144 1k blocks and 65536 inodes
Filesystem UUID: ff2b1db1-35c7-48fa-941b-0f85e8565dee
Superblock backups stored on blocks:
        8193, 24577, 40961, 57345, 73729, 204801, 221185

Allocating group tables: done
Writing inode tables: done
Creating journal (8192 blocks): done
Writing superblocks and filesystem accounting information: done
```

然后创建挂载点并挂载：

```bash
mkdir -p /mnt/emmc_p5
mount /dev/mmcblk0p5 /mnt/emmc_p5
```

完成之后可以查看情况：

```bash
root@rk3568-avod:/mnt# df -h
Filesystem                Size      Used Available Use% Mounted on
/dev/root               271.1M    184.5M     67.6M  73% /
devtmpfs                  1.9G         0      1.9G   0% /dev
tmpfs                     1.9G         0      1.9G   0% /dev/shm
tmpfs                   781.8M      8.8M    773.0M   1% /run
tmpfs                     1.9G         0      1.9G   0% /tmp
tmpfs                     1.9G    172.0K      1.9G   0% /var/volatile
tmpfs                   390.9M      4.0K    390.9M   0% /run/user/0
/dev/mmcblk0p5          229.9M     78.0K    213.0M   0% /mnt/emmc_p5
```

可以看到 eMMC 的 p5 分区已经成功挂载了。

### 8.3.3. 步骤 3：配置开机自动挂载

不过目前只是暂时挂载，重启之后又需要手动挂载。我们可以通过修改 `/etc/fstab` 文件来指定分区开机就挂载。推荐使用 `UUID` 来指定设备，比起直接使用 `/dev/mmcblk0p5` 更加稳定：

```bash
# 获取分区的 UUID
root@rk3568-avod:/mnt# blkid /dev/mmcblk0p5
/dev/mmcblk0p5: UUID="ff2b1db1-35c7-48fa-941b-0f85e8565dee" BLOCK_SIZE="1024" TYPE="ext4" PARTLABEL="testfs" PARTUUID="6a7c0000-0000-4804-8000-0f8700005208"
```

然后编辑 `/etc/fstab` 文件，在末尾添加：

```bash
UUID=ff2b1db1-35c7-48fa-941b-0f85e8565dee /mnt/emmc_p5 ext4 defaults 0 0
```

格式说明：
- `UUID`：分区的唯一标识（也可直接用 `/dev/mmcblk0p5`，但 UUID 更稳定）
- `/mnt/emmc_p5`：挂载点
- `ext4`：文件系统类型
- `defaults`：默认挂载选项（读写、自动挂载等）
- `0`：dump 备份选项（通常为 0）
- `0`：fsck 开机检查顺序（根分区 `/` 是 1，非系统分区通常设为 0 或 2）

使用 `mount -a` 来检查我们的格式是否正确。完成后重启也可以看到我们的分区会自动挂载了。

---

# 9. 日志分区的创建与挂载

产品中很重要的可能是日志的保存。不过在此之前，Linux 内核的日志本身就是一个没有那么简单的东西。

## 9.1. rsyslog.service

### 9.1.1. rsyslog 历史渊源

rsyslog 是传统的 syslog 守护进程的主流实现。它负责收集系统各组件(内核、服务、应用)产生的文本日志，根据规则(/etc/rsyslog.conf)把日志写到不同的文件(比如 `/var/log/messages`、`/var/log/secure` 等)。**它不依赖 systemd**，可以运行在任何 `init` 系统(SysV init、OpenRC 甚至 systemd)之上。

在大多数使用 systemd 的主流发行版中， rsyslog 通常也会被安装并运行，它会从 journald(接下来会讲) 读取日志 (通过一些方式来获取 journald 的信息)，然后写入到 `/var/log` 下的文本文件中。而实际上这个目录是挂载在 `/` 根目录下的，并不是一个 `tmpfs`，即使掉电了也不会丢失。

### 9.1.2. rsyslog 内核文件

前辈告诉了一个叫做 `rsyslog` 的服务，在 `lib/systemd/system`  下能够找到一个叫做 `rsyslog.service` 的服务：

```bash
[Unit]
Description=System Logging Service
Requires=syslog.socket
Wants=network.target network-online.target
After=network.target network-online.target
Documentation=man:rsyslogd(8)
Documentation=http://www.rsyslog.com/doc/

[Service]
Type=notify
ExecStart=/usr/sbin/rsyslogd -n -iNONE
StandardOutput=null
Restart=on-failure

# Increase the default a bit in order to allow many simultaneous
# files to be monitored, we might need a lot of fds.
LimitNOFILE=16384

[Install]
WantedBy=multi-user.target
Alias=syslog.service
```

而这个 `rsyslog.service` 的配置文件位于 `/etc/rsyslog.conf`，其中可以配置不同的日志保存位置。

## 9.2. systemd-journald

使用 systemd 的系统自带 `systemd-journald` 服务来管理日志。它将日志以二进制格式储存在 `/run/log/journal/`(临时) 或者 `/var/log/journal`(持久化)，并且提供 `journalctl` 命令查看。**它只在 systemd 系统上存在**。换句话说， systemd 系统默认会运行该服务。

### 9.2.1. journald 内核文件

服务文件在 `/lib/systemd/system/systemd-journald.service`。配置文件在 `etc/systemd/journald.conf`。

这里不列出具体的内容，自行查看就好。

## 9.3. 两个日志服务的关系

较新的内核一般都使用 systemd 作为 `init` 进程(这一块可能说的不太准确，还需要了解一下)。其实可以查看我们自己制作的根文件系统中去查看，目录下的 `init` 指向的是哪一个具体的文件：

```bash
lrwxrwxrwx  1 public public    9 12月 29 15:52 init -> sbin/init
lrwxrwxrwx 1 public public 22  3月  9  2018 sbin/init -> ../lib/systemd/systemd
```

很明显我们使用的就是 systemd 。

我们还可以通过 `systemctl` 命令查看我们启用了哪些日志服务：

```bash
root@rk3568-avod:/# systemctl status systemd-journald
* systemd-journald.service - Journal Service
     Loaded: loaded (/usr/lib/systemd/system/systemd-journald.service; static)
     Active: active (running) since Thu 2025-05-29 18:48:23 UTC; 52min ago
TriggeredBy: * systemd-journald-audit.socket
             * systemd-journald.socket
             * systemd-journald-dev-log.socket
       Docs: man:systemd-journald.service(8)
             man:journald.conf(5)
   Main PID: 158 (systemd-journal)
     Status: "Processing requests..."
   FD Store: 10 (limit: 4224)
        CPU: 366ms
     CGroup: /system.slice/systemd-journald.service
             `-158 /usr/lib/systemd/systemd-journald
```

```bash
root@rk3568-avod:/# systemctl status rsyslog.service
* rsyslog.service - System Logging Service
     Loaded: loaded (/usr/lib/systemd/system/rsyslog.service; enabled; preset: enabled)
     Active: active (running) since Thu 2025-05-29 19:08:58 UTC; 32min ago
TriggeredBy: * syslog.socket
       Docs: man:rsyslogd(8)
             http://www.rsyslog.com/doc/
   Main PID: 281 (rsyslogd)
        CPU: 64ms
     CGroup: /system.slice/rsyslog.service
             `-281 /usr/sbin/rsyslogd -n -iNONE
```

这里我们两个日志系统都启用了。我们在 `/etc/rsyslog.conf` 中看到其实 rsyslog 通过 `imuxsock` 模块监听由 journald 提供的 套接字(文件)。

```bash
$ModLoad imuxsock # provides support for local system logging (e.g. via logger command)
```

我们就先不究细节，只了解其流程。 这样看起来我们目前的小系统就是通过一个 systemd 默认的 journald 暂存日志(放在 `/run/log/journal/`下)，然后通过 rsyslog 转存日志到 `/var/log/` 中。

## 9.4. 新建日志分区并挂载

实际产品中我们希望日志能够在一个单独的分区中保存，与根文件系统独立，保证根文件系统损坏也不影响日志分区。我们已经可以通过 [wks文件](#6-wks-文件语法解析) 和 [parameter分区表](#7-parameter-分区表与分区管理)实现了在 eMMC 上新建分区并简单挂载。

但是如何实现让日志文件保存到我们的对应分区中其实还需要一些额外的工作。

### 9.4.1. 前辈的方法

前辈的方法是既然使用了 systemd，并且 systemd 中的服务启动顺序是有先后的(可以自己写依赖)，所以我们可以自己创建一个 mount 任务，这个任务会去将我们创建的 log 分区挂载到比如 `/var/log` 处，然后再启动我们的 rsyslog 服务。

最好是保证先后顺序。因为我们提前修改了 rsyslog.conf ，改变了日志文件保存路径，但是分区还未挂载时，如果我们的分区挂载路径是一个全新的位置，那么服务会启动失败(无效路径)。我们可以让 rsyslog 服务依赖我们的 mount 任务确保成功。

其实这里涉及到一个问题， `/var/log` 目录是本来就存在的，我们如果新建分区并挂载到该路径下，那么原来路径下的文件和分区中的文件该怎么处理？是合并还是直接覆盖掉(原路径的文件清理)？

> 答案还蛮意外的，原路径下的文件不会动，只是被隐藏起来了。当我们的分区 umount 后，又可以看到原来的文件了

也就是说自己写一个 `/etc/systemd/system/var-log.mount` 文件来实现在日志服务启动前挂载日志分区是一个非常标准，细粒度精准控制的方法。我们可以在此之前完成对日志的迁移，然后挂载复原。

这里给一个简单的例子(未验证)：

```bash
[Unit]
Description=Log partition
Before=rsyslog.service   # 必须在此服务前启动
[Mount]
What=/dev/mmcblk1pN      # 你的日志分区
Where=/var/log
Type=ext4
Options=defaults,noatime
[Install]
WantedBy=multi-user.target
```

```bash
# 启用并验证
systemctl enable var-log.mount   # 启用单元
systemctl start var-log.mount    # 立即挂载
systemctl status var-log.mount   # 检查状态
systemctl show rsyslog.service | grep After=  # 验证依赖
```

### 9.4.2. 更简单的做法

我们想让新建的分区能够自动挂载，其实在 [自动挂载](#733-步骤-3配置开机自动挂载) 这一小节中给了方法。就是修改 `/etc/fstab` 文件。

如果想要防止潜在的安全风险，还可以为分区添加一些限制：

```bash
/dev/mmcblk0pN  <your/log/dir>  ext4  defaults,noatime,nosuid,nodev,noexec  0  0
```

* `nosuid`：禁止 `setuid` 可执行文件
* `nodev`：禁止在日志目录下创建设备节点文件
* `noexec`：禁止在日志目录下运行可执行程序

这样虽然可以自动挂载，但是空白的分区是没有文件系统的，需要先为其制作文件系统了才可以挂载。总的来说最好就是使用 systemd 服务来完成是比较合适的。


---

# 10. uboot-env 分区配置

为了方便调试，可以在单独划分一个区域来储存 U-boot 的环境变量，一般将这个分区放在 U-boot 分区后。如果采用工具烧写，可能可以直接修改一个分区表文件。

这只是在烧写过程中预留了一块地址，但是还需要让 U-boot 知道我可以在这个地方保存环境变量。这个需要去修改 U-boot 源码。

直接修改 U-boot 源码目录下 `configs/<your_arch>.defconfig`，在末尾添加：

```bash
##############################################
# 允许储存环境变量
CONFIG_ENV_IS_IN_MMC=y
# 储存环境变量的设备号 (0 = eMMC)
CONFIG_SYS_MMC_ENV_DEV=0
# 储存环境变量的分区号 (根据分区表)
CONFIG_SYS_MMC_ENV_PART=2
# 环境变量偏移量 (8MB = 0x800000)
CONFIG_ENV_OFFSET=0x800000
# 环境变量大小 (2MB = 0x200000)
CONFIG_ENV_SIZE=0x200000
##############################################
```

**注意**：`CONFIG_ENV_OFFSET` 和 `CONFIG_ENV_SIZE` 使用字节地址，而分区表中使用扇区地址，需要换算（1 扇区 = 512 字节）。

更标准的做法是使用 `menuconfig` 进入可视化菜单来选中相关选项（避免因 U-boot 版本不同导致宏名称差异，手写可能会出现配置不生效的情况）。

完成后，重新编译烧写 U-boot，进入 U-boot 命令行后我们就可以使用 `saveenv` 命令了。

---

# 11. Rockchip 烧写指南

## 11.1. 烧写工具

Rockchip 在 Windows 端和 Linux 端都提供自己的烧写工具。

Windows 端工具 `RKDevTool`：
- **v2.84**：经验证可用，能够整体烧录 `wic` 镜像
- **v2.96**：存在初始扇区禁止烧录等问题，不推荐。v2.96 不能烧写 wic 镜像，分开烧写又存在一些其他问题

## 11.2. Boot 分区烧写

### 11.2.1. 带文件系统的 boot 分区

Rockchip 的 U-boot 引导方式似乎比较特别。这里采用的是利用 `extlinux.conf` 的方式进行引导。该种引导方式需要为 `boot.img` 也就是 `kernel+dtb` 打包的镜像套一层文件系统，U-boot 通过进入该文件系统的 `extlinux` 目录找到 `extlinux.conf` 文件，读取其配置来引导内核和根文件系统。

而使用 `wks` 文件制作 `wic` 时会发现，只有最终生成的 `.wic` 大镜像中的 `boot` 分区是带文件系统的。在 `deploy/images` 目录下生成的中间组件镜像时，`boot.img` 是不带文件系统的，仅仅是内核+设备树的二进制文件打包并且加了一个 `Android` 头。这个头使得 U-boot 需要使用特别的启动命令 `boot_android` 来启动内核。

从 `wic` 大镜像中拆出带文件系统的 boot 分区：

```bash
# 使用 fdisk -l 查看 .wic 镜像的分区信息
fdisk -l *.wic
    Disk avod-image-minimal-rk3568-avod.rootfs.wic：5.33 GiB，5721048064 字节，11173922 个扇区
    单元：扇区 / 1 * 512 = 512 字节
    扇区大小(逻辑/物理)：512 字节 / 512 字节
    I/O 大小(最小/最佳)：512 字节 / 512 字节
    磁盘标签类型：gpt
    磁盘标识符：00000000-0000-0000-0000-00004D9B9EF0

    设备                                          起点     末尾    扇区  大小 类型
    avod-image-minimal-rk3568-avod.rootfs.wic1    8192    16383    8192    4M Microsoft 基本数据
    avod-image-minimal-rk3568-avod.rootfs.wic2   16384    20479    4096    2M Linux 文件系统
    avod-image-minimal-rk3568-avod.rootfs.wic3   32768   557055  524288  256M Linux 文件系统
    avod-image-minimal-rk3568-avod.rootfs.wic4  557056  4751359 4194304    2G Linux 文件系统
    ...

# 使用 dd 拆解出 boot 分区
# bs=512 (扇区大小), skip=分区起始扇区, count=分区大小扇区数
dd if=*.wic of=boot.ext4 bs=512 skip=32768 count=524288
```

然后使用这个带文件系统的镜像来烧录。

### 11.2.2. 裸镜像启动（带 Android 头）

如果想要使用裸镜像，也就是 `deploy/` 目录下的 `boot.img` 镜像，这个镜像被添加了一个 `Android` 头，在 U-boot 命令行中可以使用 `boot_android` 来解析并启动：

```bash
setenv bootargs "root=/dev/mmcblk2p4 rootwait rw console=tty2,115200"
saveenv
boot_android mmc 0:3
```

正常来说 `Android` 头应该在制作时就会携带启动参数，但是可能是这里我们本意并不是生成该镜像，它只是一个中间产物，因此缺少了这些参数，需要手动设置 `bootargs`。

### 11.2.3. bootable 标记

如果分开烧写（使用 `parameter` 分区表 + 各组件镜像），需要在 `parameter` 中为 boot 分区添加 `bootable` 标签：

```
0x00080000@0x00008000(boot:bootable)
```

`wic` 大镜像在制作时内部为 boot 分区标记了可启动（可用 `sudo parted *.wic print` 查看）：

```bash
型号： (file)
磁盘 /home/public/yocto-avod/build-minimal/tmp/deploy/images/rk3568-avod/cetca-avod-minimal-rk3568-avod.rootfs-20251128022227.wic: 5721MB
扇区大小 (逻辑/物理)：512B/512B
分区表：gpt
磁盘标志：

编号  起始点  结束点  大小    文件系统  名称       标志
 1    4194kB  8389kB  4194kB            uboot      msftdata
 2    8389kB  10.5MB  2097kB            uboot-env
 3    16.8MB  285MB   268MB   ext4      boot       旧版启动
 4    285MB   2433MB  2147MB  ext4      rootfs
...
```

而拆出来的 `boot` 分区镜像再用相同的方法查看，就会发现该标志位没有了。具体原因很有可能是 `wic` 镜像在开头储存了分区表来对各个分区进行描述和标记，显然拆出来的镜像没有这个头。并且 wks 的分区定义转换到 `parameter` 文件中似乎会丢失一些参数，因此**分开烧写时务必手动添加 bootable**。

## 11.3. 波特率问题

出现乱码一般是波特率不匹配。U-boot 和内核的波特率需要分别修改（通常通过打 patch 或在 defconfig 中配置）。注意第一次编译时需要将所有的补丁和配置文件打入，不然可能会出现各种问题。

## 11.4. 关于烧写地址与 miniLoader

嵌入式设备从上电开始，每一步的执行都是确定的——从哪个地址开始执行、执行什么，必须是事先定义好的。

对于 Rockchip 平台，上电后的启动链为：

```
BootROM (板载固件，固定地址) → miniLoader.bin (SPL，闭源) → U-boot → Kernel
```

`miniLoader.bin` 是 Rockchip 提供的闭源 SPL（Secondary Program Loader），负责初始化 DDR 并加载 U-boot。它如何知道 U-boot 烧在哪里？答案是**烧写工具在烧写时会将配置参数写入 eMMC 的固定区域**，miniLoader 上电后读取这些参数来定位 U-boot。

`parameter` 分区表的作用是告诉烧写工具"每个分区从哪个扇区开始、多大"。烧写工具读取 `parameter`，将配置写入 miniLoader 的配置区。因此 `parameter` 中的分区名必须与烧写工具的约定一致（如 U-boot 分区名必须是固定值，否则烧写工具无法识别）。

如果要手动烧写（不依赖烧写工具的自动化），就需要手动将烧写地址信息配置给 miniLoader，这通常很麻烦——所以尽量通过参数表 + 烧写工具的方式完成。

这也解释了为什么 `idblock` 分区不用 `--no-table` 加入 GPT 分区表：BootROM 只认固定物理地址，和分区表无关。

---

# 12. 附录：Yocto 编译命令速查

## 12.1. 常用 bitbake 命令

注意每一次打开新的终端，记得 `source` 环境初始化脚本来获得 `bitbake` 的运行环境。

```bash
# 构建完整镜像
bitbake <image-recipe>

# 仅下载源码（不编译）
bitbake --runall=fetch <image-recipe>

# 执行指定任务
bitbake -c <task> <recipe>

# 强制重新编译（忽略已有编译结果，重新执行该任务）
bitbake -c compile -f <recipe>

# 查看配方生效的所有环境变量
bitbake -e <recipe> | grep ^变量名

# 查看配方文件路径
bitbake -e <recipe> | grep ^FILE=

# 查看 WORKDIR
bitbake -e <recipe> | grep ^WORKDIR=

# 查看可用的镜像配方
bitbake-layers show-recipes "*-image-*"

# 解析所有配方，检查语法错误（不执行构建）
bitbake -p

# 清除编译结果（保留源码和下载的 tarball）
bitbake -c cleansstate <recipe>

# 获取当前配方的任务列表
bitbake <recipe> -c listtasks

# 生成任务依赖图（输出 .dot 文件，可转为图片查看）
bitbake <recipe> -g
```

## 12.2. 关键 flag 说明

| flag | 含义 | 典型场景 |
|:---|:---|:---|
| `-c <task>` | 执行指定任务 | `bitbake -c compile virtual/kernel` 只编译内核 |
| `-c <task> -f` | 强制执行任务（忽略已有标记，即使已完成也重做） | 修改源码后强制重编译 |
| `-e` | 查看配方的完整环境（所有变量展开后的值） | 排查变量为什么是某个值 |
| `--runall=fetch` | 只执行 fetch 及之前的所有任务 | 离线环境预下载所有源码 |
| `-g` | 生成任务依赖图（pndepends.dot / taskdepends.dot） | 分析任务执行顺序、排查依赖问题 |
| `-p` | 仅解析配方，不执行任何构建 | 快速检查语法错误 |
| `-c cleansstate` | 清除该配方的编译结果及 sstate 缓存（保留源码下载） | 强制从 do_fetch 开始重新构建 |
| `-c cleanall` | 清除所有内容，包括下载的源码 | 完全重来（谨慎使用） |

**依赖图方向说明**：`bitbake <recipe> -g` 生成的依赖图中，`task1 -> task2` 表示 task2 完成后才能执行 task1。例如 `do_image_ext4 -> do_image -> do_rootfs`，执行顺序是 `do_rootfs → do_image → do_image_ext4`。
