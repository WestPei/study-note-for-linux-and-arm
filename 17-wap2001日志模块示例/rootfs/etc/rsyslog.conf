# if you experience problems, check
# http://www.rsyslog.com/troubleshoot for assistance

# rsyslog v3: load input modules
# If you do not load inputs, nothing happens!
# You may need to set the module load path if modules are not found.
#
# Ported from debian's sysklogd.conf

$ModLoad immark   # provides --MARK-- message capability
$ModLoad imuxsock # provides support for local system logging (e.g. via logger command)
$ModLoad imklog   # kernel logging (formerly provided by rklogd)

#
# Set the default permissions
#
$FileOwner root
$FileGroup adm
$FileCreateMode 0640
$DirCreateMode 0755
$Umask 0022

$template record_format, "%$NOW% %TIMESTAMP:8:15% %syslogtag%%msg:::drop-last-lf%\n"
$ActionFileDefaultTemplate record_format

auth,authpriv.*                 /var/log/rsyslog/system/auth.log

*.*;auth,authpriv.none;\
		local0.none,local1.none,local2.none,local3.none,local4.none,local5.none,local6.none,local7.none \  
								-/var/log/rsyslog/system/syslog.log
								
cron.*                          /var/log/rsyslog/system/cron.log
# daemon.*                        -/var/log/rsyslog/system/daemon.log
kern.*                          -/var/log/rsyslog/system/kern.log
lpr.*                           -/var/log/rsyslog/system/lpr.log
mail.*                          -/var/log/rsyslog/system/mail.log
user.*                          -/var/log/rsyslog/system/user.log

#
# Logging for the mail system.  Split it up so that
# it is easy to write scripts to parse these files.
#
mail.info                       -/var/log/rsyslog/system/mail.info.log
mail.warn                       -/var/log/rsyslog/system/mail.warn.log
mail.err                        /var/log/rsyslog/system/mail.err.log

# Logging for INN news system
#
news.crit                       /var/log/rsyslog/system/news.crit.log
news.err                        /var/log/rsyslog/system/news.err.log
news.notice                     -/var/log/rsyslog/system/news.notice.log

#
# Some `catch-all' logfiles.
#
*.=debug;\
		auth,authpriv.none;\
		news.none;mail.none;local5.none                 -/var/log/rsyslog/system/debug.log
*.=info;*.=notice;*.=warn;\
		auth,authpriv.none;\
		cron,daemon.none;\
		mail,news.none;\
		local0.none,local1.none,local2.none,local3.none,local4.none,local5.none,local6.none,local7.none \  
								-/var/log/rsyslog/system/messages.log

#
# Emergencies are sent to everybody logged in.
#
*.emerg                         :omusrmsg:*

# Save boot messages also to boot.log
local0.*                                                /var/log/rsyslog/app/ccClient.log
local1.*                                                /var/log/rsyslog/app/HMClient.log
local2.*                                                /var/log/rsyslog/app/NMClient.log
local3.*                                                /var/log/rsyslog/app/logArchive.log
local4.*                                                /var/log/rsyslog/app/FTClient.log
local5.*                                                /var/log/rsyslog/state/runstate.log
local6.info                                             /var/log/rsyslog/platform/switch.log
local7.info                                             /var/log/rsyslog/platform/bsp.log

# Remote Logging (we use TCP for reliable delivery)
# An on-disk queue is created for this action. If the remote host is
# down, messages are spooled to disk and sent when it is up again.
#$WorkDirectory /var/spool/rsyslog # where to place spool files
#$ActionQueueFileName uniqName # unique name prefix for spool files
$ActionQueueMaxDiskSpace 10m   # 1gb space limit (use as much as possible)
#$ActionQueueSaveOnShutdown on # save messages to disk on shutdown
#$ActionQueueType LinkedList   # run asynchronously
#$ActionResumeRetryCount -1    # infinite retries if host is down
# remote host is: name/ip:port, e.g. 192.168.0.1:514, port optional
#*.* @@remote-host:514


# ######### Receiving Messages from Remote Hosts ##########
# TCP Syslog Server:
# provides TCP syslog reception and GSS-API (if compiled to support it)
#$ModLoad imtcp.so  # load module
#$InputTCPServerRun 514 # start up TCP listener at port 514

# UDP Syslog Server:
#$ModLoad imudp.so  # provides UDP syslog reception
#$UDPServerRun 514 # start a UDP syslog server at standard port 514

#
# Include all config files in /etc/rsyslog.d/
#
$IncludeConfig /etc/rsyslog.d/*.conf
