- [1. I2C设备-温度芯片](#1-i2c设备-温度芯片)
	- [1.1. 入手，查询芯片信息](#11-入手查询芯片信息)
	- [1.2. 驱动源码概览](#12-驱动源码概览)
	- [1.3. `lm92_probe()`](#13-lm92_probe)
	- [1.4. 两个`struct device`的`sysfs`分层解耦](#14-两个struct-device的sysfs分层解耦)
	- [1.5. hwmon子系统 属性注册](#15-hwmon子系统-属性注册)
	- [1.6. `probe`后续：`hwmon_dev`的创建](#16-probe后续hwmon_dev的创建)
	- [1.7. 驱动功能详解](#17-驱动功能详解)
- [2. max6635驱动修改(sysfs)](#2-max6635驱动修改sysfs)
	- [2.1. `temp_hyst_show`](#21-temp_hyst_show)
	- [2.2. `config`寄存器](#22-config寄存器)
	- [2.3. 将驱动编入内核](#23-将驱动编入内核)
	- [2.4. 驱动测试](#24-驱动测试)
- [3. 将MAX6635虚拟为字符设备](#3-将max6635虚拟为字符设备)
	- [3.1. 核心结构](#31-核心结构)
		- [3.1.1. data 结构体](#311-data-结构体)
		- [3.1.2. 数据传递的结构体](#312-数据传递的结构体)
	- [3.2. 字符设备注册](#32-字符设备注册)
	- [3.3. IO 操作](#33-io-操作)
		- [3.3.1. IO 操作编号](#331-io-操作编号)
	- [3.4. 驱动设计回顾](#34-驱动设计回顾)
		- [3.4.1. 不用全局变量](#341-不用全局变量)
		- [3.4.2. 私有数据](#342-私有数据)
		- [3.4.3. "找妈妈/找儿子"](#343-找妈妈找儿子)
		- [3.4.4. 功能的分层](#344-功能的分层)
		- [3.4.5. 理解 `cdev`](#345-理解-cdev)

---


# 1. I2C设备-温度芯片

I2C 总线设备算是驱动中较为简单的一类，从他入手。前辈本意是去学习 SoC 自带的 eeprom 驱动，可惜的是 rk3568 似乎不带 eeprom，只好增加难度，去查看温度芯片(这个总有了吧)。

---

## 1.1. 入手，查询芯片信息

如何找到温度芯片的信息呢？可以从设备树入手。当前项目使用的设备树为`rk3568-ok3568c.dts`，在其中查看`I2C`节点下的设备。芯片一般会有多个I2C控制器，我们在i2c2中找到了对应的芯片:

```c
&i2c2 {
        status = "okay";
        pinctrl-names = "default";
        pinctrl-0 = <&i2c2m1_xfer>;

	max6635: hwmon@48 {
		compatible = "national,max6635";
		reg = <0x48>;
		
		//硬件需要做更正，alert和critial中断线需要处于一个中断源，
		//因为一个中断源对应着一个时钟源，而一个普通监管设备一般只有一个时钟源
		//interrupt-names = "alert", "critical";
		//interrupts = <RK_PC2 IRQ_TYPE_LEVEL_LOW>, <RK_PA5 IRQ_TYPE_LEVEL_LOW>;
		#thermal-sensor-cells = <0>;
	};
};
```

 至于如何发现这个是温度芯片的，那就说来话长。因为在之前阅读bite源码，做温度检测时，读取的`sysfs文件中就有一个叫做`hwmon`的路径。

> `hwmon`是Linux内核中用于硬件监控的子系统(Hardware Monitoring)。它提供了一个统一的框架和接口，用于读取系统硬件传感器数据：
>
> * 温度(CPU、GPU、主板、硬盘等)
> * 电压(电源、核心电压等)
> * 风扇转速(CPU、机箱风扇)
> * 电流、功耗等

根据前辈的描述，这类温度芯片虽然是走的I2C总线，但是内核中对它们进行了抽象，而不是像其他I2C驱动一样，直接通过I2C来独写寄存器。(具体原因可能是温度芯片的寄存器交互逻辑过于复杂，涉及到数据转换和字符拼接，直接交互不太友好)。

先mark一下，等读到源码再去仔细探究。

---

## 1.2. 驱动源码概览

学习了设备-总线-驱动模型之后我们明白，ARM架构下驱动和设备是靠`compatible`与驱动的`id`来匹配的。所以我们直接在源码中查询`national,max6635`。

`compatible`的字段一般是`<厂商名, 芯片型号>`的格式。具体的匹配流程应该还会复杂一点，比如不同厂商的相同芯片之类的，这里先不深入讨论，总之这里我们找到了匹配上的驱动代码：

```
kernel/drivers/hwmon/lm92.c
```

直接来到代码的底部，看看都有什么：

```c
/*
 * Module and driver stuff
 */

static const struct i2c_device_id lm92_id[] = {
	{ "lm92", lm92 },
	{ "max6635", max6635 },
	{ }
};
MODULE_DEVICE_TABLE(i2c, lm92_id);

static struct i2c_driver lm92_driver = {
	.class		= I2C_CLASS_HWMON,
	.driver = {
		.name	= "lm92",
	},
	.probe_new	= lm92_probe,
	.id_table	= lm92_id,
	.detect		= lm92_detect,
	.address_list	= normal_i2c,
};

module_i2c_driver(lm92_driver);

MODULE_AUTHOR("Jean Delvare <jdelvare@suse.de>");
MODULE_DESCRIPTION("LM92/MAX6635 driver");
MODULE_LICENSE("GPL");
```

其中的核心就是这个`struct i2c_driver`，这个是内核I2C子系统中管理I2C驱动的核心结构体：

```c
struct i2c_driver {
	unsigned int class;

	/* Standard driver model interfaces */
	int (*probe)(struct i2c_client *client, const struct i2c_device_id *id);
	void (*remove)(struct i2c_client *client);

	/* New driver model interface to aid the seamless removal of the
	 * current probe()'s, more commonly unused than used second parameter.
	 */
	int (*probe_new)(struct i2c_client *client);

	struct device_driver driver;
	const struct i2c_device_id *id_table;

    /* Device detection callback for automatic device creation */
	int (*detect)(struct i2c_client *client, struct i2c_board_info *info);
	const unsigned short *address_list;
	struct list_head clients;
    
	u32 flags;
};
```

这里我只保留了一些我关注的成员，可以注意到这里有两个`probe()`，而这个函数也是驱动初始化的核心。这里为啥会有两个呢？根据资料，这是Linux内核在API迁移过程的过渡。

传统的`probe()`中，会多传入一个`const struct i2c_device_id **id*`参数，而在现代Linux的I2C驱动(使用设备树或ACPI枚举)根本不会使用这个参数。这是6.1内核的一个过渡，后续新版本内核最终会保留一个接口。

---

## 1.3. `lm92_probe()`

这里`lm92.c`中使用的就是新版的`probe`：

```c
static int lm92_probe(struct i2c_client *new_client)
{
	struct device *hwmon_dev;
	struct lm92_data *data;

	data = devm_kzalloc(&new_client->dev, sizeof(struct lm92_data),
			    GFP_KERNEL);
	if (!data)
		return -ENOMEM;

	data->client = new_client;
	mutex_init(&data->update_lock);

	/* Initialize the chipset */
	lm92_init_client(new_client);

	hwmon_dev = devm_hwmon_device_register_with_groups(&new_client->dev,
							   new_client->name,
							   data, lm92_groups);
	return PTR_ERR_OR_ZERO(hwmon_dev);
}
```

一来就遇到了问题，这里使用`devm_kzalloc()`为`lm92_data`结构体申请了内存。不过具体的细节我没太看明白。不过看这里的结构，`i2c_client`是I2C子系统中定义的客户端设备结构体，类似`platform_device`一样，在里面包含了`struct device`基类。

这个结构体是内核子系统的标准入口，但是每个驱动需要维护自己的私有数据，比如这里会有一个`struct lm92_data`，它包含了`i2c_client`。`i2c_client`结构体的初始化应该是交给内核来做，然后调用驱动的`probe`，再自己的代码中完成`lm92_data`的初始化。

`devm_kzalloc()`为`lm92_data`分配内存，并将它与`i2c_client->dev`的生命周期进行绑定。

在`probe()`中还有另一个`struct device`结构体：`hwmon_dev`。该结构体由`devm_hwmon_device_register_with_groups()`分配内存，并且其`parent`指向`i2c_client->dev`。然后在函数内部(很深的地方)，由`dev_set_drvdata`让`hwmon_dev->driver_data`指向了`lm92_data`。

这里关系可能有一点绕，但是我们只需知道这一条路径：

```
hwmon_dev->driver_data:lm92_data->client:i2c_client->dev
```

从设计上来讲，一般只需要从设备结构体找到私有数据，然后找到I2C的客户端就好。

---

## 1.4. 两个`struct device`的`sysfs`分层解耦

**这样我们的max6635芯片不就有两个`struct device`吗？具体的设备又是怎么管理的呢?**

> 的确一个温度芯片会在`sysfs`中对应两个`struct device`，从而产生两个目录：
>
> * I2C目录：/sys/bus/i2c/device/*，这是由`i2c_client->dev`产生
> * hwmon目录：/sys/class/hwmon/*，这是由`hwmon_dev`产生
>
> D老师告诉我这是为了分层解耦：
>
> 1. I2C层：负责底层总线通信(探测、地址、独写时序)。
> 2. hwmon层：负责提供标准化的温度传感器接口，不关心底层总线结构。
>
> 两者(`device`结构体)通过`parent`指针关联，在`sysfs`下的表现就是两个目录下只有对应的属性，I2C目录只关心设备树/总线相关的信息。而hwmon目录则是传感器的属性。

在了解设计目的后，我又有了新的困惑。我一直对`struct device`结构体的理解是，系统在解析设备树(嵌入式ARM)或者ACPI枚举(x86和ARM)时实时创建的(不过这个想法明显有问题，只是一直没有深究)，在这里却在`probe()`中新创建了`hwmon`子系统的设备结构体。另外就是这里为了解耦将`I2C`的底层实现与传感器的高层功能分开了，那我的驱动该怎么办呢？

让D老师来为我们一一解惑。

这两个`struct device`是谁创建的？`i2c_client->dev`毫无疑问是在解析设备树时发现了有这个节点，生成并注册的。然后到驱动匹配时触发了咱们驱动代码中的`probe()`，里面调用了`devm_hwmon_device_register_with_groups()`动态创建了`hwmon_dev`。它本质上就是一个软件功能抽象，可以认为是一个虚拟设备，由hwmon子系统进行管理(这里的`hwmon_dev`是`struct device`，hwmon子系统自己的结构体是`struct hwmon_device`，这个需要在注册函数内部去查看，结构与其他子系统类似，内部有一个`struct device`结构体指向`hwmon_dev`)。

那么既然又有了一个新的`dev`，它会在`sysfs`中有自己的路径，那么它的属性是谁决定的呢？答案还是`devm_hwmon_device_register_with_groups()`可以看到入参里有一个`lm92_groups`，这个结构体定义了暴露在`sysfs`目录下的属性。此外这个函数不仅创建了`hwmon_dev`，还将其注册了，可以说是一条龙服务。

```c
static struct attribute *lm92_attrs[] = {
	&sensor_dev_attr_temp1_input.dev_attr.attr,
	&sensor_dev_attr_temp1_crit.dev_attr.attr,
	&sensor_dev_attr_temp1_crit_hyst.dev_attr.attr,
	&sensor_dev_attr_temp1_min.dev_attr.attr,
	&dev_attr_temp1_min_hyst.attr,
	&sensor_dev_attr_temp1_max.dev_attr.attr,
	&sensor_dev_attr_temp1_max_hyst.dev_attr.attr,
	&dev_attr_alarms.attr,
	&sensor_dev_attr_temp1_crit_alarm.dev_attr.attr,
	&sensor_dev_attr_temp1_min_alarm.dev_attr.attr,
	&sensor_dev_attr_temp1_max_alarm.dev_attr.attr,
	NULL
};
ATTRIBUTE_GROUPS(lm92);
```

(这里我们还没有注意到这个结构体里的具体内容是什么，等一下和hwmon一起说)



所以到底是如何解耦的呢...在LM92的驱动内容中可以看到，功能函数中最后仍然调用的是I2C子系统提供的接口。

D老师告诉我，其实hwmon子系统并没有提供具体的接口函数，它只提供了一套数据结构，和注册接口。比如我们在`probe()`中调用的`devm_hwmon_device_register_with_groups()`就是hwmon子系统定义的。它可以为硬件设备模型抽象一个功能层(`hwmon_dev`)。**在我看来就是I2C子系统暴露在`sysfs`中的数据不是人看的，通过hwmon将寄存器值转换成人看得懂的属性暴露在`sysfs`中。这个就是hwmon子系统最大的功能。**

在驱动代码中我们可以看到，hwmon子系统的用处就是：

* `probe`阶段初始化一个`hwmon_dev`，与`i2c_client->dev`绑定(`hwmon_dev->parent`指向`i2c_client->dev`)

* 提供宏创建`hwmon`设备的属性(`lm92_attrs`，暴露在`sysfs`中)，并绑定属性修改函数(`show`，`store`)。

第一个部分源码就在`probe`中，感兴趣的可以去看看。

第二个部分主要是：

```c
/* hwmon子系统提供的宏,创建属性并绑定独写属性的函数 */
static SENSOR_DEVICE_ATTR_RO(temp1_input, temp, t_input);
static SENSOR_DEVICE_ATTR_RW(temp1_crit, temp, t_crit);
static SENSOR_DEVICE_ATTR_RW(temp1_crit_hyst, temp_hyst, t_crit);
static SENSOR_DEVICE_ATTR_RW(temp1_min, temp, t_min);
static DEVICE_ATTR_RO(temp1_min_hyst);
static SENSOR_DEVICE_ATTR_RW(temp1_max, temp, t_max);
static SENSOR_DEVICE_ATTR_RO(temp1_max_hyst, temp_hyst, t_max);
static DEVICE_ATTR_RO(alarms);
static SENSOR_DEVICE_ATTR_RO(temp1_crit_alarm, alarm, 2);
static SENSOR_DEVICE_ATTR_RO(temp1_min_alarm, alarm, 0);
static SENSOR_DEVICE_ATTR_RO(temp1_max_alarm, alarm, 1);

/* 将上述注册的属性放入结构体，probe时注册到子系统中 */
static struct attribute *lm92_attrs[] = {
	&sensor_dev_attr_temp1_input.dev_attr.attr,
	&sensor_dev_attr_temp1_crit.dev_attr.attr,
	&sensor_dev_attr_temp1_crit_hyst.dev_attr.attr,
	&sensor_dev_attr_temp1_min.dev_attr.attr,
	&dev_attr_temp1_min_hyst.attr,
	&sensor_dev_attr_temp1_max.dev_attr.attr,
	&sensor_dev_attr_temp1_max_hyst.dev_attr.attr,
	&dev_attr_alarms.attr,
	&sensor_dev_attr_temp1_crit_alarm.dev_attr.attr,
	&sensor_dev_attr_temp1_min_alarm.dev_attr.attr,
	&sensor_dev_attr_temp1_max_alarm.dev_attr.attr,
	NULL
};
ATTRIBUTE_GROUPS(lm92);
```

宏定义在`include/linux/hwmon-sysfs.h`中。可以看到hwmon子系统只是提供了抽象层的框架：`device`结构体的创建与绑定，`attr`属性创建与绑定。**但是这些属性具体有哪些，如何去独写修改属性都是由驱动自己去实现的，只是命名规范需要遵循hwmon子系统**。这个很好去看，去查看两个核心的`attr`结构体定义：`struct sensor_device_attribute()`(hwmon子系统定义)与`struct device_attribute`(这个是sysfs的标准定义)。

这样就联系起来了。用户肯定是希望在`sysfs`中看到的是一个直观的数据，而不是一个二进制寄存器值。所以Linux内核设计了hwmon子系统来在内部进行转换。这个框架很灵活，硬件设备要在`sysfs`中暴露哪些属性自己决定。并且hwmon也不规定总线类型，不管你是I2C还是SPI，都无所谓。因为与硬件交互的代码由驱动自己写。比如在`temp_show()`中，如果芯片是I2C，那就调用I2C子系统的接口；如果是SPI，那就调用SPI子系统的接口。

而I2C子系统会提供与硬件的通信接口，这个我们在代码中可以看到，比如`i2c_smbus_write_word_swapped()`。

---

## 1.5. hwmon子系统 属性注册

上面虽然给了hwmon子系统在驱动中的角色，这里我们还是来看一看代码吧。

常规的属性注册是：

```c
static DEVICE_ATTR_RO(alarms);
```

```c
/* include/linux/device.h */
#define DEVICE_ATTR_RO(_name) \
	struct device_attribute dev_attr_##_name = __ATTR_RO(_name)
```

```c
/* include/linux/sysfs.h */
#define __ATTR_RO(_name) {						\
	.attr	= { .name = __stringify(_name), .mode = 0444 },		\
	.show	= _name##_show,						\
}
```

而hwmon子系统规定了自己的属性注册：

```c
static SENSOR_DEVICE_ATTR_RO(temp1_input, temp, t_input);
```

```c
/* include/linux/hwmon-sysfs.h */
#define SENSOR_DEVICE_ATTR_RO(_name, _func, _index)		\
	SENSOR_DEVICE_ATTR(_name, 0444, _func##_show, NULL, _index)

#define SENSOR_DEVICE_ATTR(_name, _mode, _show, _store, _index)	\
struct sensor_device_attribute sensor_dev_attr_##_name		\
	= SENSOR_ATTR(_name, _mode, _show, _store, _index)

#define SENSOR_ATTR(_name, _mode, _show, _store, _index)	\
	{ .dev_attr = __ATTR(_name, _mode, _show, _store),	\
	  .index = _index }
```

```c
/* include/linux/sysfs.h */
#define __ATTR(_name, _mode, _show, _store) {				\
	.attr = {.name = __stringify(_name),				\
		 .mode = VERIFY_OCTAL_PERMISSIONS(_mode) },		\
	.show	= _show,						\
	.store	= _store,						\
}
```

hwmon在`struct device_attribute`下创建了一个子类`struct sensor_device_attribute`：

```c
struct sensor_device_attribute{
	struct device_attribute dev_attr;
	int index;
};
```

多了一个`index`，这里温度传感器的值首先会被保存到`lm92_data->temp`中，然后还要与`sysfs`中的文件对应，所有有一个索引值。

---

## 1.6. `probe`后续：`hwmon_dev`的创建

这里跟一下`probe`中，hwmon子系统是如何创建`struct hwmon_dev`的。

在`lm92_probe()`中：

```c
static int lm92_probe(struct i2c_client *new_client)
{
    hwmon_dev = devm_hwmon_device_register_with_groups(&new_client->dev,
							   new_client->name,
							   data, lm92_groups);
}
```

现在来看看`devm_hwmon_device_register_with_groups()`的函数细节：

```c
struct device *
devm_hwmon_device_register_with_groups(struct device *dev, const char *name,
				       void *drvdata,
				       const struct attribute_group **groups)
{
	struct device **ptr, *hwdev;

	if (!dev)
		return ERR_PTR(-EINVAL);

	ptr = devres_alloc(devm_hwmon_release, sizeof(*ptr), GFP_KERNEL);
	if (!ptr)
		return ERR_PTR(-ENOMEM);

	hwdev = hwmon_device_register_with_groups(dev, name, drvdata, groups);
	if (IS_ERR(hwdev))
		goto error;

	*ptr = hwdev;
	devres_add(dev, ptr);
	return hwdev;

error:
	devres_free(ptr);
	return hwdev;
}
EXPORT_SYMBOL_GPL(devm_hwmon_device_register_with_groups);
```

第一个困惑肯定就是`devres_alloc()`这是个啥？对应的可以看到还有`devres_add()`和`devres_free()`？这一系列`devres_*()`函数是干什么用的？

```c
ptr = devres_alloc(devm_hwmon_release, sizeof(*ptr), GFP_KERNEL);

#define devres_alloc(release, size, gfp) \
	__devres_alloc_node(release, size, gfp, NUMA_NO_NODE, #release)
```

```c
void *__devres_alloc_node(dr_release_t release, size_t size, gfp_t gfp, int nid,
			  const char *name)
{
	struct devres *dr;

	dr = alloc_dr(release, size, gfp | __GFP_ZERO, nid);
	if (unlikely(!dr))
		return NULL;
	set_node_dbginfo(&dr->node, name, size);
	return dr->data;
}
EXPORT_SYMBOL_GPL(__devres_alloc_node);
```

```c
struct devres {
	struct devres_node		node;
	/*
	 * Some archs want to perform DMA into kmalloc caches
	 * and need a guaranteed alignment larger than
	 * the alignment of a 64-bit integer.
	 * Thus we use ARCH_KMALLOC_MINALIGN here and get exactly the same
	 * buffer alignment as if it was allocated by plain kmalloc().
	 */
	u8 __aligned(ARCH_KMALLOC_MINALIGN) data[];
};
```

可以看到`ptr`本身的类型是`struct device **`，其值是一个地址，该地址的值为`struct device *`，而这里利用`devres_alloc()`分配了一个`devres->data`。说人话就是让`ptr`指向了用于**资源管理的结构体内部元素**。

> `devres`是一个内核用于管理设备资源的机制，在为设备分配资源时会记录一个小账本，这个账本保存了对应资源的一些信息，包括如何释放该资源。
>
> 很直观地我们看到`devm_hwmon_device_register_with_groups`中调用`devres_alloc`时传入了释放资源的函数`devm_hwmon_release`。

然后我们可以看到如果`hwdev`创建成功，那么就会让`*ptr = hwdev`，这里实际上就是

```c
ptr = (struct devres *)dr->data
*ptr = hwdev 等价于 *(dr->data)=hwdev 
就是将创建好的hwdev纳入资源管理中
```

这里还进行了一步操作：

```c
devres_add(dev, ptr);
```

```c
void devres_add(struct device *dev, void *res)
{
	struct devres *dr = container_of(res, struct devres, data);
	unsigned long flags;

	spin_lock_irqsave(&dev->devres_lock, flags);
	add_dr(dev, &dr->node);
	spin_unlock_irqrestore(&dev->devres_lock, flags);
}
EXPORT_SYMBOL_GPL(devres_add);
```

```c
static void add_dr(struct device *dev, struct devres_node *node)
{
	devres_log(dev, node, "ADD");
	BUG_ON(!list_empty(&node->entry));
	list_add_tail(&node->entry, &dev->devres_head);
}
```

最后其实就是将`hwdev`对应的资源管理结构体加入到其父节点`i2c_client->dev->devres_head`，**根本作用就是将`hwdev`的声明周期与其父设备绑定**，当`i2c_client`被销毁时，会遍历其`devres_head`，然后销毁对应的资源。

从逻辑上也很好理解，`hwmon`子系统的设备实例本身就是依附于父节点的(这里就是温度芯片的设备树实例，I2C设备)，不可能单独存在，其生命周期也应该与之匹配。

由于`devres`先于资源结构体创建，如果资源结构体`hwdev`创建失败了我们也需要对应销毁这个`devres`，所以这里的`ptr`**才会是指针的指针`struct device **`**，确保`ptr`指向`devres`(指向的其实是内部的`data`)。这样在`hwdev`创建失败后，进入`error`分支时，才可以通过`devres_free`来清理内存。不然就会内存泄漏了。

```c
void devres_free(void *res)
{
	if (res) {
		struct devres *dr = container_of(res, struct devres, data);

		BUG_ON(!list_empty(&dr->node.entry));
		kfree(dr);
	}
}
EXPORT_SYMBOL_GPL(devres_free);
```

而`ptr`本身作为函数内部的局部变量，函数执行完毕后就从内存清理掉了。



这样`devm_hwmon_device_register_with_groups`的整体流程我们就清楚了，它主要是构建了资源管理结构`devres`将`hwmondev`的声明周期于父节点进行绑定，并且确保不会出现内存泄漏。而`hwdev`的创建则放在了`hwmon_device_register_with_groups`中。

---

现在进入`hwmon_device_register_with_groups`内部来看一看：

```c
struct device *
hwmon_device_register_with_groups(struct device *dev, const char *name,
				  void *drvdata,
				  const struct attribute_group **groups)
{
	if (!name)
		return ERR_PTR(-EINVAL);

	return __hwmon_device_register(dev, name, drvdata, NULL, groups);
}
EXPORT_SYMBOL_GPL(hwmon_device_register_with_groups);
```

在`hwmon_device_register_with_groups`中只是对`name`做了判断，设备不能没有名字！主要的逻辑放在了`__hwmon_device_register`中：

```c
// drivers/hwmon/hwmon.c

static struct device *
__hwmon_device_register(struct device *dev, const char *name, void *drvdata,
			const struct hwmon_chip_info *chip,
			const struct attribute_group **groups);
```

源码较长，这里就不贴了，可以直接去内核源码对应目录查看。

在流程之外我对函数内部局部变量的声明周期有一些疑问，这里主要是`hdev`和`hwdev`。这里捋一下

> 函数内部通过`kzalloc()`为`struct hwmon_device *hwdev`实例在堆空间上分配了内存，该内存生命周期不归这个函数管了，需要单独使用`free`或者`unregister`来销毁。
>
> 而`struct *hdev`作为一个函数内部的局部变量，它指向的是`hwdev->dev`，也就是堆空间中结构体中的元素。函数返回时`hdev`这个指针会被销毁，但是该指针指向的地址并未销毁，能够被正常传递出去。

下面继续看代码，这里又看到了一个新东西：`ida_alloc`与`hwmon_ida`：

```c
static DEFINE_IDA(hwmon_ida);
#define DEFINE_IDA(name)	struct ida name = IDA_INIT(name)
```

```c
static inline int ida_alloc(struct ida *ida, gfp_t gfp)
{
	return ida_alloc_range(ida, 0, ~0, gfp);
}
```

从函数内容来看，就是分配了一个ID号，但是有啥用呢？D老师告诉我它是hwmon子系统的设备实例编号，具体来说就是在`sysfs`中我们看到的`hwmon_dev`对应的设备目录就会叫做`hwmon%d`。

hwmon子系统自己维护了一个位表，用来分配和记录这些编号(ida的内部逻辑，我没有深究)。`ida_alloc_range`分配了ID后，会在设备名字后添加这个编号：

```c
id = ida_alloc(&hwmon_ida, GFP_KERNEL);

dev_set_name(hdev, HWMON_ID_FORMAT, id);
```

`dev_set_name`会将hwmon标准格式名与ID拼接在一起，给到`hdev->kobj-name`中，最后在`sysfs`中看到的目录名就是这个。

```c
#define HWMON_ID_PREFIX "hwmon"
#define HWMON_ID_FORMAT HWMON_ID_PREFIX "%d"
```

由于在`hwmon_device_register_with_groups()`中定义：

```c
return __hwmon_device_register(dev, name, drvdata, NULL, groups);
```

`__hwmon_device_register`的入参`chip`为`NULL`，所以源码中直接

```c
hdev->groups = groups;
```

将`lm92_groups`赋给了`hdev`。直觉上讲也没有任何问题。



`hwdev`会从`dev`(设备树节点生成的`struct device`)中获取一个可用的`label`(如果有的话，没有就跳过了)：

```c
if (dev && device_property_present(dev, "label")) {
    err = device_property_read_string(dev, "label", &label);
    if (err < 0)
        goto free_hwmon;

    hwdev->label = kstrdup(label, GFP_KERNEL);
    if (hwdev->label == NULL) {
        err = -ENOMEM;
        goto free_hwmon;
    }
}
```

进行`hdev`和`hwdev`的初始化，将传入的指针传给它们：

```c
    hwdev->name = name;
    hdev->class = &hwmon_class;
    hdev->parent = dev;
    while (tdev && !tdev->of_node)
        tdev = tdev->parent;
    hdev->of_node = tdev ? tdev->of_node : NULL;
    hwdev->chip = chip;
    dev_set_drvdata(hdev, drvdata);
    dev_set_name(hdev, HWMON_ID_FORMAT, id);
    err = device_register(hdev);
    if (err) {
        put_device(hdev);
        goto ida_remove;
    }
```

中间唯一有一点疑问的是这个：

```c
    while (tdev && !tdev->of_node)
        tdev = tdev->parent;
    hdev->of_node = tdev ? tdev->of_node : NULL;
```

这看起来像是沿着`i2c_client->dev`向上寻找一个具有有效设备树节点的设备(非hwmon子系统这种虚拟设备)。至于为啥需要找到这个设备树节点，D老师告诉我是为了：

* 通过设备树进行电源管理（如 `runtime PM` 关联）
* 在 `/sys/class/hwmon/hwmonX/` 下提供 `of_node` 符号链接，方便用户空间定位设备树节点。
* 热管理子系统（`hwmon_thermal`）可以通过 `hdev->of_node` 查找对应的 thermal zone 配置。

反正也没太看懂，先mark吧。不过这里这个`hwmon_thermal`在接下来的代码中就有：

```c
	if (hdev->of_node && chip && chip->ops->read &&
	    chip->info[0]->type == hwmon_chip &&
	    (chip->info[0]->config[0] & HWMON_C_REGISTER_TZ)) {
		err = hwmon_thermal_register_sensors(hdev);
		if (err) {
			device_unregister(hdev);
			/*
			 * Don't worry about hwdev; hwmon_dev_release(), called
			 * from device_unregister(), will free it.
			 */
			goto ida_remove;
		}
	}
```

D老师告诉我，这个是hwmon框架与Linux热管理(Thermal)子系统的集成，它会将温度传感器注册为`thermal zone`的设备。这里的`hwmon_thermal_register_sensors()`会读取设备树中`thermal zone`的相关配置，并将传感器与其关联。这样传感器就可以作为系统热管理的温度源，用于触发CPU降频，控制风扇转速等。

因为咱们这里的hwmon子系统注册使用的方式入参中`chip`始终是`NULL`，所以这段代码不会被触发。

不过hwmon子系统是有其他的注册方式的，对于新的驱动，使用类似`devm_hwmon_device_register_with_info`来注册时就会传入`chip`信息。

---

## 1.7. 驱动功能详解

网上可以查阅到MAX6635芯片的数据手册。[MAX6635数据手册](https://www.analog.com/cn/products/max6635.html)

根据手册再对应驱动可以发现，这里的驱动更多是针对lm92的，只是兼容MAX6635，因此很多6635的高级功能并没有实现。只是做了基础的功能。比如：

* 读取当前温度和阈值：`temp_show`
* 设置高温报警/低温报警阈值以及过热关断阈值：`temp_store`
* 设置迟滞(这个值决定了报警后要高于/低于阈值多少后才会消除报警)：`temp_hyst_store`
* 读取过温以及低温报警实际消除温度：`temp_hyst_show`，`temp_min_hyst_show`
* 以及读取报警标志位：`alarms_show`，`alarm_show`
* 初始化时推出关断模式(启用温度传感器)：`lm92_init_client`中设置`LM92_REG_CONFIG`的`bit0`为0。

更复杂的故障队列，配置寄存器等功能该驱动中其实都并没有实现。

到这里我们对lm92的驱动源码应该有了一个比较全面的了解。

---

# 2. max6635驱动修改(sysfs)

上一章我们阅读了max6635的驱动源码，本质上是lm92的兼容驱动。现在作为练手，我们将max6635中的一些没有实现的功能添加到驱动中。
新建一个驱动源码文件

```
drivers/hwmon/max6635.c
```

然后我们基于`lm92.c`驱动来进行修改，将对应的结构体名都修改为`max6635_*`。
原来的`lm92_detect`用于读取传感器中的厂商名，然后找到具体的传感器型号，原来的驱动是兼容max6635的，但是对于lm92有一些特殊的处理。所以`detect`是在匹配lm92。这里我们不需要了。

把基础的结构体和各种信息修改完毕之后，我们就可以考虑修改和增加功能了。

这里需要注意的是lm92与max6635的寄存器命名有些微区别。这里单独为max6635写驱动，就将命名与数据手册对应了，放弃使用原来的`crit`，`max`和`min`而是使用`max`，`high`和`low`。

---

## 2.1. `temp_hyst_show`

原来的驱动对于`hyst`的处理：
	* 对`high`和`max`，其迟滞温度为`* - hyst`
	* 对`low`，其迟滞温度为`* + hys`

所以写了两个函数：`temp_hyst_show`与`temp_min_hyst_show`。实际上我们可以通过判断`attr->index == t_low`来判断我们是要获取哪一个迟滞温度。从而返回对应的输出：

```c
static ssize_t temp_hyst_show(struct device *dev,
			      struct device_attribute *devattr, char *buf)
{
	struct sensor_device_attribute *attr = to_sensor_dev_attr(devattr);
	struct max6635_data *data = max6635_update_device(dev);

    /* 
    * hyst_temp for high and low is different. it decides on the index
    */
    return (attr->index == t_low) ? 
                sprintf(buf, "%d\n", TEMP_FROM_REG(data->temp[attr->index])
                    + TEMP_FROM_REG(data->temp[t_hyst])) : 
                sprintf(buf, "%d\n", TEMP_FROM_REG(data->temp[attr->index])
                    - TEMP_FROM_REG(data->temp[t_hyst]));
}
```

需要注意的是`temp_low_hyst`的结构体定义也需要修改：

```c
// static DEVICE_ATTR_RO(temp1_low_hyst);
static SENSOR_DEVICE_ATTR_RW(temp1_low_hyst, temp_hyst, t_low);

static struct attribute *max6635_attrs[] = {
	...
	// &dev_attr_temp1_low_hyst.attr,
	&sensor_dev_attr_temp1_low_hyst.dev_attr.attr,
}
```

这是最后暴露在`sysfs`中的属性。需要对应着修改。

---

## 2.2. `config`寄存器

这是max6635自己的额外功能，lm92驱动中完全不涉及的。在原本的驱动中，只有在`lm92_init_client()`中使用过该寄存器，将关断位置`0`打开传感器。这个也是lm92本身的功能，max6635兼容。

lm92的`config`寄存器另有他用，我们需要针对max6635的`config`寄存器功能进行驱动修改。

手册中`config`寄存器的定义如下：

| D7 | D6 | D5 | D4 | D3 | D2 | D1 | D0 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0  | 0 | SMB Timeout Disable | Fault Queue Enable | ALERT Polarity | OVERT Polarity | Comparator or Interrupt | Shutdown |

* Power-on default = 0h.
* D0: 0 = normal operation; 1 = shutdown.
* D1: 0 = comparator mode; 1 = interrupt mode.
* D2 to D3: 0 = active low; 1 = active high.
* D5: 0 = normal SMBus operation; 1 = full I2C compatibility.
* D7 to D6: Reserved locations, always write zeros.

`config`寄存器的每一位都有单独的定义。那么我们可以为每一位单独创建属性结构体分别进行控制。`D6 to D7`是默认为0的扩展位，我们需要保证它们清零。

另外我们需要在`data`中缓存`config`的值，避免频繁的调用`i2c`协议接口。
```c
/* Client data (each client gets its own) */
struct max6635_data {
	struct i2c_client *client;
	struct mutex update_lock;
	bool valid; /* false until following fields are valid */
	unsigned long last_updated; /* in jiffies */

	/* registers values */
	s16 temp[t_num_regs];	/* index with enum temp_index */
	u8 config;	/* store config reg */
};
```

```c
/* add config attr */
static SENSOR_DEVICE_ATTR_RW(config_shutdown, config, bit_shutdown);
static SENSOR_DEVICE_ATTR_RW(config_comparator_interrupt, config, bit_comparator_interrupt);
static SENSOR_DEVICE_ATTR_RW(config_overt_polarity, config, bit_overt_polarity);
static SENSOR_DEVICE_ATTR_RW(config_alert_polarity, config, bit_alert_polarity);
static SENSOR_DEVICE_ATTR_RW(config_fault_queue, config, bit_fault_queue);
static SENSOR_DEVICE_ATTR_RW(config_SMB_timeout, config, bit_SMB_timeout);
```

```c
static struct attribute *max6635_attrs[] = {
	/* config reg attr */
	&sensor_dev_attr_config_shutdown.dev_attr.attr,
	&sensor_dev_attr_config_comparator_interrupt.dev_attr.attr,
	&sensor_dev_attr_config_overt_polarity.dev_attr.attr,
	&sensor_dev_attr_config_alert_polarity.dev_attr.attr,
	&sensor_dev_attr_config_fault_queue.dev_attr.attr,
	&sensor_dev_attr_config_SMB_timeout.dev_attr.attr,
	NULL
};
ATTRIBUTE_GROUPS(max6635)
```

这是新定义的`config`相关的属性。另外我们可以借鉴`temp`寄存器中低3位的方法，通过移位的方式来控制读取对应的寄存器位，所以这里定义一个枚举变量：

```c
enum config_index {
	bit_shutdown,
	bit_comparator_interrupt,
	bit_overt_polarity,
	bit_alert_polarity,
	bit_fault_queue,
	bit_SMB_timeout
};
```

根据定义，我们需要同步实现`config_show()`与`config_store()`，实现对`config`寄存器特定功能位的读取和写入。我们的实现可以直接参考`temp_*()`的写法，注意这里仍然是使用`i2c_smbus`协议：

```c
static ssize_t config_show(struct device *dev, struct device_attribute *devattr,
				char *buf)
{
	struct sensor_device_attribute *attr = to_sensor_dev_attr(devattr);
	struct max6635_data *data = max6635_update_device(dev); /* get update data */
	u8 config = data->config;
	int val = (config >> (attr->index)) & 1; /* get the specific bit */
	return sprintf(buf, "%d\n", val);
}
```

```c
static ssize_t config_store(struct device *dev, struct device_attribute *devattr,
				const char *buf, size_t count)
{
	struct sensor_device_attribute *attr = to_sensor_dev_attr(devattr);
	struct max6635_data *data = dev_get_drvdata(dev);
	struct i2c_client *client = data->client;
	int nr = attr->index;
	unsigned long val;
	u8 config;
	int err;

	err = kstrtol(buf, 10, &val);
	if (err)
		return err;
	if (val != 0 && val != 1)
		return -EINVAL;

	mutex_lock(&data->update_lock);
	/* get config reg first */
	config = i2c_smbus_read_byte_data(client, MAX6635_REG_CONFIG);
	if (config < 0) {
		mutex_unlock(&data->update_lock);
		return -EIO;
	}
	/* modify specific bit */
	if (val)
		config |= (1 << nr);
	else
		config &= ~(1 << nr);
	/* D6 and D7 always set zero */
	config &= 0x3F;

	data->config = config; /* update data */
	i2c_smbus_write_byte_data(client, MAX6635_REG_CONFIG, config);
	mutex_unlock(&data->update_lock);
	return count;
}
```

其中的核心就是通过`nr = data->index`来判断当前需要独写的`config`位，然后通过位操作进行配置。

可以发现获取寄存器信息都是通过`max6635_update_device()`实现的，而原来的`update`函数只实现了温度的获取，没有得到`config`寄存器的值，所以同步更新该函数：

```c
static struct max6635_data *max6635_update_device(struct device *dev)
{
	struct max6635_data *data = dev_get_drvdata(dev);
	struct i2c_client *client = data->client;
	int i;

	mutex_lock(&data->update_lock);

	if (time_after(jiffies, data->last_updated + HZ) ||
	    !data->valid) {
		dev_dbg(&client->dev, "Updating max6635 data\n");
		for (i = 0; i < t_num_regs; i++) {
			data->temp[i] =
				i2c_smbus_read_word_swapped(client, regs[i]);
		}
		/* get initial config */
		data->config = i2c_smbus_read_byte_data(client, MAX6635_REG_CONFIG);

		data->last_updated = jiffies;
		data->valid = true;
	}

	mutex_unlock(&data->update_lock);

	return data;
}
```

---

## 2.3. 将驱动编入内核

咱们自己写了一个新的驱动源码，那必然是要有一个对应的`Kconfig`项对应，这里咱们写的是`MAX6635`的驱动，那么一般源码名字也为`max6635.c`。然后我们在驱动源码的目录中(这里是`/drivers/hwmon/`)找到这个`Kconfig`文件，在里面参考`lm92`添加一个`SENSORS_MAX6635`：

```c
config SENSORS_MAX6635
	tristate "Maxim MAX6635 sensor chip"
	depends on I2C
	help
	  If you say yes here you get support for Maxim MAX6635 sensor chips.

	  This driver can also be built as a module. If so, the module
	  will be called max6635.
```

这样保证了你在`make menuconfig`中可以找到这么一个选项来配置。现在光有了选型还不行，还得修改咱们的`Makefile`保证选取了对应选项后，要将源码编入内核。

同样是直接参考`lm92`的写法：

```makefile
obj-$(CONFIG_SENSORS_MAX6635)	+= max6635.o
```

然后我们可以通过`menuconfig`也可以通过直接修改我们使用的`defconfig`来添加`MAX6635`的配置。不过由于它和`LM92`会抢同一个设备，所以记得注释掉`LM92`的配置项，保证只有一个驱动被编入内核。

---

## 2.4. 驱动测试

遇到了一些硬件的问题，一块板子是怎么读都是`0x00`，前辈说是被干扰了。第二块板子是I2C通信全部`timeout`，硬件有问题。
直到第三块板子终于正常了，能够正常的独写。

如何去测试我的驱动没有问题呢？比较简单的方法就是我们通过`sysfs`提供的文件独写修改属性，然后用Linux下的I2C调试工具来检测对应地址的寄存器值。对具体的功能逻辑还可以测试一下输出。

这里讲一下常用的I2C的调试工具吧。

主要的I2C调试工具都包含在了`i2c-tools`中：
1. 总线探测与设备发现
`i2cdetect`：扫描I2C总线并列出检测到的设备。
   * `i2cdetect -l`：列出系统所有可用的I2C总线。
   * `i2cdetect -y 2`：扫描总线2上的所有设备地址(`-y`表示跳过确认)。其结果分为三类：`--`表示该地址无设备应答；`UU`表示该地址被内核驱动占用，用户空间程序无法直接访问；`16进制数`表示在此地址探测到设备。
2. 寄存器查看
`i2cdump`：批量导出并查看设备的所有寄存器值，用于全面了解设备状态。
   * `i2cdump -y 2 0x48 b`：以字节为单位(`b`)，导出总线2上`0x48`地址设备的全部寄存器。
   * 其他常用模式：`w`(16位字读取)。
`i2cget`：读取设备中单个寄存器的值，适合定点检查。
   * `i2cget -y 2 0x48 0x00 w`：从总线上`0x48`地址设备的`0x00`寄存器读取2个字节的数据。
3. 寄存器写入
`i2cset`：向设备写入单个寄存器的值，可以用来测试设备功能。
   * `i2cset -y 2 0x48 0x01 0x00 b`：向`0x01`寄存器写入单字节数据`0x00`。
   * ==警告==：在设备被内核驱动占用时，切勿使用`i2cset`或带`-f`参数的写命令。这可能导致驱动状态混乱、数据丢失甚至系统崩溃！

另外这里介绍几个通用的参数
* `-y`：禁用交互模式。默认情况下，很多的`i2c-tools`命令执行前要求确认。在脚本中使用该工具时，必须加上该参数来禁用交互。
* `-f`：强制访问。该参数允许用户在设备被内核驱动占用时强制从用户空间访问它。通常用于调制**只读**来确认硬件状态。因为该操作会**绕过驱动**，**所以使用该参数必须清楚地知道自己在做什么**。
* `-r`：后跟寄存器范围。主要是供`i2cdump`使用，可以限制导出的寄存器地址范围。例如，`i2cdump -y -r 0x00-0x10 2 0x48 b`这样只会导出`0x00`到`0x10`的寄存器。
* `-a`：允许工具访问非标准I2C地址，但是一般不推荐使用(就当没有)。

# 3. 将MAX6635虚拟为字符设备

学习了 [SMI总线驱动源码](/11-SMI总线驱动源码.md)，了解到了基本的字符设备从注册到 `ops` 的操作函数大概是什么样的。为了练习编写字符驱动函数，我选择将 MAX6635 虚拟成一个字符设备。

在原来的基础上改动似乎有点麻烦了。这里我们先整理一下思路。

## 3.1. 核心结构

编写驱动前我认为需要理顺设备在设备树上的节点关系。
**原来的 MAX6635 驱动中**，内核首先找到了 I2C 控制器/适配器的节点，并且进行了初始化，得到了 `i2c_client` 这么一个从设备结构体，里面包含有 I2C从设备(MAX6635)对应的 `struct device`，这个对应的是设备的物理实体。在 `probe` 中创建了一个私有数据结构 `max6635_data`，他负责联系起 I2C 物理设备与 hwmon子系统。在驱动中实际是通过物理实体 `i2c_client` 作为入参，然后找到我们的私有数据 `data` 和 `hwmon` 结构体来进行具体的操作。私有数据 `data` 中会缓存传感器的寄存器值，而 hwmon 结构体不负责交互，它的作用就是将传感器的各个寄存器抽象成属性暴露到 `sysfs`中。然后用户可以直接用 `echo` 和 `cat` 来交互。

类比一下，字符驱动的 `cdev` 其实与 `hwmon` 子系统类似，它的功能就是在内核中注册主次设备号，然后在 `/dev` 下创建对应的设备文件，我们就可以在用户态下利用 IO 操作等方式与设备进行交互了。只是它更加底层一点，提供的都是一些编程接口(比如在 bite 中调用)。

我认为关键就是掌握一点，数据结构的关系。我们使用的数据结构要与内核管理层进行关联，保证驱动中的数据结构能够与设备的生命周期同步。(这个一般意味着我们的私有数据 `*_data` 通过 `devm_kzalloc` 从内核中分配内存，并且与对应的硬件设备 `struct device` 生命周期进行绑定)。在我看来这个 `data` 就是整个驱动的核心结构体，它负责连接所有的结构体和驱动的核心数据(记得带锁)。

当然可能某些情况中不叫做 `*_data`，但是总会有这么一个结构的。这里 `smi_bus` 就是一个比较特殊的例子，`cdev_info` 与 `sys_info` 分别有一些这个功能。我认为是可以重构一下代码，用一个大的结构体完成该工作。

### 3.1.1. data 结构体

这里我们创建了 `max6635_char_data` 的结构体：

```c
struct max6635_char_data {
	struct i2c_client *client;
	struct cdev cdev;
	struct device *device;
	struct mutex lock;
	struct class *class;
};
```

这是驱动的一个比较核心的私有结构体。与原本的 `max6635_data` 是很像的。


### 3.1.2. 数据传递的结构体

那么首先我们就需要想清楚我们如果要将传感器抽象为一个字符设备，**我们该怎么组织数据结构**？我们当然就可以借鉴 `smi_bus` 这个驱动了，里面的 `smi_data`。

字符驱动中有一个很有意思的地方就是 IO 操作，它会将数据在内核空间与用户空间中传递，在内核中可以通过 `copy_to_user` 和 `copy_from_user` 来获得用户空间传递进来的数据，一般可以以结构体的形式来组织。那么**这个结构体就很关键**，可以把它认为是一个消息数据帧，帧结构由我们自己定义。

在 `smi_bus` 中由于 交换芯片所需要的信息稍微复杂一些，所以 `smi_data` 中的成员比较多，包含有总线 ID，设备编号，PHY 地址等等。

而我们的传感器更简单。很容易想到寄存器，寄存器地址和值就是关键的数据，那么我们就可以定义我们的消息结构就是：

```c
/* 用于直接读取寄存器 */
struct max6635_reg_rw {
    uint8_t reg;                    /* 寄存器地址 */
    uint16_t val;                   /* 写入值/返回值 */
};
```

这是最简单粗暴的，那么我们如果想要根据功能，只暴露上层抽象给用户态呢？比如这里直接读寄存器我还得去查传感器的数据手册，不如我直接在驱动中定义好，比如 读温度，设置阈值温度，设置配置寄存器等功能。这些功能大多很简单，有些时候甚至可以不用结构体。

```c
/* 用于 GET_LIMIT/SET_LIMIT 结构 */
struct max6635_limit {
    enum max6635_limit_type type;   /* 阈值类型 */
    int32_t value;                  /* 温度值 毫摄氏度 */
};
```

## 3.2. 字符设备注册

这一步一般都是在 `probe` 中完成。

字符设备驱动的注册是有标准流程的，在此之前我们首先需要与原来的驱动一样，使用 `devm_kzalloc()` 来为私有数据结构分配内核空间，并与设备示例生命周期绑定。并完成这个私有数据基本的初始化流程。

注意到这个私有结构体中 `cdev` 是一个完整结构体而不是指针，这是因为 `cdev` 相关的操作要求 `cdev` 已经创建，而不是像之前都是需要申请分配。

接下来就是字符驱动注册的标准流程了：

* `alloc_chrdev_region()`
* `cdev_init()`
* `cdev_add()`
* `class_create()`或者其他分配 `class` 的方式
* `device_create()`

另外对应的就是 `remove` 中的流程：

* `device_destroy()`
* `class_destroy()`
* `cdev_del()`
* `unregister_chrdev_region()`

可以看到这个顺序是正好相反的，因为这里的结构是嵌套的，注册时从外向里，注销时就得反着来。

## 3.3. IO 操作

字符设备的操作也是有标准的， 一般是使用 `file_operations` 进行注册。

```c
static const struct file_operations max6635_char_ops = {
	.owner				= THIS_MODULE,
	.open				= max6635_open,
	.release			= max6635_release,
	.read				= max6635_read,
	.unlocked_ioctl		= max6635_ioctl,
};
```

这里我还没有太了解字符设备，但是通俗来讲就是 Linux 内核将字符设备模拟成了一个文件，我们可以通过操作文件的方式来操作设备。那么文件操作肯定就具备打开、关闭还有正常的读写操作了。
这里 `open` 和 `release` 对应的就是打开和关闭，而其他的大部分功能其实都是可以通过 `ioctl` 来实现。
注意这里的 `owner` 成员。它总是指向字符设备对应的驱动模块。**在现代 Linux 中，`open` 和 `release` 操作会自动进行引用计数的增减**，无需手动操作(这一点还需要验证)。

### 3.3.1. IO 操作编号

这里讲一讲 IO 操作的写法，注意看

```c
long (*unlocked_ioctl) (struct file *, unsigned int, unsigned long);
```

这是文件操作中的定义，我们实际的 IO 操作定义会是这样的：

```c
static long max6635_ioctl(struct file *mfilp, unsigned int cmd, unsigned long arg)
```

三个参数分别是文件实例指针 `filep`，IO 指令和一个数据指针。
通过这三个参数可以进行对应的 IO 操作，这个数据指针就是用来在内核空间和用户空间之间传递数据用的。用户空间读时，内核将数据写到 `arg` 中，用户空间写时内核从 `arg` 中读取数据。

这里的这个指令号我们可以自己去硬编码，只需要我们内部能够识别就可以了，前辈就是这么干的：

```c
#define SMI_SET_REG                     0x7001
#define SMI_GET_REG                     0x7002
#define SMI_LOCK_REG                    0x7003
#define SMI_UNLOCK_REG                  0x7004
```

不过内核中提供了一套标准的生成 IO 指令的宏
```c
include/uapi/asm-generic/ioctl.h
```

它们大概长这样：
```c
_IOX(MAGIC_NUM, NUM, size)
```

IO操作对内核和用户空间来说是对同一个数据结构的相反操作：

* IOR：用户空间读，内核写
* IOW：用户空间写，内核读
* IOWR：用户空间和内核都涉及读写操作

`MAGIC_NUM`是一个魔法数字(ASCII字符)，用于区分不同的设备驱动，一般是一个与驱动相关的，比如这里我们就用`M`。这个魔法数字在调试时可以看到 IO 指令来自哪一个驱动。这个魔法数字一般有一些默认设置，需要时可以查阅。

`NUM`是只该命令在本驱动内的编号，一般从 `1` 开始。

`size`只该次传递数据的大小，可以直接用 `sizeof()` 自动计算并编码。

我们使用这个宏有很多好处：

* 内核自动检查缓冲区大小，内核会根据编码情况来检查传递的指针对应内存是否足够大。
* 帮助 `compat_ioctl` 处理，32位程序运行在64位内核上时，`compat_ioctl` 可以自动转换数据结构。
* 自我文档化：看命令定义就能够知道数据方向和数据大小。
* 避免冲突：相同的序号，也可以通过魔数的不同来进行区分。

## 3.4. 驱动设计回顾

我们在驱动代码编写时，通过阅读原本的一些成熟的驱动和前辈的代码，可以注意到一些比较关键的设计原则。

### 3.4.1. 不用全局变量

如果是一个单一设备的特定驱动，倒是无所谓驱动变量该如何组织。但是我们写驱动一般还是会考虑带多个设备的情况，那我们就需要保证每一个设备的驱动私有数据结构不会相互干扰，所以尽量不要使用所谓的全局指针或者变量。

驱动都会有自己的一个私有数据结构 `<driver>_data`，它会保存一些关键的数据结构指针，与 `dev` 等数据结构相关联。保证内核能够通过设备节点或者设备文件找到对应的驱动数据和操作函数。

这个数据结构我们只需要在源码中声明，只有在 `probe` 中才会使用 `devm_kzalloc` 来为其在内核堆上分配内存，并且与对应设备的 `struct device` 生命周期进行绑定。**这是内核驱动的标准做法**！

### 3.4.2. 私有数据

这个私有数据也有一些必须要包含的内容：

* 指向设备的指针(一般是`struct device` 的子类，比如 I2C 从设备就是 `struct i2c_client`)。
* 互斥锁(保证驱动操作是原子的)。
* 以及一些私有数据，这个根据情况而定。比如字符驱动中一般就会内嵌一个 `struct cdev`，这样保证我们可以通过 `inode->i_cdev` 的 `container_of()` 来扎到我们要使用的驱动私有数据。

### 3.4.3. "找妈妈/找儿子"

我认为内核驱动的一个理解难点就在于，有这么多的数据结构，它们该如何相互找到。这是很重要的！因为从不同的入口看到的结构体可能是不同的，但是最后我们要进行的操作可能是一样的。所以我们要保证这些结构体都能够相互联系从而找到彼此。

在"设备-总线-驱动"模型中就已经玩过一次这个游戏了，但是现在加入字符驱动后变得更加复杂了一些。不过可以稍稍解耦来看这个问题。**不过核心其实都是我们的私有数据结构 `<driver>_data`。**

我们从 `probe` 开始看起吧，在这之前内核已经注册了设备节点，并且将其放在了对应的总线子系统下，它具有的基本结构就是：`struct <bus>_dev->struct device->struct device_node`

这里就将设备的硬件(设备树节点 `device_node`)与内核中的设备实例(总线设备 `<bus>_dev`)连接起来了。

然后在 `probe` 中 ，首先就是使用 `devm_kzalloc()` 创建一个驱动私有数据结构 `<driver>_data`，并且将其与 `device` 设备结构的声明周期绑定。不过需要注意的是，**此时 `data` 与 `device` 并没有实际的指针相连**，仅仅是保证了生命周期的一致。

在完成互斥锁的初始化后。我们可能就需要手动将这些结构体进行连接了。在字符驱动中，我们希望 `data` 直接挂载在 `device` 下，这里我们使用了 I2C 子系统提供的接口 `i2c_set_clientdata()`。函数内部其实就是将 `i2c_client->device->driver_data` 指向了 `data`。

然后我们又将 `data` 中的 `i2c_client *`指针指向了它，这样就是双箭头了。原来的 max6635 驱动还不太一样，因为它使用了hwmon子系统，所以多套了一层，大概长这样：

```c
struct device hw_dev->driver_data = data
hw_dec->parent = i2c_client->device
```

因为原来的驱动和我们打交道的是 hwmon 子系统，所以私有数据结构就是挂在 `hwmon_dev` 下的。

现在回到我们的字符驱动。我们会去注册私有数据中的 `cdev` (这里提一嘴，我们的 `cdev` 是直接在私有数据中声明的，不是指针哦，在 `probe` 时直接就分配空间了，这里只是将其注册到内核中，让内核能够找到) 这里的 `alloc_chrdev_region`，`cdev_init`，`cdev_add`就是将 `cdev` 与申请的主次设备号联系在一起，并且注册到内核中。`device_create` 干的事情会更复杂一些，它触发用户态的 `udev`，在用户态下创建字符设备文件，并且还会返回一个 `struct device`，它应该就是我们虚拟出的字符设备对应的实例。这个 `device->parent` 会指向我们的 `<bus>_dev->device`。

标准设计中我们会将这个 `device_create()` 创建的字符设备对应的结构放入 `data` 中，从而将它与设备的硬件结构实例 `struct device`联系起来。通过 `data` 来实现双向的查找。这样一来，我们所有需要使用的结构体都通过 `data` 串了起来。

简单来说，内核提供的接口能够帮助我们把一些复杂的操作简化，但是中间涉及到我们自己的私有数据时，还需要自己将他们连接起来。

### 3.4.4. 功能的分层

这里提这个功能的分层，其实是为了解答我关于一个驱动怎么要创建那么多个 `struct device` 的结构体的疑问。在原始驱动中我就有这个问题，hwmon 子系统就会自己创建一个 `struct device` 作为 sysfs 属性目录。

而这里我们的字符设备驱动其实也是类似的。首先是每个设备都有的硬件实例 `strcut device`，这个本来也不归驱动管，由内核在初始化时或者热插拔时自动注册的。每一个 `struct device` 结构体中都包含一个 `kobject`，已经知道了它是 sysfs 的核心，对应着一个具体的目录。所以设备的硬件结构体 `device` 对应的其实是 sysfs 下的设备树结构拓扑，它是硬件视角。

而我们字符设备还创建了一个 `cdev` 以及 一个与主次设备号绑定的 `struct device`。这两个都是设备的功能层实现。`cdev` 中虽然也有一个 `kobject` 但是D老师告诉我它并没有被挂到 sysfs 目录中，**`cdev` 结构体只存在于内核的 `cdev_map` 哈希表和 `kobject` 内部层次中，用来管理引用计数和对象生命周期的**(其实我也没太看懂)。所以在 sysfs 中一个字符设备这里只会有两个目录，一个就是硬件视角下的，另一个就是通过 `device_create()` 创建的对应 `class` 下的目录，这个目录下的内容是字符设备功能相关的。

### 3.4.5. 理解 `cdev`

`cdev` 实际上管理着字符设备的功能，而 `device_create` 创建出的这个 `device` 结构体其实就是 `cdev`对应的在 sysfs 下暴露的目录(可以这么理解，他俩是一层)，在其中可以看到字符设备的主次设备号。

cdev 的内核角色就是将设备号(`dev_t`)与一组 `file_operations` 绑定，并注册到内核的 VFS 层，让用户空间可以通过设备节点(`/dev/xxx`)进行 `open/read/write` 等系统调用。

它本身就是一个“标准接口”：不论你的硬件是 I2C 传感器、PCI 采集卡，还是纯软件生成的随机数，只要你想用字符设备的方式去访问，都需要用到 `cdev`。

而 `device_create()`在 sysfs 下创建接口这件事情对 `cdev` 来说不是必须的，但是在现代 Linux 驱动中我们总是这样做：

* 自动生成 `/dev` 下的设备节点，不用手操
* 在 `/sys/class/` 下创建目录，里面自动包含设备号和 `uevent` 文件
* 运行用户通过 `device_create_file` 来添加自定义属性，用户可以通过 sysfs 读写设备参数。

这样做就是为了用户空间使用更方便。和 `cdev` 本身没啥太大关系。其实我觉得一个很大的作用是会帮我们在 `/dev` 下创建字符设备文件，不用咱们手动用 `mknod`。

现在先这样粗浅地理解一下吧。
