# SPI 总线

基本参考资料：
* [SPI通信协议讲解](https://zhuanlan.zhihu.com/p/150121520)

* [SPI原理超详细讲解](https://blog.csdn.net/as480133937/article/details/105764119)

---

# SPI 总线简介

### SPI 概览

Serial Peripheral interface 是 SPI 全称，即串行外围设备接口。最早是由 Motorola 在 `MC68HCXX` 处理器上定义的。SPI 主要应用在 EEPROM，FLASH ，实时时钟，AD 转换器以及数字信号处理与数字信号解码之间，

SPI 是一种**高速的，全双工，同步的通信总线**。并且在芯片的管脚上只有四根线。

---

### SPI 优势

1. 支持全双工通信
2. 通信协议简单
3. 数据传输速率快

---

### SPI 缺点

SPI 总线没有指定的流控制，没有应答机制确认是否收到数据，所以跟 I2C 总线比较，SPI 在数据可靠性上稍弱。

---

## SPI 总线特点

### 主从模式(Master-Slave)

SPI 规定了两个 SPI 设备之间通信必须由主设备(Master)来控制此设备(Slave)。主设备可以一对多，通过提供时钟和对从设备进行**片选**(Slave Select)来实现。SPI 协议本身规定从设备的时钟由主设备通过`SCK`引脚提供。从设备自己不具备时钟控制和生产能力，没有时钟从设备也无法工作。

---

### 同步(Synchronous)

Master 设备会根据将要交换的数据来产生相应的时钟脉冲(Clock Pulse)，时钟脉冲组成了时钟信号(Clock Signal) ，时钟信号通过时钟极性 (CPOL) 和 时钟相位 (CPHA) 控制着两个 SPI 设备间何时数据交换以及何时对接收到的数据进行采样，来保证数据在两个设备之间是同步传输的。

---

### 数据交换(Data Exchanges)

SPI 设备间的数据传输之所以又被称为数据交换，是因为 SPI 协议规定一个 SPI 设备不能在数据通信过程中仅仅只充当一个 "发送者(Transmitter)" 或者 "接收者(Receiver)"。在每个 Clock 周期内，SPI 设备都会发送并接收一个 bit 大小的数据(不管主设备好还是从设备)，相当于该设备有一个 bit 大小的数据被交换了。一个 Slave 设备要想能够接收到 Master 发过来的控制信号，必须在此之前能够被 Master 设备进行访问 (Access)。所以，Master 设备必须首先通过 SS/CS pin 对 Slave 设备进行片选, 把想要访问的 Slave 设备选上。 在数据传输的过程中，每次接收到的数据必须在下一次数据传输之前被采样。如果之前接收到的数据没有被读取，那么这些已经接收完成的数据将有可能会被丢弃，导致 SPI 物理模块最终失效。因此，在程序中一般都会在 SPI 传输完数据后，去读取 SPI 设备里的数据, 即使这些数据(Dummy Data)在我们的程序里是无用的(虽然发送后紧接着的读取是无意义的，但仍然需要从寄存器中读出来)。

---

### SPI 只分主从

SPI 没有读和写的区分，因为每一次 SPI 的执行都是主从设备之间的一次数据交换。简单来说就是发数据一定会受到数据，而收数据前也得发出数据。如果要实现读操作，只需要发送空数据就好；而写操作就忽略读到的数据。

SPI的数据传输本质上就是主从设备的缓存区数据通过移位寄存器实现交换的过程。

---

## SPI 工作机制

* `SSPBUF`：泛指 SPI 设备内部缓冲区，一般是以 FIFO 的形式(先进先出)，保存传输过程种的临时数据。
* `SSPSR`：泛指 SPI 设备里面的移位寄存器，其作用是根据设置好的数据位宽把数据移入或者移出 `SSPBUF`。
* `Controller`：泛指 SPI 设备里面的控制寄存器，通过配置它们来设置 SPI 总线的传输模式。
而一般我们只需要针对 SPI 设备的四个PIN脚进行编程即可控制整个 SPI 设备间的通信：

* `SCK`：主要的作用是 Master (主)设备往 Slave (从)设备传输时钟信号, 控制数据交换的时机以及速率；
* `SS/CS`：用于 Master (主)设备片选 Slave (从)设备，使被选中的 Slave (从)设备能够被 Master (主)设备所访问；
* `SDO/MOSI`：在 Master (主)上面也被称为 Tx-Channel，作为数据的出口，主要用于 SPI 设备发送数据；
* `SDI/MISO`：在 Master(主) 上面也被称为 Rx-Channel，作为数据的入口，主要用于 SPI 设备接收数据。

---

### 极性和相位

SPI 的极性`Polarity`和相位`Phase`，最常见的写法就是`CPOL`与`CPHA`，不过也会有一些其他的写法：

* `CKPOL`(Clock Polarity) = `CPOL` = `POL` = `Polarity` = 极性
* `CKPHA`(Clock Phase) = `CPHA` = `PHA` = `Phase` = 相位

这里给出 SPI 配置时钟极性和相位的方法，这决定了数据传输时的细节：

`CPOL`为极性：
```
CPOL = 1 => 空闲时为高电平，即第一个跳变沿为下降沿。

CPOL = 0 => 空闲时为低电平，即第一个跳变沿为上升沿。
```

`CPHA`为相位：
```
CPHA = 0 => 从第一个跳变沿开始采样。

CPHA = 1 => 从第二个跳变沿开始采样。
```

可以发现`CPOL`与`CPHA`本质上不决定采样究竟是上升沿还是下降沿，而是配合起来决定。

比如`CPOL = 0` ，`CPHA = 0`时，意味着空闲时低电平，从第一个跳变开始采样，因此采样时为上升沿采样。因此这里数据采样在上升沿，而下降沿就会发送数据。

从设备的SPI模式可能在出厂时就固定了，但是主设备一般可以配置自己的SPI模式。**在通信时要保证主从设备的模式相同**。

---

### 简单粗暴但是有效

上述基本就是SPI总线协议的全部了。非常的简单粗暴，不规定最大传输速率。没有地址方案，也没有通信应答机制，也不规定流控制规则。

只需要硬件连接正确，传输时将对应的片选线拉低，就可以通信了，一次一个字节。

而实际的通信控制都依赖SPI设备自己去实现了。SPI就是如此粗暴，它甚至都不关心物理接口的电气特性，比如信号的标准电压。我猜测可能是因为它是沿触发的，所以也用不着关心。

而关于模式的细节，这里可以补充一下。首先是明确，SPI的通信其实是主从设备的**数据交换**。

`SSPBUF`与 CPU 以及 SPI 控制器总线打交道，从 CPU 处拿去/输出数据。`SSPSR`作为移位寄存器，从`SSPBUF`中获取/输出数据，然后再将数据一位一位地推出去。

这里主要就是涉及到`SSPSR`的传输细节。实际上通信时就是主从设备的`SSPSR`在打交道。`SSPSR`会将数据从高位(MSB)一位一位地从`MOSI`推出去，然后从`MISO`中一位一位地取出数据放到`SSPSR`的地位`LSB`。从设备逻辑一样，只是`MISO`与`MOSI`反过来了。这样完成一个传输周期后，主从设备中的`SSPSR`中的数据就被交换了。

而`CPOL`与`CPHA`配置的模式则是保证主从设备在传输时能够读取稳定的数据。比如`CPOL = 0` ，`CPHA = 0`时，空闲时为低电平，
* 对**接收方**而言，在上升沿时进行采样，也就是说设备会在此时对总线上的数据进行锁存。比如主设备就采样`MISO`，从设备采样`MOSI`。对方必须保证在采样时数据的稳定。
* 对**发送方**而言，在下降沿将一位数据发送到总线上，并且知道下一次下降沿前保证数据稳定。这样就可以保证在两次下降沿之间的上升沿时，对方能够采样到正确的数据。
* **实现全双工的关键**在于，在同一个时钟周期里，上升沿用于双方的"读取"操作，下降沿用于双方的"写入"操作。主机和从机都在上升沿采样对方送来的数据，并在下降沿把下一位要发送的数据放到总线上。这种精妙的"读-写"交替机制正是SPI能高效全双工的基石。

另外这里还有两个需要关注的定义：`Bus-Width`与`Channel-Width`：

* `Bus-Width`(总线宽度)：定义 CPU/系统总线访问 SPI模块内部寄存器时的数据宽度。例如，设置为`Byte`时，每次通过总线向`SSPBUF`写入一个字节的数据。
* `Channel-Width`(通道宽度)：定义 SPI 控制器与外部设备(从机)之间，单次通过 MOSI/MISO 传输的数据宽度，这个通常也是`1 Byte`左右。

最终要的是保证`Bus-Width`大于或等于`Channel-Width`，也就是说数据准备的速度得比数据发送的速度快，至少也得一样。这样防止数据传输出现不完整或者断流。

---



