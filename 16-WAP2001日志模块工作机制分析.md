# 日志模块整体工作机制分析

> 目标平台：Rockchip RK3568 + Linux 6.1 + systemd
> 分析日期：2026-07-23

---

## 目录

- [日志模块整体工作机制分析](#日志模块整体工作机制分析)
  - [目录](#目录)
  - [1. 总体架构概览](#1-总体架构概览)
  - [2. 日志产生层（数据源）](#2-日志产生层数据源)
    - [facility 分配规则](#facility-分配规则)
    - [日志级别控制](#日志级别控制)
  - [3. 日志去重层（共享内存 + 信号量）](#3-日志去重层共享内存--信号量)
    - [实现原理](#实现原理)
    - [并发控制](#并发控制)
  - [4. 日志收集/存储层（rsyslog + journald）](#4-日志收集存储层rsyslog--journald)
    - [4.1 rsyslog](#41-rsyslog)
    - [4.2 journald](#42-journald)
    - [4.3 journal 存储位置重定向](#43-journal-存储位置重定向)
  - [5. 日志轮转层（三层轮转策略）](#5-日志轮转层三层轮转策略)
    - [第一层：BSP 文件夹轮转](#第一层bsp-文件夹轮转)
    - [第二层：logrotate 定时轮转](#第二层logrotate-定时轮转)
    - [第三层：打包归档线程（持续运行）](#第三层打包归档线程持续运行)
  - [6. 首次启动日志归档（log-manage.sh）](#6-首次启动日志归档log-managesh)
  - [7. 日期管理（防RTC掉电回退）](#7-日期管理防rtc掉电回退)
    - [启动时恢复（`set_valid_date()`）](#启动时恢复set_valid_date)
    - [运行时持久化（`date_refresh_task` 线程）](#运行时持久化date_refresh_task-线程)
  - [8. 完整数据流图](#8-完整数据流图)
  - [9. 文件清单](#9-文件清单)
    - [源代码文件](#源代码文件)
    - [根文件系统配置](#根文件系统配置)
  - [10. 关键设计要点总结](#10-关键设计要点总结)

---

## 1. 总体架构概览

整个日志系统分为五个层次，按数据流向自下而上组织：

```
应用/BSP 代码 (日志产生)
       │
       ▼
  共享内存去重 (跨进程重复日志过滤)
       │
       ▼
  syslog() → rsyslogd / journald (收集与路由)
       │
       ▼
  多级轮转：logrotate (cron) + log_package 线程 + log-manage.sh
       │
       ▼
  持久化存储：/mnt/log/ + /var/log/ 各子目录
```

---

## 2. 日志产生层（数据源）

系统提供五种日志写入路径：

| 路径 | API | 输出目标 | 用途 |
|------|-----|---------|------|
| **BSP 直写** | `bsplog_record()` | `/mnt/log/bsplog/bsp_runlog` | BSP 底层硬件日志（GPIO、Watchdog、LED、离散量等） |
| **syslog 路由** | `syslog_record()` | 通过 rsyslog 分发到多目录 | 应用层、平台层通用日志 |
| **运行状态宏** | `syslog_runstate()` | `/var/log/rsyslog/state/runstate.log` | 设备运行状态快照（独立 facility: `LOG_LOCAL5`） |
| **故障日志** | `faultlog_record()` | `/var/log/rsyslog/state/bsp_fault.log` | 故障诊断用结构化日志（带时间戳和模块名） |
| **告警历史** | `add_alarm_log_entry()` | `/var/log/alarm_history` | 滑动窗口保留最近100行告警 |

### facility 分配规则

`syslog_record()` 中所有日志统一走 `LOG_LOCAL7`，路由规则定义在 `/etc/rsyslog.conf`：

| Facility | 路由目标 | 说明 |
|----------|---------|------|
| 标准 syslog（auth/cron/kern/mail/user 等） | `/var/log/rsyslog/system/` 下对应文件 | 系统标准日志 |
| `local0` ~ `local4` | `/var/log/rsyslog/app/` 下各客户端日志 | 应用客户端（ccClient, HMClient, NMClient, logArchive, FTClient） |
| `local5` | `/var/log/rsyslog/state/runstate.log` | 运行状态日志 |
| `local6.info` | `/var/log/rsyslog/platform/switch.log` | 交换机日志 |
| `local7.info` | `/var/log/rsyslog/platform/bsp.log` | BSP平台日志 |

### 日志级别控制

- **BSP 直写**（`bsplog.h:89-90`）：
  - 控制台打印阈值：`WARN` 及以上
  - 文件写入阈值：`INFO` 及以上
- **journald**（`journald.conf:37`）：`MaxLevelStore=info`，不存储 debug 级别

---

## 3. 日志去重层（共享内存 + 信号量）

`syslog_record()` 在 `bsplog.c:354-409` 实现了一套跨进程的相同日志去重机制。

### 实现原理

```
shmget(key=2234) → 共享内存 (存储上一条日志内容)
         ↓
  当前日志与上条比较 (strncmp)
         ├── 不同 → 立即 syslog() 写入
         │         若 repeat_times > 0，先输出 "......above log msg repeat N times......"
         ├── 相同 → repeat_times++，抑制本次输出
         └── 共享内存不存在(首次) → 退化为直接 syslog() 无过滤
```

### 并发控制

- **共享内存**：System V 共享内存，key = `2234`，大小 = `sizeof(log_info)`
- **互斥信号量**：POSIX 命名信号量 `log_mutex`（`O_RDWR | O_CREAT, 0664`）
- **故障日志信号量**：`faultlog_mutex`，保护 `bsp_fault.log` 的写入

> 这层去重是**多进程安全**的。`start.c` 进程启动后，其他 BSP 子进程也可能调用 `syslog_record()`，共享内存 + 信号量确保跨进程互斥。

---

## 4. 日志收集/存储层（rsyslog + journald）

### 4.1 rsyslog

`/etc/rsyslog.conf` 作为核心日志路由引擎：

- **输入模块**：`imuxsock`（本地 Unix socket）、`imklog`（内核日志）、`immark`（标记消息）
- **权限控制**：`$FileOwner root`、`$FileGroup adm`、`$FileCreateMode 0640`、`$DirCreateMode 0755`
- **自定义格式**：`"%$NOW% %TIMESTAMP:8:15% %syslogtag%%msg:::drop-last-lf%\n"`
- **路由规则**：按 facility + level 组合分发到 4 个目录

```
/var/log/rsyslog/
├── system/
│   ├── auth.log
│   ├── syslog.log
│   ├── cron.log
│   ├── kern.log
│   ├── messages.log
│   └── debug.log
├── app/
│   ├── ccClient.log
│   ├── HMClient.log
│   ├── NMClient.log
│   ├── logArchive.log
│   └── FTClient.log
├── platform/
│   ├── switch.log
│   └── bsp.log
└── state/
    ├── runstate.log
    └── bsp_fault.log
```

- 带 `-` 前缀的文件名表示**异步写入**（不阻塞 rsyslog）
- `*.emerg` 会广播给所有登录用户（`:omusrmsg:*`）

### 4.2 journald

`/etc/systemd/journald.conf` 关键配置：

| 参数 | 值 | 说明 |
|------|----|------|
| `Storage` | `auto` | 自动选择存储位置 |
| `Compress` | `yes` | 启用压缩 |
| `SystemMaxUse` | `512M` | 日志总大小上限 |
| `SystemMaxFileSize` | `8M` | 单个日志文件上限 |
| `SystemMaxFiles` | `64` | 最多保留文件数 |
| `ForwardToSyslog` | `yes` | 转发给 rsyslog（形成双写） |
| `ForwardToConsole` | `no` | 不输出到控制台 |
| `MaxLevelStore` | `info` | 只存储 info 及以上 |

### 4.3 journal 存储位置重定向

在 `bsplog_init()` 中（`bsplog.c:263-276`），若定义了 `INCLUDE_JOURNAL_LOG` 宏（`bsplog.h:63` 已定义）：

```c
mkdir -p /mnt/log/bsplog/journal                      // 在 BSP 日志目录下创建 journal 子目录
ln -s /mnt/log/bsplog/journal /var/log/journal         // 软链接 /var/log/journal → BSP 目录
journalctl --flush                                     // 刷写内存中的日志到磁盘
```

> 目的：将 journald 持久化存储也纳入 `/mnt/log/`（可能是独立持久化分区），统一管理。

---

## 5. 日志轮转层（三层轮转策略）

### 第一层：BSP 文件夹轮转

**触发时机**：每次 `bsplog_init()` 调用（即进程启动时）

**执行函数**：`update_log_folders()`（`bsplog.c:67-193`）

**流程**：
1. 扫描 `/mnt/log/` 下所有 `bsplog-N` 目录，记录最大/最小编号
2. 将当前 `bsplog/` 目录重命名为 `bsplog-{max+1}`
3. 若 `bsplog-N` 数量超过 `LOG_SAVE_MAX=10`，删除编号最小的目录
4. 创建新的空 `bsplog/` 目录

```
/mnt/log/
├── bsplog/          ← 当前写入目录
├── bsplog-10/       ← 上次轮转的历史
├── bsplog-9/
├── ...
└── bsplog-1/        ← 最旧（达到上限后被删除）
```

### 第二层：logrotate 定时轮转

**触发时机**：cron 每小时执行 `logrotate /etc/logrotate.conf`

**核心规则**（`/etc/logrotate.d/customizedsetting`）：

```
/var/log/rsyslog/*/*.log {
    missingok          # 文件不存在不报错
    compress           # 压缩旧日志为 .gz
    copytruncate       # 复制后截断（不关闭文件句柄，避免丢日志）
    rotate 10          # 保留10个轮转文件
    size 8M            # 单文件超过8MB触发
    dateext            # 使用日期作为后缀
    dateformat -%Y%m%d_%s  # 日期格式
    notifempty         # 空文件不轮转
    create 644 root root   # 轮转后创建新文件
    postrotate
        /usr/bin/pkill -HUP rsyslogd   # 通知 rsyslog 重新打开日志文件
    endscript
}
```

### 第三层：打包归档线程（持续运行）

**初始化**：`log_package_init()` 创建两个守护线程

| 线程 | 线程名 | 职责 | 运行周期 |
|------|--------|------|---------|
| 监控线程 | `log_monitor` | 用 `du` 命令检查5个目录大小 | 每10秒 |
| 打包线程 | `log_package` | `tar -czf` 压缩归档超限目录 | 事件驱动 |

**监控目录及阈值**：

| 目录 | 阈值 | 定义常量 |
|------|------|---------|
| `/var/log/rsyslog/system` | 128MB | `DIR_MAXSIZE` |
| `/var/log/rsyslog/platform` | 128MB | `DIR_MAXSIZE` |
| `/var/log/rsyslog/state` | 128MB | `DIR_MAXSIZE` |
| `/var/log/journal` | 512MB | `JOURNAL_MAXSIZE` |
| `/var/log/coredump` | 128MB | `DIR_MAXSIZE` |

**线程同步机制**（生产者-消费者模型）：

```
sem_monitor(初始值=1) / sem_package(初始值=0)

monitor:                    package:
  sem_wait(sem_monitor)       sem_wait(sem_package)
  ├── 检查目录大小             ├── tar -czf 打包
  ├── 若超限:                  ├── 清理源文件
  │   set archive_flag         │   - journal: fuser检测后rm
  │   sem_post(sem_package)    │   - coredump: rm -rf
  └── 若未超限:                │   - 其他: rm *.log*.gz
      sem_post(sem_monitor)    └── sem_post(sem_monitor)
```

**归档文件命名**：`/var/log/log-archive/{航班号}-{目录名}-{时间戳}.tar.gz`

> 另外支持**航班变更触发**：当外部设置 `flight_info.change_flag=1` 时，monitor 线程会触发所有监控目录归档（使用上一航班名 `past_name`）。

---

## 6. 首次启动日志归档（log-manage.sh）

**触发条件**（`start.c:99-105`）：

```c
if (access("/tmp/proc_started", F_OK) != 0)  // 标记文件不存在 → 首次启动
{
    bsplog_archive();                          // → system("/home/root/log-manage.sh")
    system("touch /tmp/proc_started");         // 创建标记，防止下次重启重复执行
}
```

**log-manage.sh 工作流程**：

1. **打包**：`tar -zcvf /var/log/archive/wap_<timestamp>.tar.gz /var/log --exclude=archive --exclude=lost+found`
2. **清理**（打包成功后）：
   - 删除 `rsyslog/` 下所有 `*.gz` 文件
   - 删除 `journal/` 下所有 `system@*` 文件
   - 清空 `coredump/` 目录
   - 删除 `access.log`、`lighttpd.error.log`、`install.log`
3. **数量/容量控制**：
   - 归档文件数 > 50 → 删除最旧的 10 个（`file_max/5`）
   - 归档总大小 > 512MB → 同样触发删除

---

## 7. 日期管理（防RTC掉电回退）

**背景**：嵌入式设备 RTC 电池失效或掉电后，系统时间可能回退到 epoch（1970-01-01），导致日志时间戳错乱。

**实现**（`bsplog.c:451-573`）：

### 启动时恢复（`set_valid_date()`）

```
读取 /var/log/date_current（上次关机时保存的时间）
    │
    ▼
比较: 系统当前时间 + 1年  vs  备份时间
    ├── 当前时间+1年 > 备份时间  → 系统时间有效，不恢复
    └── 当前时间+1年 ≤ 备份时间  → 系统时间回退严重
            ↓
        date -s @<backup_time>  恢复时间
```

### 运行时持久化（`date_refresh_task` 线程）

- 每 60 秒将当前 `time()` 写入 `/var/log/date_current`
- 写入前做同样的回退检查，只覆盖更新、不覆盖更旧的时间

---

## 8. 完整数据流图

```
┌─────────────────────────────────────────────────────────────────────────┐
│  应用 / BSP 代码                                                         │
│                                                                          │
│  bsplog_record() ──────────────→ /mnt/log/bsplog/bsp_runlog              │
│       │                                                                  │
│  syslog_record() ────┐         共享内存去重层 (shm key=2234 + sem)       │
│      (LOG_LOCAL7)    │              ↓                                    │
│  syslog_runstate() ──┤          syslog() 系统调用                        │
│      (LOG_LOCAL5)    │              ↓                                    │
│  faultlog_record() ──┘          /dev/log (Unix socket)                   │
│                                      ↓                                   │
│  ┌──────────────  rsyslogd  ────────────────────────────────────────┐   │
│  │  facility+level 路由:                                             │   │
│  │    standard  →  /var/log/rsyslog/system/*.log                     │   │
│  │    local0~4  →  /var/log/rsyslog/app/*.log                        │   │
│  │    local5    →  /var/log/rsyslog/state/runstate.log               │   │
│  │    local6    →  /var/log/rsyslog/platform/switch.log              │   │
│  │    local7    →  /var/log/rsyslog/platform/bsp.log                 │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────  systemd-journald  ─────────────────────────────────┐   │
│  │  Storage=auto, Compress=yes, SystemMaxUse=512M                     │   │
│  │  ForwardToSyslog=yes → 转发给 rsyslog（形成双写）                   │   │
│  │  持久化路径 → /var/log/journal → (软链) → /mnt/log/bsplog/journal  │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────  轮转与归档  ──────────────────────────────────────────┐   │
│  │                                                                    │   │
│  │  [Cron 每小时]  logrotate                                           │   │
│  │    /var/log/rsyslog/*/*.log > 8M → compress + rotate 10            │   │
│  │                                                                    │   │
│  │  [后台线程 每10s]  log_package (monitor + package 双线程)            │   │
│  │    5个目录 > 128M/512M → tar.gz → /var/log/log-archive/            │   │
│  │                                                                    │   │
│  │  [首次启动]  log-manage.sh                                          │   │
│  │    tar 整个 /var/log → /var/log/archive/wap_<ts>.tar.gz            │   │
│  │    清理源 + 限制归档 ≤50个 / ≤512MB                                 │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────  日期管理  ────────────────────────────────────────────┐   │
│  │  date_refresh 线程: 每60s写入 /var/log/date_current                │   │
│  │  set_valid_date: 启动时检测回退 → date -s 恢复                      │   │
│  └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 9. 文件清单

### 源代码文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `applications/start.c` | 177 | BSP 启动主程序，系统上电后第一个可执行文件 |
| `applications/lib/bsplog/bsplog.c` | 706 | 核心日志库：BSP直写、syslog去重、故障日志、告警、日期管理 |
| `applications/lib/bsplog/log_package.c` | 266 | 日志打包归档：双线程监控+打包 |
| `applications/lib/bsplog/log_package.h` | 51 | 打包归档头文件：阈值定义、数据结构 |
| `applications/lib/bsplog/Makefile` | 12 | bsplog 库构建文件 |
| `applications/include/bsplog.h` | 110 | BSP日志API声明、路径常量、模块名宏 |
| `applications/include/logmsg.h` | 83 | syslog日志API声明、模块名宏、级别定义 |

### 根文件系统配置

| 文件 | 说明 |
|------|------|
| `rootfs/etc/rsyslog.conf` | rsyslog 主配置：模块加载、facility路由、记录格式 |
| `rootfs/etc/logrotate.conf` | 全局 logrotate 配置：weekly、rotate 4、include logrotate.d |
| `rootfs/etc/logrotate.d/customizedsetting` | 自定义日志轮转规则：8M触发、compress、copytruncate |
| `rootfs/etc/crontab` | 系统 crontab：每小时执行 logrotate |
| `rootfs/etc/systemd/journald.conf` | journald 配置：512M上限、转发syslog、info级别 |
| `rootfs/home/root/log-manage.sh` | 首次启动日志归档脚本 |

---

## 10. 关键设计要点总结

| 要点 | 说明 |
|------|------|
| **存储分层** | BSP 日志放 `/mnt/log/`（可能是独立持久化分区），syslog 日志放 `/var/log/`，journal 通过软链也纳入 `/mnt/log/` |
| **三级兜底轮转** | logrotate（小时级，8M触发） → log_package 线程（10s级，128M/512M触发） → log-manage.sh（首次启动全量归档），多层保障防止磁盘写满 |
| **跨进程安全** | System V 共享内存（key=2234）+ POSIX 命名信号量（`log_mutex`/`faultlog_mutex`）保护 syslog 去重和故障日志写入 |
| **线程同步** | 两个 POSIX 无名信号量（`sem_monitor`/`sem_package`）实现 monitor/package 线程的生产者-消费者模型 |
| **双写机制** | journald `ForwardToSyslog=yes` → 每条日志同时写入 journal 数据库和 rsyslog 文本文件 |
| **去重与合并** | 连续相同的 syslog 消息被抑制，仅记录重复次数，减少磁盘 I/O 和空间占用 |
| **嵌入式容错** | `date_refresh` 机制每60s备份时间，启动时检测 RTC 回退（>1年）并用备份时间恢复 |
| **告警滑动窗口** | `alarm_history` 保留最近 100 行告警，超出时触发 `rotate_alarm_log()` 截断 |
| **航班关联归档** | 归档文件打包时带航班号前缀，支持航班变更时触发全部目录归档 |
| **首次启动保护** | `/tmp/proc_started` 标记避免重启时误触发全量归档，只在出厂首次上电执行 |

---

*本文档基于 `/home/peijd/temp/wap2001-log-module` 目录下的源代码和配置文件分析生成。*
