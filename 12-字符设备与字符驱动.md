虽然看了前辈的 `smi_bus` 驱动代码，自己也仿写了一个 `max6635` 抽象字符驱动的代码，但是对于其中的细节还是知其然不知其所以然。

因此想要单开一个文档，专门来讲讲字符驱动的事情。顺便带着看看字符驱动是怎么被使用的，以及所谓的 `udev` 是什么。

另外我一直是有一个困惑的，就是关于驱动的生命周期绑定啊，结构体之间的关联这些，因为内核做了很多工作，我自己在写驱动时就不清楚哪些是我该做的，哪些是我不用做的...

---

有关字符驱动的部分内容，特别是源码相关的，可以移步至[驱动实例第3章](/06-驱动实例(MAX6635).md)。


# 1. 文件操作与字符设备

了解内核文件操作的原理：

[Linux内核文件系统](https://zhuanlan.zhihu.com/p/2001753017226110671#)

## 1.1. 字符设备的基石：驱动注册

字符设备驱动在初始化时必须做两件事：

- 定义并实现 `struct file_operations`（open / read / write / release 等）
- 把设备号和 `cdev` 注册进内核

典型的代码骨架：

```c
// 驱动自己的 file_operations
static struct file_operations my_fops = {
    .owner = THIS_MODULE,
    .open  = my_open,
    .read  = my_read,
    .write = my_write,
    .release = my_release,
};

static struct cdev my_cdev;

static int __init my_init(void)
{
    dev_t dev;
    int err;

    // 1. 分配设备号
    err = alloc_chrdev_region(&dev, 0, 1, "my_dev");
    // 2. 初始化 cdev 并绑定 fops
    cdev_init(&my_cdev, &my_fops);
    // 3. 将 cdev 加入内核的设备号哈希表
    err = cdev_add(&my_cdev, dev, 1);
    ...
}
```

其中 `cdev_add` 的核心作用，是把 `cdev` 放到一个全局的 `cdev_map`（基于主/次设备号的哈希表）中，供后续 open 时查找。

---

## 1.2. 用户态 open 的完整内核路径

当应用程序调用 `open("/dev/my_dev", O_RDWR)` 时，依次经过：

```
sys_open()
  → do_sys_open()
    → do_sys_openat2()
      → do_filp_open()          // 路径查找，最终拿到 inode
        → path_openat()
          → do_tmpfile()/do_o_path()/do_open()
            → vfs_open()
              → do_dentry_open()
```

在 `do_dentry_open()` 中，内核会从 inode 得到该文件的操作函数表，并**直接调用 inode->i_fop->open**。

**关键点**：字符设备文件（通过 mknod 或 devtmpfs 创建）的 inode 在 `init_special_inode()` 时，其 `i_fop` 已被设置为 `def_chr_fops`。  
`def_chr_fops` 只定义了一个 `open` 方法，就是 `chrdev_open`。所以，每次打开一个字符设备文件，最终都会进入：

```c
// fs/char_dev.c
static int chrdev_open(struct inode *inode, struct file *filp)
{
    const struct file_operations *fops;
    struct cdev *p;
    struct cdev *new = NULL;
    int ret = 0;

    spin_lock(&cdev_lock);
    p = inode->i_cdev;
    if (!p) {
        struct kobject *kobj;
        int idx;
        spin_unlock(&cdev_lock);
        // 根据设备号查找 cdev_map，获得 cdev
        kobj = kobj_lookup(cdev_map, inode->i_rdev, &idx);
        if (!kobj)
            return -ENXIO;          // 找不到对应设备，返回 No such device
        new = container_of(kobj, struct cdev, kobj);
        spin_lock(&cdev_lock);
        p = inode->i_cdev;
        if (!p) {
            inode->i_cdev = p = new;    // 把 cdev 填入 inode，以后直接复用
            list_add(&inode->i_devices, &p->list);
        }
    } else if (!cdev_get(p))
        ret = -ENXIO;
    spin_unlock(&cdev_lock);
    if (ret)
        return ret;

    // 获取驱动的 fops，并增加其模块引用计数
    fops = fops_get(p->ops);
    if (!fops)
        goto out_cdev_put;

    // 关键步骤：用驱动的 fops 替换 file 当前的 fops（原本是 def_chr_fops）
    replace_fops(filp, fops);

    // 如果驱动的 fops 提供了 open 方法，调用它
    if (filp->f_op->open) {
        ret = filp->f_op->open(inode, filp);
        if (ret)
            goto out_cdev_put;
    }

    return 0;

out_cdev_put:
    cdev_put(p);
    return ret;
}
```

**流程小结**：

1. 通过 `inode->i_rdev`（设备号）在 `cdev_map` 里找到 `cdev`；
2. 将 `cdev` 记录在 `inode->i_cdev` 中（加速下次 open）；
3. 把 `cdev->ops` 赋值给 `file->f_op`，此后该 `file` 的 read/write 等都走驱动的函数；
4. 调用驱动的 `open` 方法，完成硬件初始化、私有数据分配等。

这里不知道为什么上了一道保险，似乎字符设备在创建时使用的操作函数并不是 `cdev` 中我们定义好的，而是走了一个标准流程，在其中进行了一个替换。

询问了一下D老师，D老师表示这样做可以保证对于特殊的文件(主要就是块设备，字符设备这样的)内核的行为一致。在注册和初始化时不去做特殊操作。这个一致性就是在 `init_special_inode()` 在创建 inode 时，强制将 `i_fop` 设置为 `def_chr_fops` 来达成。只有在实际要打开文件时才进行替换。

有一点像写时复制。在初始化时只做必要的操作，比如注册 inode。设备号可以找到对应的驱动，从而能够找到对应的 `cdev`。只有真正打开设备文件时，内核才会去匹配这个 `cdev` 对应的 `fops`并使用接口。

因为打开文件是所有文件操作的第一步，因此在完成 `f_ops` 的替换后，之后的读写或者 ioctl 操作就已经用我们的驱动定义的操作替换了~

---

## 1.3. 读写操作的内核路径

对于常规的 read：

```c
sys_read()
  → ksys_read()
    → vfs_read()
```

`vfs_read()` 的核心逻辑：

```c
// fs/read_write.c
ssize_t vfs_read(struct file *file, char __user *buf, size_t count, loff_t *pos)
{
    ssize_t ret;

    if (!(file->f_mode & FMODE_READ))
        return -EBADF;
    if (!file->f_op || !file->f_op->read)
        return -EINVAL;

    // 调用驱动注册的 read 方法
    ret = file->f_op->read(file, buf, count, pos);
    if (ret > 0)
        fsnotify_access(file);    // 通知文件访问事件
    return ret;
}
```

**关键细节**：

- `file->f_op` 已经被 `chrdev_open` 替换为驱动的 `fops`，所以这里的 `file->f_op->read` 就是 `my_read`。
- `pos` 参数是 `file->f_pos` 的地址，驱动中通常需要 `*pos` 来更新当前读写位置。

对于 write，过程几乎一样：

```c
sys_write() → ksys_write() → vfs_write()
// 同样调用 file->f_op->write(file, buf, count, pos)
```

注意：如果驱动实现的是 `read_iter` / `write_iter`，`vfs_read`/`vfs_write` 也会优先调用它们（通过 `do_iter_readv_writev`），现代内核更推荐使用 `read_iter/write_iter`，但经典字符设备仍可用 `read/write`。

---

## 1.4. 关闭设备文件

应用调用 `close(fd)` 或进程退出时：

```c
sys_close()
  → __close_fd()
    → filp_close()
      → fput()
        → __fput()
```

在 `__fput()` 中：

```c
if (file->f_op->release)
    file->f_op->release(inode, file);
```

也就是调用驱动的 `release`（对应 `file_operations` 里的 `.release` 成员）。  
最后 `cdev_put` 减少 `cdev` 引用，如果引用计数归零则释放模块依赖等。

---

## 1.5. 关键数据结构一览

```c
// include/linux/cdev.h
struct cdev {
    struct kobject kobj;
    struct module *owner;
    const struct file_operations *ops;  // 驱动的 fops
    struct list_head list;
    dev_t dev;
    unsigned int count;
} __randomize_layout;

// include/linux/fs.h
struct file_operations {
    struct module *owner;
    ssize_t (*read) (struct file *, char __user *, size_t, loff_t *);
    ssize_t (*write) (struct file *, const char __user *, size_t, loff_t *);
    int (*open) (struct inode *, struct file *);
    int (*release) (struct inode *, struct file *);
    ...
};

// include/linux/fs.h (部分)
struct inode {
    dev_t i_rdev;          // 设备号
    struct cdev *i_cdev;   // 指向关联的 cdev（首次 open 后设置）
    struct file_operations *i_fop;  // 对于设备文件初始为 def_chr_fops
    ...
};

struct file {
    const struct file_operations *f_op;  // 打开后替换为驱动的 fops
    loff_t f_pos;
    void *private_data;     // 驱动通常用它存每文件实例的数据
    ...
};
```

---

## 1.6. 整体调用流程图（文字版）

```
用户态：open("/dev/my_dev", ...)
  → sys_open
    → do_sys_open
      → 路径解析 → 得到 inode（i_fop = def_chr_fops）
        → do_dentry_open
          → inode->i_fop->open  =>  chrdev_open()
             - 根据 inode->i_rdev 查 cdev_map 得到 cdev
             - file->f_op = cdev->ops
             - 调用 cdev->ops->open（驱动的 my_open）
  → fd = 返回的文件描述符

用户态：read(fd, buf, size)
  → sys_read
    → vfs_read
      → file->f_op->read  =>  my_read()

用户态：write(fd, buf, size)
  → sys_write
    → vfs_write
      → file->f_op->write  =>  my_write()

用户态：close(fd)
  → sys_close
    → fput → __fput
      → file->f_op->release => my_release()
```

---

## 1.7. 字符设备的注册与创建

这里可以看一看 arm 的系统调用情况：

* [Linux内核学习之 -- ARMv8架构的系统调用笔记](https://blog.csdn.net/xi_xix_i/article/details/140383166)

这里面讲了讲系统调用在 ARMv8 架构下的具体的定义和调用流程。

```c
// 1. 内核层面注册
alloc_chrdev_region(&dev, 0, 1, "my_dev");
cdev_init(&my_cdev, &my_fops);
cdev_add(&my_cdev, dev, 1);

// 2. 用户空间层面创建节点
my_class = class_create(THIS_MODULE, "my_class");
device_create(my_class, NULL, dev, NULL, "my_dev");
```

在驱动的 `probe` 中字符驱动的注册创建就是上面这几个核心函数。
它们一般情况下都是一起使用的，但是其实用户空间的文件是可以手动创建的，这是原来没有设备模型时的老方法，已经被摈弃了。

### 用户空间的节点创建

内核层面注册其实还比较好看懂，因为代码是可以直接跳转的，而用户空间的比较麻烦了，直接看代码有点追不到。

调用链从 

```c
device_create -> device_create_groups_vargs ->
device_add -> devtmpfs_create_node -> devtmpfs_submit_req
```

这里似乎和一个叫做 `devtmpfs` 的文件系统有关，它应该是一个内存上的临时文件系统。这里最后的 `devtmpfs_submit_req` 似乎是将请求提交给了另一个线程。询问了D老师，告诉我 `devtmpfs` 有一个专用的内核线程，请求将交给它处理。

这个线程可以在 `drivers/base/devtmpfs.c` 中找到初始化，其实和前面的函数在一个文件中。

```c
static struct task_struct *thread;

int __init devtmpfs_init(void)
{
    ...
    thread = kthread_run(devtmpfsd, &err, "kdevtmpfs");
}
```

这个线程的函数名就是 `devtmpfsd`。

```c
static int __ref devtmpfsd(void *p)
{
	int err = devtmpfs_setup(p);

	complete(&setup_done);
	if (err)
		return err;
	devtmpfs_work_loop();
	return 0;
}
```

我们就继续往里看:

```c
devtmpfs_work_loop -> handle -> handle_create -> vfs_mknod
```

这里才最终进入 `mknod` 方法，但是又不好搞了，因为 `vfs_mknod` 中调用的是 `dir->i_op->mknod`。也就是文件系统目录 inode 对应的 `mknod` 函数指针，这里的 `/dev` 是 devtmpfs，所以我们得回去看这个文件系统的操作函数。结果发现：

```c
/* drivers/base/devtmpfs.c */
static struct file_system_type internal_fs_type = {
	.name = "devtmpfs",
#ifdef CONFIG_TMPFS
	.init_fs_context = shmem_init_fs_context,
#else
	.init_fs_context = ramfs_init_fs_context,
#endif
	.kill_sb = kill_litter_super,
};
```

devtmpfs 实际上是一个 shmem 文件系统套壳，那么我们又去看 shmem 文件系统吧。

```c
/* mm/shmem.c */
static const struct inode_operations shmem_dir_inode_operations = {
#ifdef CONFIG_TMPFS
	.mknod		= shmem_mknod,
};
```

这里我们需要注意是目录的 inode 操作不是文件的 inode 操作。然后继续看 `shmem_mknod` 内部。

中间也是稍微追了一下：

```c
shmem_mknod -> shmem_get_inode -> init_special_inode

void init_special_inode(struct inode *inode, umode_t mode, dev_t rdev)
{
	inode->i_mode = mode;
	if (S_ISCHR(mode)) {
		inode->i_fop = &def_chr_fops;
		inode->i_rdev = rdev;
	} else if (S_ISBLK(mode)) {
		inode->i_fop = &def_blk_fops;
		inode->i_rdev = rdev;
	} else if (S_ISFIFO(mode))
		inode->i_fop = &pipefifo_fops;
	else if (S_ISSOCK(mode))
		;	/* leave it no_open_fops */
	else
		printk(KERN_DEBUG "init_special_inode: bogus i_mode (%o) for"
				  " inode %s:%lu\n", mode, inode->i_sb->s_id,
				  inode->i_ino);
}
```

这里对字符设备有： `inode->i_fop = &def_chr_fops`：

```c
/* fs/char_dev.c */
const struct file_operations def_chr_fops = {
	.open = chrdev_open,
	.llseek = noop_llseek,
};
```

终于也是找到了到底是如何将 inode 的 open操作绑定成一个通用的字符设备操作的。下面给出一个完整的调用链：

```
device_add()
 └─ devtmpfs_create_node(dev)
    └─ devtmpfs_submit_req(&req, tmp)
       └─ [内核线程 kdevtmpfs] handle(req)
          └─ devtmpfs_mknod(req)                     // drivers/base/devtmpfs.c
             └─ vfs_mknod(..., req->mode, req->devt) // fs/namei.c
                └─ dir->i_op->mknod(...)             // 父目录 inode 的 mknod
                   │                                 // dir->i_op = shmem_dir_inode_operations
                   └─ shmem_mknod()                  // mm/shmem.c
                      └─ shmem_get_inode(sb, dir, mode, dev, ...)
                         └─ switch (mode & S_IFMT)
                            └─ default:
                               └─ init_special_inode(inode, mode, dev)
                                  └─ if (S_ISCHR(mode))
                                     inode->i_fop = &def_chr_fops;
                                     inode->i_rdev = dev;
```

不过我们最初的问题还没有解决：**用户空间的节点创建**(差点给忘了...)

首先我们需要明白一个用户空间可见的文件必须具有：

* inode 索引节点，包含文件类型、权限、设备号、`f_ops` 等，它是核心。
* dentry 目录项，把 inode 挂到文件系统目录树中。
* 文件系统被挂载，其根目录项能够在进程的命名空间可见。

在 `shmem_mknod` 里，创建的是 `inode`；
让它变成用户空间的文件，靠的是 `devtmpfs` 已经挂载到 `/dev`，并且内核线程通过 `vfs_mknod` 在 `devtmpfs` 的根目录下创建了 `dentry` 并关联了 `inode`。
于是 `/dev/my_de`v 就在用户空间诞生，可以像普通文件一样被 `open` 了。

**这里的流程可能有一些些微的问题，但是我们基本已经知道了流程**，对于目前的我来说足够了。

这条路就是内核通过 `device_create` 来一步一步直接在 `/dev` 下创建用户空间下的设备节点的过程，是通过 `devtmpfs` 文件系统来完成的。这个似乎与 `udev` 还不太一样。

---


# 2. `udev`

